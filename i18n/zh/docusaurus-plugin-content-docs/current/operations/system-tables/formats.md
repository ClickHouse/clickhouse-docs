---
description: '包含所有格式的列表，以及用于指示各格式是否适用于输入/输出、是否支持并行化的标志。'
keywords: ['系统表', '格式']
slug: /operations/system-tables/formats
title: 'system.formats'
doc_type: 'reference'
---

## 描述 \{#description\}

列出所有格式，并标明各格式是否适用于输入/输出，以及是否支持并行化。

## 列 \{#columns\}

{/*AUTOGENERATED_START*/ }

* `name` ([String](../../sql-reference/data-types/)) — 格式名称。
* `is_input` ([UInt8](../../sql-reference/data-types/)) — 表示该格式是否适用于数据输入的标志。
* `is_output` ([UInt8](../../sql-reference/data-types/)) — 表示该格式是否适用于数据输出的标志。
* `supports_parallel_parsing` ([UInt8](../../sql-reference/data-types/)) — 表示该格式是否支持并行解析的标志。
* `supports_parallel_formatting` ([UInt8](../../sql-reference/data-types/)) — 表示该格式是否支持并行格式化的标志。
* `is_tty_friendly` ([UInt8](../../sql-reference/data-types/)) — 表示该格式通常是否能在终端中正常显示的标志。对于其他格式，CLI 会在输出前进行询问。
* `content_type` ([String](../../sql-reference/data-types/)) — 与输出格式对应的 HTTP Content-Type。可能取决于当前的格式设置。
* `supports_random_access` ([UInt8](../../sql-reference/data-types/)) — 表示该格式是否支持在输入中随机访问的标志。
* `has_schema_inference` ([UInt8](../../sql-reference/data-types/)) — 该格式可以从数据中动态推断 schema (无论是从嵌入的头部信息/元数据中，还是从数据片段中) 。
* `has_external_schema` ([UInt8](../../sql-reference/data-types/)) — 该格式要么具有固定 schema，要么接受以其自身格式定义的预定义 schema。
* `prefers_large_blocks` ([UInt8](../../sql-reference/data-types/)) — 该格式会在输出时写入较大的块，并在输入时生成较大的块。
* `supports_append` ([UInt8](../../sql-reference/data-types/)) — 该格式支持向单个文件追加写入。
* `supports_subsets_of_columns` ([UInt8](../../sql-reference/data-types/)) — 该输入格式能够识别某些列被省略的情况。

{/*AUTOGENERATED_END*/ }