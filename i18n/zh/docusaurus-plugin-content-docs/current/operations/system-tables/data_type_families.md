---
description: '包含已支持数据类型信息的系统表'
keywords: ['system table', 'data_type_families']
slug: /operations/system-tables/data_type_families
title: 'system.data_type_families'
doc_type: 'reference'
---

## 描述 \{#description\}

包含受支持[数据类型](../../sql-reference/data-types/index.md)的信息。

## 列 \{#columns\}

{/*AUTOGENERATED_START*/ }

* `name` ([String](../../sql-reference/data-types/)) — 数据类型名称。
* `case_insensitive` ([UInt8](../../sql-reference/data-types/)) — 该属性显示是否可以在查询中以不区分大小写的方式使用数据类型名称。例如，`Date` 和 `date` 都是有效的。
* `alias_to` ([String](../../sql-reference/data-types/)) — `name` 作为其别名所对应的数据类型名称。

{/*AUTOGENERATED_END*/ }

## 示例 \{#example\}

```sql
SELECT * FROM system.data_type_families WHERE alias_to = 'String'
```

```text
┌─name───────┬─case_insensitive─┬─alias_to─┐
│ LONGBLOB   │                1 │ String   │
│ LONGTEXT   │                1 │ String   │
│ TINYTEXT   │                1 │ String   │
│ TEXT       │                1 │ String   │
│ VARCHAR    │                1 │ String   │
│ MEDIUMBLOB │                1 │ String   │
│ BLOB       │                1 │ String   │
│ TINYBLOB   │                1 │ String   │
│ CHAR       │                1 │ String   │
│ MEDIUMTEXT │                1 │ String   │
└────────────┴──────────────────┴──────────┘
```

## 另请参阅 \{#see-also\}

* [Syntax](../../sql-reference/syntax.md) — 关于支持的语法的信息。