---
slug: /managing-data/delete_mutations
sidebar_label: 'Delete mutations'
title: '删除变更'
hide_title: false
description: '介绍删除变更（通过删除操作修改表数据的 ALTER 查询）的页面'
keywords: ['delete mutations', 'ALTER TABLE DELETE', 'data mutations', 'data parts rewrite', 'ReplacingMergeTree']
doc_type: 'reference'
---

删除变更是指通过删除操作来修改表数据的 `ALTER` 查询。最典型的例如 `ALTER TABLE DELETE` 等查询。执行此类查询会生成数据分片的新变更版本。这意味着，此类语句会对变更之前已写入的所有数据分片进行整体重写，从而产生大量写入请求。

:::info
对于删除操作，可以通过使用 [ReplacingMergeTree](/guides/replacing-merge-tree) 或 [CollapsingMergeTree](/engines/table-engines/mergetree-family/collapsingmergetree) 等专用表引擎来替代默认的 MergeTree 表引擎，以避免产生如此大量的写入请求。
:::

import DeleteMutations from '@site/docs/sql-reference/statements/alter/delete.md';

<DeleteMutations />
