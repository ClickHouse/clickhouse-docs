---
slug: /managing-data/update_mutations
sidebar_label: 'Update mutations'
title: '更新变更'
hide_title: false
description: '介绍更新变更的页面——通过更新操作来修改表数据的 ALTER 查询'
doc_type: 'reference'
keywords: ['update', 'mutations', 'alter table', 'data manipulation', 'modify data']
---

更新变更是指通过更新操作来修改表数据的 `ALTER` 查询。最典型的例子是 `ALTER TABLE UPDATE` 等查询。执行此类查询会生成数据分片的新变更版本。这意味着，此类语句会对变更之前已写入的所有数据分片进行整体重写，从而导致大量写入请求。

:::info
对于更新操作，你可以通过使用诸如 [ReplacingMergeTree](/guides/replacing-merge-tree) 或 [CollapsingMergeTree](/engines/table-engines/mergetree-family/collapsingmergetree) 这类专用表引擎来替代默认的 MergeTree 表引擎，从而避免产生如此大量的写入请求。
:::

import UpdateMutations from '@site/docs/sql-reference/statements/alter/update.md';

<UpdateMutations />
