---
slug: /managing-data/update_mutations
sidebar_label: 'Update mutations'
title: '更新变更（Update mutations）'
hide_title: false
description: '介绍更新变更的页面——通过更新来操作表数据的 ALTER 查询'
doc_type: 'reference'
keywords: ['update', 'mutations', 'alter table', 'data manipulation', 'modify data']
---

更新变更指的是通过更新来操作表数据的 `ALTER` 查询，最典型的例子是 `ALTER TABLE UPDATE` 等。执行这类查询会生成数据分片的新的变更版本。这意味着，此类语句会对在变更之前插入的所有数据所在的整个数据分片进行重写，从而产生大量写请求。

:::info
对于更新操作，你可以使用专门的表引擎（如 [ReplacingMergeTree](/guides/replacing-merge-tree) 或 [CollapsingMergeTree](/engines/table-engines/mergetree-family/collapsingmergetree)）来替代默认的 MergeTree 表引擎，从而避免产生如此大量的写请求。
:::

import UpdateMutations from '@site/docs/sql-reference/statements/alter/update.md';

<UpdateMutations />
