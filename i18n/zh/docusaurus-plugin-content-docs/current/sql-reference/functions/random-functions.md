---
description: '随机数生成函数文档'
sidebar_label: '随机数'
slug: /sql-reference/functions/random-functions
title: '随机数生成函数'
doc_type: 'reference'
---



# 生成随机数的函数

本节中的所有函数接受零个或一个参数。参数(如果提供)的唯一作用是防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination),从而确保同一行中对同一随机函数的两次不同调用返回不同的随机值。

相关内容

- 指南:[在 ClickHouse 中生成随机数据](/guides/generating-test-data)
- 博客:[在 ClickHouse 中生成随机数据](https://clickhouse.com/blog/generating-random-test-distribution-data-for-clickhouse)

:::note
随机数由非加密算法生成。
:::

:::note
以下文档由 `system.functions` 系统表生成。
:::

<!--
The inner content of the tags below are replaced at doc framework build time with
docs generated from system.functions. Please do not modify or remove the tags.
See: https://github.com/ClickHouse/clickhouse-docs/blob/main/contribute/autogenerated-documentation-from-source.md
-->


<!--AUTOGENERATED_START-->

## fuzzBits {#fuzzBits}

引入版本:v20.5

以概率 `p` 翻转输入字符串 `s` 的每个比特位。

**语法**

```sql
fuzzBits(s, p)
```

**参数**

- `s` — 要进行比特位模糊处理的字符串或固定长度字符串 [`String`](/sql-reference/data-types/string) 或 [`FixedString`](/sql-reference/data-types/fixedstring)
- `p` — 翻转每个比特位的概率,取值范围为 `0.0` 到 `1.0` [`Float*`](/sql-reference/data-types/float)

**返回值**

返回与 `s` 类型相同的模糊处理后的字符串。[`String`](/sql-reference/data-types/string) 或 [`FixedString`](/sql-reference/data-types/fixedstring)

**示例**

**使用示例**

```sql title=查询
SELECT fuzzBits(materialize('abacaba'), 0.1)
FROM numbers(3)
```

```response title=响应
┌─fuzzBits(materialize('abacaba'), 0.1)─┐
│ abaaaja                               │
│ a*cjab+                               │
│ aeca2A                                │
└───────────────────────────────────────┘
```


## rand {#rand}

引入版本:v1.1

返回一个均匀分布的随机 `UInt32` 数字。

使用线性同余生成器,其初始状态从系统获取,这意味着虽然看起来是随机的,但并非真正随机,如果初始状态已知则可以预测。
对于需要真正随机性的场景,请考虑使用替代方法,如系统级调用或集成外部库。

**语法**

```sql
rand([x])
```

**别名**:`rand32`

**参数**

- `x` — 可选且被忽略。该参数的唯一目的是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回 `UInt32` 类型的随机数。[`UInt32`](/sql-reference/data-types/int-uint)

**示例**

**使用示例**

```sql title=Query
SELECT rand();
```

```response title=Response
1569354847
```


## rand64 {#rand64}

引入版本：v1.1

返回一个均匀分布的随机 `UInt64` 数值。

使用线性同余生成器，其初始状态从系统获取，这意味着虽然看起来是随机的，但并非真正的随机数，如果初始状态已知则结果可预测。
对于需要真正随机性的场景，建议使用替代方法，例如系统级调用或集成外部库。

**语法**

```sql
rand64([x])
```

**参数**

- `x` — 可选参数，会被忽略。该参数的唯一作用是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回一个均匀分布的随机 UInt64 数值。[`UInt64`](/sql-reference/data-types/int-uint)

**示例**

**使用示例**

```sql title=Query
SELECT rand64();
```

```response title=Response
15030268859237645412
```


## randBernoulli {#randBernoulli}

引入版本：v22.10

返回从[伯努利分布](https://en.wikipedia.org/wiki/Bernoulli_distribution)中抽取的随机 Float64 数值。

**语法**

```sql
randBernoulli(probability[, x])
```

**参数**

- `probability` — 成功概率，取值范围为 `0` 到 `1`。[`Float64`](/sql-reference/data-types/float)
- `x` — 可选参数，将被忽略。该参数的唯一作用是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回从指定伯努利分布中抽取的随机 Float64 数值。[`UInt64`](/sql-reference/data-types/int-uint)

**示例**

**使用示例**

```sql title=Query
SELECT randBernoulli(.75) FROM numbers(5)
```

```response title=Response
┌─randBernoulli(0.75)─┐
│                   1 │
│                   1 │
│                   0 │
│                   1 │
│                   1 │
└─────────────────────┘
```


## randBinomial {#randBinomial}

引入版本:v22.10

返回从[二项分布](https://en.wikipedia.org/wiki/Binomial_distribution)中抽取的随机 Float64 数值。

**语法**

```sql
randBinomial(experiments, probability[, x])
```

**参数**

- `experiments` — 试验次数 [`UInt64`](/sql-reference/data-types/int-uint)
- `probability` — 每次试验成功的概率,取值范围为 `0` 到 `1` [`Float64`](/sql-reference/data-types/float)
- `x` — 可选参数,将被忽略。该参数的唯一作用是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回从指定二项分布中抽取的随机 Float64 数值。[`UInt64`](/sql-reference/data-types/int-uint)

**示例**

**使用示例**

```sql title=Query
SELECT randBinomial(100, .75) FROM numbers(5)
```

```response title=Response
┌─randBinomial(100, 0.75)─┐
│                      74 │
│                      78 │
│                      76 │
│                      77 │
│                      80 │
└─────────────────────────┘
```


## randCanonical {#randCanonical}

引入版本：v22.11

返回一个服从均匀分布的随机 `Float64` 数值，取值范围为 `0`（包含）到 `1`（不包含）。

**语法**

```sql
randCanonical([x])
```

**参数**

- `x` — 可选参数，将被忽略。该参数的唯一作用是在查询中多次调用相同函数时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回一个随机 Float64 数值。[`Float64`](/sql-reference/data-types/float)

**示例**

**使用示例**

```sql title=Query
SELECT randCanonical();
```

```response title=Response
0.345217890123456
```


## randChiSquared {#randChiSquared}

引入版本：v22.10

返回从[卡方分布](https://en.wikipedia.org/wiki/Chi-squared_distribution)中抽取的随机 Float64 数值。

**语法**

```sql
randChiSquared(degree_of_freedom[, x])
```

**参数**

- `degree_of_freedom` — 自由度。[`Float64`](/sql-reference/data-types/float)
- `x` — 可选且会被忽略。该参数的唯一作用是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回从指定卡方分布中抽取的随机 Float64 数值。[`Float64`](/sql-reference/data-types/float)

**示例**

**使用示例**

```sql title=Query
SELECT randChiSquared(10) FROM numbers(5)
```

```response title=Response
┌─randChiSquared(10)─┐
│ 10.015463656521543 │
│  9.621799919882768 │
│   2.71785015634699 │
│ 11.128188665931908 │
│  4.902063104425469 │
└────────────────────┘
```


## randConstant {#randConstant}

引入版本：v1.1

生成一个在当前查询执行期间对所有行保持不变的随机值。

此函数：

- 在单次查询中为每一行返回相同的随机值
- 在不同的查询执行中生成不同的值

它适用于在数据集的所有行中应用一致的随机种子或标识符。

**语法**

```sql
randConstant([x])
```

**参数**

- `x` — 可选参数，会被忽略。该参数的唯一作用是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回一个 `UInt32` 类型的列，每行包含相同的随机值。[`UInt32`](/sql-reference/data-types/int-uint)

**示例**

**基本用法**

```sql title=查询
SELECT randConstant() AS random_value;
```

```response title=响应
| random_value |
|--------------|
| 1234567890   |
```

**带参数的用法**

```sql title=查询
SELECT randConstant(10) AS random_value;
```

```response title=响应
| random_value |
|--------------|
| 9876543210   |
```


## randExponential {#randExponential}

引入版本：v22.10

返回从[指数分布](https://en.wikipedia.org/wiki/Exponential_distribution)中抽取的随机 Float64 数值。

**语法**

```sql
randExponential(lambda[, x])
```

**参数**

- `lambda` — 分布的速率参数或 lambda 值 [`Float64`](/sql-reference/data-types/float)
- `x` — 可选参数，将被忽略。该参数的唯一作用是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回从指定指数分布中抽取的随机 Float64 数值。[`Float64`](/sql-reference/data-types/float)

**示例**

**使用示例**

```sql title=Query
SELECT randExponential(1/10) FROM numbers(5)
```

```response title=Response
┌─randExponential(divide(1, 10))─┐
│              44.71628934340778 │
│              4.211013337903262 │
│             10.809402553207766 │
│              15.63959406553284 │
│             1.8148392319860158 │
└────────────────────────────────┘
```


## randFisherF {#randFisherF}

引入版本：v22.10

返回从 [F 分布](https://en.wikipedia.org/wiki/F-distribution)中抽取的随机 Float64 数值。

**语法**

```sql
randFisherF(d1, d2[, x])
```

**参数**

- `d1` — `X = (S1 / d1) / (S2 / d2)` 中的 d1 自由度。[`Float64`](/sql-reference/data-types/float)
- `d2` — `X = (S1 / d1) / (S2 / d2)` 中的 d2 自由度。[`Float64`](/sql-reference/data-types/float)
- `x` — 可选参数，会被忽略。该参数的唯一作用是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回从指定 F 分布中抽取的随机 Float64 数值。[`Float64`](/sql-reference/data-types/float)

**示例**

**使用示例**

```sql title=Query
SELECT randFisherF(10, 3) FROM numbers(5)
```

```response title=Response
┌─randFisherF(10, 20)─┐
│  0.7204609609506184 │
│  0.9926258472572916 │
│  1.4010752726735863 │
│ 0.34928401507025556 │
│  1.8216216009473598 │
└─────────────────────┘
```


## randLogNormal {#randLogNormal}

引入版本:v22.10

返回从[对数正态分布](https://en.wikipedia.org/wiki/Log-normal_distribution)中抽取的随机 Float64 数值。

**语法**

```sql
randLogNormal(mean, stddev[, x])
```

**参数**

- `mean` — 分布的均值。[`Float64`](/sql-reference/data-types/float)
- `stddev` — 分布的标准差。[`Float64`](/sql-reference/data-types/float)
- `x` — 可选参数,会被忽略。该参数的唯一作用是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回从指定对数正态分布中抽取的随机 Float64 数值。[`Float64`](/sql-reference/data-types/float)

**示例**

**使用示例**

```sql title=查询
SELECT randLogNormal(100, 5) FROM numbers(5)
```

```response title=响应
┌─randLogNormal(100, 5)─┐
│  1.295699673937363e48 │
│  9.719869109186684e39 │
│  6.110868203189557e42 │
│  9.912675872925529e39 │
│ 2.3564708490552458e42 │
└───────────────────────┘
```


## randNegativeBinomial {#randNegativeBinomial}

引入版本：v22.10

返回从[负二项分布](https://en.wikipedia.org/wiki/Negative_binomial_distribution)中抽取的随机 Float64 数值。

**语法**

```sql
randNegativeBinomial(experiments, probability[, x])
```

**参数**

- `experiments` — 实验次数。[`UInt64`](/sql-reference/data-types/int-uint)
- `probability` — 每次实验失败的概率，取值范围为 `0` 到 `1`。[`Float64`](/sql-reference/data-types/float)
- `x` — 可选参数，将被忽略。该参数的唯一作用是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回从指定负二项分布中抽取的随机 Float64 数值 [`UInt64`](/sql-reference/data-types/int-uint)

**示例**

**使用示例**

```sql title=查询
SELECT randNegativeBinomial(100, .75) FROM numbers(5)
```

```response title=响应
┌─randNegativeBinomial(100, 0.75)─┐
│                              33 │
│                              32 │
│                              39 │
│                              40 │
│                              50 │
└─────────────────────────────────┘
```


## randNormal {#randNormal}

引入版本:v22.10

返回从[正态分布](https://en.wikipedia.org/wiki/Normal_distribution)中抽取的随机 Float64 数值。

**语法**

```sql
randNormal(mean, stddev[, x])
```

**参数**

- `mean` — 分布的均值 [`Float64`](/sql-reference/data-types/float)
- `stddev` — 分布的标准差 [`Float64`](/sql-reference/data-types/float)
- `x` — 可选参数,将被忽略。该参数的唯一作用是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回从指定正态分布中抽取的随机 Float64 数值。[`Float64`](/sql-reference/data-types/float)

**示例**

**使用示例**

```sql title=Query
SELECT randNormal(10, 2) FROM numbers(5)
```

```response title=Response
┌──randNormal(10, 2)─┐
│ 13.389228911709653 │
│  8.622949707401295 │
│ 10.801887062682981 │
│ 4.5220192605895315 │
│ 10.901239123982567 │
└────────────────────┘
```


## randPoisson {#randPoisson}

引入版本:v22.10

返回从[泊松分布](https://en.wikipedia.org/wiki/Poisson_distribution)中抽取的随机 Float64 数值。

**语法**

```sql
randPoisson(n[, x])
```

**参数**

- `n` — 平均发生次数。[`UInt64`](/sql-reference/data-types/int-uint)
- `x` — 可选参数,将被忽略。该参数的唯一作用是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回从指定泊松分布中抽取的随机 Float64 数值。[`UInt64`](/sql-reference/data-types/int-uint)

**示例**

**使用示例**

```sql title=Query
SELECT randPoisson(10) FROM numbers(5)
```

```response title=Response
┌─randPoisson(10)─┐
│               8 │
│               8 │
│               7 │
│              10 │
│               6 │
└─────────────────┘
```


## randStudentT {#randStudentT}

引入版本：v22.10

返回从 [Student's t 分布](https://en.wikipedia.org/wiki/Student%27s_t-distribution)中抽取的随机 Float64 数值。

**语法**

```sql
randStudentT(degree_of_freedom[, x])
```

**参数**

- `degree_of_freedom` — 自由度。[`Float64`](/sql-reference/data-types/float)
- `x` — 可选参数，将被忽略。该参数的唯一作用是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回从指定 Student's t 分布中抽取的随机 Float64 数值。[`Float64`](/sql-reference/data-types/float)

**示例**

**使用示例**

```sql title=Query
SELECT randStudentT(10) FROM numbers(5)
```

```response title=Response
┌─────randStudentT(10)─┐
│   1.2217309938538725 │
│   1.7941971681200541 │
│ -0.28192176076784664 │
│   0.2508897721303792 │
│  -2.7858432909761186 │
└──────────────────────┘
```


## randUniform {#randUniform}

引入版本:v22.10

返回从区间 $[\min, \max]$ 中均匀抽取的随机 Float64 数值。

**语法**

```sql
randUniform(min, max[, x])
```

**参数**

- `min` — 范围的左边界(包含)。[`Float64`](/sql-reference/data-types/float)
- `max` — 范围的右边界(包含)。[`Float64`](/sql-reference/data-types/float)
- `x` — 可选参数,会被忽略。该参数的唯一作用是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回从由 `min` 和 `max` 构成的区间中均匀抽取的随机数值。[`Float64`](/sql-reference/data-types/float)

**示例**

**使用示例**

```sql title=Query
SELECT randUniform(5.5, 10) FROM numbers(5)
```

```response title=Response
┌─randUniform(5.5, 10)─┐
│    8.094978491443102 │
│   7.3181248914450885 │
│    7.177741903868262 │
│    6.483347380953762 │
│    6.122286382885112 │
└──────────────────────┘
```


## randomFixedString {#randomFixedString}

引入版本:v20.5

生成指定字节长度的随机固定长度字符串。
返回的字符不一定是 ASCII 字符,即可能无法打印。

**语法**

```sql
randomFixedString(length)
```

**参数**

- `length` — 字符串的字节长度。[`UInt*`](/sql-reference/data-types/int-uint)

**返回值**

返回填充了随机字节的字符串。[`FixedString`](/sql-reference/data-types/fixedstring)

**示例**

**使用示例**

```sql title=Query
SELECT randomFixedString(13) AS rnd, toTypeName(rnd)
```

```response title=Response
┌─rnd──────┬─toTypeName(randomFixedString(13))─┐
│ j▒h㋖HɨZ'▒ │ FixedString(13)                 │
└──────────┴───────────────────────────────────┘
```


## randomPrintableASCII {#randomPrintableASCII}

引入版本:v20.1

生成包含指定字符数的随机 [ASCII](https://en.wikipedia.org/wiki/ASCII#Printable_characters) 字符串。

如果传递 `length < 0`,函数的行为未定义。

**语法**

```sql
randomPrintableASCII(length[, x])
```

**参数**

- `length` — 字符串长度(以字节为单位)。[`(U)Int*`](/sql-reference/data-types/int-uint)
- `x` — 可选参数,会被忽略。该参数的唯一作用是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回包含随机 ASCII 可打印字符集的字符串。[`String`](/sql-reference/data-types/string)

**示例**

**使用示例**

```sql title=查询
SELECT number, randomPrintableASCII(30) AS str, length(str) FROM system.numbers LIMIT 3
```

```response title=响应
┌─number─┬─str────────────────────────────┬─length(randomPrintableASCII(30))─┐
│      0 │ SuiCOSTvC0csfABSw=UcSzp2.`rv8x │                               30 │
│      1 │ 1Ag NlJ &RCN:*>HVPG;PE-nO"SUFD │                               30 │
│      2 │ /"+<"with:=LjJ Vm!c&hI*m#XTfzz │                               30 │
└────────┴────────────────────────────────┴──────────────────────────────────┘
```


## randomString {#randomString}

引入版本:v20.5

生成指定字符数的随机字符串。
返回的字符不一定是 ASCII 字符,即可能无法打印。

**语法**

```sql
randomString(length[, x])
```

**参数**

- `length` — 字符串的字节长度。[`(U)Int*`](/sql-reference/data-types/int-uint)
- `x` — 可选且被忽略的参数。该参数的唯一作用是在查询中多次使用相同函数调用时防止[公共子表达式消除](/sql-reference/functions/overview#common-subexpression-elimination)。[`Any`](/sql-reference/data-types)

**返回值**

返回填充了随机字节的字符串。[`String`](/sql-reference/data-types/string)

**示例**

**使用示例**

```sql title=Query
SELECT randomString(5) AS str FROM numbers(2)
```

```response title=Response
���
�v6B�
```


## randomStringUTF8 {#randomStringUTF8}

引入版本:v20.5

生成一个包含指定码点数量的随机 [UTF-8](https://en.wikipedia.org/wiki/UTF-8) 字符串。
不会返回未分配 [平面](<https://en.wikipedia.org/wiki/Plane_(Unicode)>)(平面 4 至 13)中的码点。
与 ClickHouse 服务器交互的客户端仍可能无法正确显示生成的 UTF-8 字符串。

**语法**

```sql
randomStringUTF8(length)
```

**参数**

- `length` — 字符串的码点长度。[`(U)Int*`](/sql-reference/data-types/int-uint)

**返回值**

返回一个由随机 UTF-8 码点组成的字符串。[`String`](/sql-reference/data-types/string)

**示例**

**使用示例**

```sql title=查询
SELECT randomStringUTF8(13)
```

```response title=响应
┌─randomStringUTF8(13)─┐
│ 𘤗𙉝д兠庇󡅴󱱎󦐪􂕌𔊹𓰛       │
└──────────────────────┘
```

<!--AUTOGENERATED_END-->
