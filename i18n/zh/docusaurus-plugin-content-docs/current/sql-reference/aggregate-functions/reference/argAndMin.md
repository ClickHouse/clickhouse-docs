---
description: '计算最小 `val` 对应的 `arg` 和 `val`。如果有多行的最小 `val` 相同，则返回的那一组对应 `arg` 和 `val` 是非确定的。'
slug: /sql-reference/aggregate-functions/reference/argandmin
title: 'argAndMin'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## argAndMin \{#argAndMin\}

引入于：v1.1

计算最小 `val` 对应的 `arg` 和 `val`。如果有多行的 `val` 相同且都是最小值，则返回哪一行对应的 `arg` 和 `val` 是不确定的。
`arg` 部分和 `min` 部分都像[聚合函数](/sql-reference/aggregate-functions/index.md)一样工作，它们在处理过程中都会[跳过 `Null`](/sql-reference/aggregate-functions/index.md#null-processing)，并在存在非 `Null` 值时返回非 `Null` 值。

:::note
与 `argMin` 的唯一区别是 `argAndMin` 会同时返回参数和取最小值的数值。
:::

**另请参阅**

* [argMin](/sql-reference/aggregate-functions/reference/argMin.md)
* [Tuple](/sql-reference/data-types/tuple.md)

**语法**

```sql
argAndMin(arg, val)
```

**参数**

* `arg` — 用于查找最小 `val` 值时返回的参数。[`const String`](/sql-reference/data-types/string)
* `val` — 最小值。[`(U)Int8/16/32/64`](/sql-reference/data-types/int-uint) 或 [`Float*`](/sql-reference/data-types/float) 或 [`Date`](/sql-reference/data-types/date) 或 [`DateTime`](/sql-reference/data-types/datetime) 或 [`Tuple`](/sql-reference/data-types/tuple)

**返回值**

返回一个 tuple，其中包含与最小 `val` 值对应的 `arg` 值，以及该最小的 `val` 值。[`Tuple`](/sql-reference/data-types/tuple)

**示例**

**基本用法**

```sql title=Query
SELECT argAndMin(user, salary) FROM salary;
```

```response title=Response
┌─argAndMin(user, salary)─┐
│ ('worker',1000)         │
└─────────────────────────┘
```

**包含 NULL 处理的扩展示例**

```sql title=Query
CREATE TABLE test
(
    a Nullable(String),
    b Nullable(Int64)
)
ENGINE = Memory AS
SELECT *
FROM VALUES((NULL, 0), ('a', 1), ('b', 2), ('c', 2), (NULL, NULL), ('d', NULL));

SELECT argMin(a,b), argAndMin(a, b), min(b) FROM test;
```

```response title=Response
┌─argMin(a, b)─┬─argAndMin(a, b)─┬─min(b)─┐
│ a            │ ('a',1)         │      0 │
└──────────────┴─────────────────┴────────┘
```

**在参数中使用元组（Tuple）**

```sql title=Query
SELECT argAndMin(a, (b, a)), min(tuple(b, a)) FROM test;
```

```response title=Response
┌─argAndMin(a, (b, a))─┬─min((b, a))─┐
│ ('a',(1,'a'))        │ (0,NULL)    │
└──────────────────────┴─────────────┘
```

{/*AUTOGENERATED_END*/ }

**另请参阅**

* [argMin](/sql-reference/aggregate-functions/reference/argMin.md)
* [Tuple](/sql-reference/data-types/tuple.md)
