---
description: '执行简单（一维）线性回归分析。'
slug: /sql-reference/aggregate-functions/reference/simplelinearregression
title: 'simpleLinearRegression'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## simpleLinearRegression \{#simpleLinearRegression\}

首次引入于：v20.1

进行简单（一维）线性回归。

**语法**

```sql
simpleLinearRegression(x, y)
```

**参数**

* `x` — 包含自变量值的列。[`Float64`](/sql-reference/data-types/float)
* `y` — 包含因变量值的列。[`Float64`](/sql-reference/data-types/float)

**返回值**

返回结果直线 `y = k*x + b` 的常数 `(k, b)`。[`Tuple(Float64, Float64)`](/sql-reference/data-types/tuple)

**示例**

**完美线性拟合**

```sql title=Query
SELECT arrayReduce('simpleLinearRegression', [0, 1, 2, 3], [0, 1, 2, 3]);
```

```response title=Response
┌─arrayReduce('simpleLinearRegression', [0, 1, 2, 3], [0, 1, 2, 3])─┐
│ (1,0)                                                             │
└───────────────────────────────────────────────────────────────────┘
```

**带截距的线性拟合**

```sql title=Query
SELECT arrayReduce('simpleLinearRegression', [0, 1, 2, 3], [3, 4, 5, 6]);
```

```response title=Response
┌─arrayReduce('simpleLinearRegression', [0, 1, 2, 3], [3, 4, 5, 6])─┐
│ (1,3)                                                             │
└───────────────────────────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/ }
