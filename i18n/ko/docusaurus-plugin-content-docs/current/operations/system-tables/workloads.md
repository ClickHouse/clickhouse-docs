---
description: '로컬 서버에 상주하는 워크로드에 대한 정보를 포함하는 system 테이블입니다.'
keywords: ['system 테이블', '워크로드']
slug: /operations/system-tables/workloads
title: 'system.workloads'
doc_type: 'reference'
---

## 설명 \{#description\}

로컬 서버에 존재하는 [워크로드](/operations/workload-scheduling.md#workload_entity_storage)에 대한 정보를 포함합니다. 이 테이블에는 각 워크로드에 대해 하나의 행이 존재합니다.

## 컬럼 \{#columns\}

{/*AUTOGENERATED_START*/ }

* `name` ([String](../../sql-reference/data-types/)) — 워크로드의 이름입니다.
* `parent` ([String](../../sql-reference/data-types/)) — 상위 워크로드의 이름입니다.
* `create_query` ([String](../../sql-reference/data-types/)) — 워크로드에 대한 CREATE 쿼리입니다.

{/*AUTOGENERATED_END*/ }

## 예시 \{#example\}

```sql
SELECT *
FROM system.workloads
FORMAT Vertical
```

```text
Row 1:
──────
name:         production
parent:       all
create_query: CREATE WORKLOAD production IN `all` SETTINGS weight = 9

Row 2:
──────
name:         development
parent:       all
create_query: CREATE WORKLOAD development IN `all`

Row 3:
──────
name:         all
parent:
create_query: CREATE WORKLOAD `all`
```