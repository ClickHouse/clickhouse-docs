---
description: '모든 테이블의 기존 제약 조건에 대한 정보를 담고 있는 시스템 테이블.'
keywords: ['시스템 테이블', '제약 조건']
slug: /operations/system-tables/constraints
sidebar_label: 'system.constraints'
sidebar_position: 20
title: 'system.constraints'
doc_type: 'reference'
---

## 설명 \{#description\}

모든 테이블에 있는 제약 조건 정보를 포함합니다.

[임시 테이블](../../sql-reference/statements/create/table.md#temporary-tables)에 정의된 제약 조건은 생성된 세션에서만 `system.constraints`에 표시됩니다. 이 항목들은 `database` 필드가 비어 있는 상태로 표시됩니다.

## 컬럼 \{#columns\}

{/*AUTOGENERATED_START*/ }

* `database` ([String](../../sql-reference/data-types/)) — 데이터베이스 이름.
* `table` ([String](../../sql-reference/data-types/)) — 테이블 이름.
* `name` ([String](../../sql-reference/data-types/)) — 제약 조건의 이름.
* `type` ([Enum8(&#39;CHECK&#39; = 0, &#39;ASSUME&#39; = 1)](../../sql-reference/data-types/)) — 제약 조건의 유형.
* `expression` ([String](../../sql-reference/data-types/)) — 제약 조건의 표현식.

{/*AUTOGENERATED_END*/ }

## 예시 \{#example\}

```sql
SELECT * FROM system.constraints LIMIT 2 FORMAT Vertical;
```

```text
Row 1:
──────
database:    default
table:       hits
name:        check_hits
type:        CHECK
expression:  CounterID > 0

Row 2:
──────
database:    default
table:       hits
name:        assume_positive
type:        ASSUME
expression:  WatchID > 0
```