---
'description': '뷰를 구현하는 데 사용됩니다 (자세한 정보는 `CREATE VIEW 쿼리`를 참조하세요). 데이터를 저장하지 않고, 지정된
  `SELECT` 쿼리만 저장합니다. 테이블을 읽을 때 이 쿼리를 실행하며 (쿼리에서 모든 불필요한 컬럼을 삭제합니다).'
'sidebar_label': '뷰'
'sidebar_position': 90
'slug': '/engines/table-engines/special/view'
'title': '뷰 테이블 엔진'
'doc_type': 'reference'
---


# 뷰 테이블 엔진

뷰를 구현하는 데 사용됩니다 (자세한 정보는 `CREATE VIEW 쿼리`를 참조하십시오). 데이터는 저장하지 않고, 지정된 `SELECT` 쿼리만 저장합니다. 테이블에서 읽을 때 이 쿼리를 실행하고 (쿼리에서 불필요한 컬럼을 모두 삭제합니다).
