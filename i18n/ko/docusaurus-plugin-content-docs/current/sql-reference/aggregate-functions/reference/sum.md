---
description: '합계를 계산합니다. 숫자에만 사용할 수 있습니다.'
slug: /sql-reference/aggregate-functions/reference/sum
title: 'sum'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## sum \{#sum\}

도입: v1.1

숫자 값의 합을 계산합니다.

**구문**

```sql
sum(num)
```

**인수**

* `num` — 숫자 값을 담은 컬럼. [`(U)Int*`](/sql-reference/data-types/int-uint) 또는 [`Float*`](/sql-reference/data-types/float) 또는 [`Decimal*`](/sql-reference/data-types/decimal)

**반환 값**

값들의 합을 반환합니다. [`(U)Int*`](/sql-reference/data-types/int-uint) 또는 [`Float*`](/sql-reference/data-types/float) 또는 [`Decimal*`](/sql-reference/data-types/decimal)

**예시**

**직원 급여 합계 계산**

```sql title=Query
CREATE TABLE employees
(
    id UInt32,
    name String,
    salary UInt32
)
ENGINE = Memory;

INSERT INTO employees VALUES
    (87432, 'John Smith', 45680),
    (59018, 'Jane Smith', 72350),
    (20376, 'Ivan Ivanovich', 58900),
    (71245, 'Anastasia Ivanovna', 89210);

SELECT sum(salary) FROM employees;
```

```response title=Response
┌─sum(salary)─┐
│      266140 │
└─────────────┘
```

{/*자동 생성 끝*/ }
