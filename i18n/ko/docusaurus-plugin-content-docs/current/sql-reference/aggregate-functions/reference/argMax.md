---
description: '`val` 값이 최대일 때의 `arg` 값을 계산합니다.'
slug: /sql-reference/aggregate-functions/reference/argmax
title: 'argMax'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## argMax \{#argMax\}

도입 버전: v1.1

최대 `val` 값에 해당하는 `arg` 값을 계산합니다. 최대값인 `val`이 동일한 여러 행이 있는 경우, 연관된 `arg` 중 어떤 값이 반환될지는 결정적이지 않습니다.
`arg`와 `max` 두 부분 모두 [집계 함수](/sql-reference/aggregate-functions/index.md)처럼 동작하며, 처리 과정에서 둘 다 [`Null`을 건너뛰고](/sql-reference/aggregate-functions/index.md#null-processing), `Null`이 아닌 값이 하나라도 있으면 `Null`이 아닌 값을 반환합니다.

**관련 항목**

* [Tuple](/sql-reference/data-types/tuple.md)

**구문**

```sql
argMax(arg, val)
```

**인수**

* `arg` — 최댓값을 찾을 인수입니다. [`const String`](/sql-reference/data-types/string)
* `val` — 최댓값입니다. [`(U)Int8/16/32/64`](/sql-reference/data-types/int-uint) 또는 [`Float*`](/sql-reference/data-types/float) 또는 [`Date`](/sql-reference/data-types/date) 또는 [`DateTime`](/sql-reference/data-types/datetime) 또는 [`Tuple`](/sql-reference/data-types/tuple)

**반환 값**

최대 `val` 값에 해당하는 `arg` 값을 반환합니다. 반환 타입은 `arg` 타입과 동일합니다.

**예시**

**기본 사용법**

```sql title=Query
SELECT argMax(user, salary) FROM salary;
```

```response title=Response
┌─argMax(user, salary)─┐
│ director             │
└──────────────────────┘
```

**NULL 처리를 포함한 확장 예제**

```sql title=Query
CREATE TABLE test
(
    a Nullable(String),
    b Nullable(Int64)
)
ENGINE = Memory AS
SELECT *
FROM VALUES(('a', 1), ('b', 2), ('c', 2), (NULL, 3), (NULL, NULL), ('d', NULL));

SELECT argMax(a, b), max(b) FROM test;
```

```response title=Response
┌─argMax(a, b)─┬─max(b)─┐
│ b            │      3 │
└──────────────┴────────┘
```

**인수로 Tuple 사용하기**

```sql title=Query
SELECT argMax(a, (b,a)) FROM test;
```

```response title=Response
┌─argMax(a, tuple(b, a))─┐
│ c                      │
└────────────────────────┘
```

{/*AUTOGENERATED_END*/ }
