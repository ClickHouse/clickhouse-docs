---
description: '모든 구간(수직선 상의 선분) 합집합의 전체 길이를 계산합니다.'
sidebar_label: 'intervalLengthSum'
slug: /sql-reference/aggregate-functions/reference/intervalLengthSum
title: 'intervalLengthSum'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## intervalLengthSum \{#intervalLengthSum\}

도입된 버전: v21.7

여러 개의 숫자 범위를 입력받아, 서로 겹치는 구간을 하나의 통합 범위로 합쳤을 때의 전체 길이를 계산합니다.

:::note
인수는 모두 동일한 데이터 타입이어야 합니다.
그렇지 않으면 예외가 발생합니다.
:::

**구문**

```sql
intervalLengthSum(start, end)
```

**인자**

* `start` — 구간의 시작값입니다. [`(U)Int32/64`](/sql-reference/data-types/int-uint) 또는 [`Float*`](/sql-reference/data-types/float) 또는 [`DateTime`](/sql-reference/data-types/datetime) 또는 [`Date`](/sql-reference/data-types/date)
* `end` — 구간의 끝값입니다. [`(U)Int32/64`](/sql-reference/data-types/int-uint) 또는 [`Float*`](/sql-reference/data-types/float) 또는 [`DateTime`](/sql-reference/data-types/datetime) 또는 [`Date`](/sql-reference/data-types/date)

**반환 값**

모든 범위(숫자 축의 구간) 합집합의 전체 길이를 반환합니다. 인자의 타입에 따라 반환값은 UInt64 타입 또는 Float64 타입이 될 수 있습니다. [`UInt64`](/sql-reference/data-types/int-uint) 또는 [`Float64`](/sql-reference/data-types/float)

**예시**

**Float32 예시**

```sql title=Query
CREATE TABLE fl_interval (id String, start Float32, end Float32) ENGINE = Memory;
INSERT INTO fl_interval VALUES ('a', 1.1, 2.9), ('a', 2.5, 3.2), ('a', 4, 5);

SELECT id, intervalLengthSum(start, end), toTypeName(intervalLengthSum(start, end)) FROM fl_interval GROUP BY id ORDER BY id;
```

```response title=Response
┌─id─┬─intervalLengthSum(start, end)─┬─toTypeName(intervalLengthSum(start, end))─┐
│ a  │                           3.1 │ Float64                                   │
└────┴───────────────────────────────┴───────────────────────────────────────────┘
```

**DateTime 예제**

```sql title=Query
CREATE TABLE dt_interval (id String, start DateTime, end DateTime) ENGINE = Memory;
INSERT INTO dt_interval VALUES ('a', '2020-01-01 01:12:30', '2020-01-01 02:10:10'), ('a', '2020-01-01 02:05:30', '2020-01-01 02:50:31'), ('a', '2020-01-01 03:11:22', '2020-01-01 03:23:31');

SELECT id, intervalLengthSum(start, end), toTypeName(intervalLengthSum(start, end)) FROM dt_interval GROUP BY id ORDER BY id;
```

```response title=Response
┌─id─┬─intervalLengthSum(start, end)─┬─toTypeName(intervalLengthSum(start, end))─┐
│ a  │                          6610 │ UInt64                                    │
└────┴───────────────────────────────┴───────────────────────────────────────────┘
```

**날짜 예제**

```sql title=Query
CREATE TABLE date_interval (id String, start Date, end Date) ENGINE = Memory;
INSERT INTO date_interval VALUES ('a', '2020-01-01', '2020-01-04'), ('a', '2020-01-12', '2020-01-18');

SELECT id, intervalLengthSum(start, end), toTypeName(intervalLengthSum(start, end)) FROM date_interval GROUP BY id ORDER BY id;
```

```response title=Response
┌─id─┬─intervalLengthSum(start, end)─┬─toTypeName(intervalLengthSum(start, end))─┐
│ a  │                             9 │ UInt64                                    │
└────┴───────────────────────────────┴───────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/ }
