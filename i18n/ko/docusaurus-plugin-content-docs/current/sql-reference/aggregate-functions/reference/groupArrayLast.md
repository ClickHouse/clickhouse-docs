---
description: '마지막 인수의 값들로 배열을 생성합니다.'
slug: /sql-reference/aggregate-functions/reference/grouparraylast
title: 'groupArrayLast'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## groupArrayLast \{#groupArrayLast\}

도입 버전: v23.1

마지막 인자 값들로 구성된 배열을 생성합니다.
예를 들어, `groupArrayLast(1)(x)`는 `[anyLast(x)]`와 동일합니다.
일부 상황에서는 실행 순서에 여전히 의존할 수 있습니다.
이는 SELECT가 `ORDER BY`를 사용하는 서브쿼리에서 나오고, 서브쿼리 결과가 충분히 작은 경우에 적용됩니다.

**문법**

```sql
groupArrayLast(max_size)(x)
```

**매개변수**

* `max_size` — 결과 배열의 최대 크기. [`UInt64`](/sql-reference/data-types/int-uint)

**인수**

* `max_size` — 결과 배열의 최대 크기. [`UInt64`](/sql-reference/data-types/int-uint)
* `x` — 인수(컬럼 이름 또는 표현식). [`Any`](/sql-reference/data-types)

**반환 값**

마지막 인수 값으로 구성된 배열을 반환합니다. [`Array(T)`](/sql-reference/data-types/array)

**예시**

**사용 예시**

```sql title=Query
SELECT groupArrayLast(2)(number+1) numbers FROM numbers(10);
```

```response title=Response
┌─numbers─┐
│ [9,10]  │
└─────────┘
```

**groupArray와 비교**

```sql title=Query
-- Compare with groupArray (first values)
SELECT groupArray(2)(number+1) numbers FROM numbers(10);
```

```response title=Response
┌─numbers─┐
│ [1,2]   │
└─────────┘
```

{/*AUTOGENERATED_END*/ }
