:::important best practices
ClickHouse 서버를 구성할 때 구성 파일을 추가하거나 편집할 때는 다음을 수행해야 합니다:
- `/etc/clickhouse-server/config.d/` 디렉토리에 파일 추가
- `/etc/clickhouse-server/users.d/` 디렉토리에 파일 추가
- `/etc/clickhouse-server/config.xml` 파일은 그대로 유지
- `/etc/clickhouse-server/users.xml` 파일은 그대로 유지
:::
