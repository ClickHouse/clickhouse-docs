---
description: 'Содержит хранящееся в памяти состояние метаданных S3Queue и строки, которые в данный момент обрабатываются в каждом файле.'
keywords: ['системная таблица', 's3queue_metadata_cache']
slug: /operations/system-tables/s3queue_metadata_cache
title: 'system.s3queue_metadata_cache'
doc_type: 'reference'
---

## Описание \{#description\}

Содержит хранящееся в памяти состояние метаданных S3Queue и строки, обрабатываемые в данный момент в каждом файле.

## Столбцы \{#columns\}

{/*AUTOGENERATED_START*/ }

* `zookeeper_path` ([String](../../sql-reference/data-types/)) — Путь к метаданным в ZooKeeper
* `file_path` ([String](../../sql-reference/data-types/)) — Путь к обрабатываемому файлу
* `file_name` ([String](../../sql-reference/data-types/)) — Имя обрабатываемого файла
* `rows_processed` ([UInt64](../../sql-reference/data-types/)) — Текущее количество обработанных строк
* `status` ([String](../../sql-reference/data-types/)) — Статус обработки: Processed, Processing, Failed
* `processing_start_time` ([Nullable(DateTime)](../../sql-reference/data-types/)) — Время начала обработки файла
* `processing_end_time` ([Nullable(DateTime)](../../sql-reference/data-types/)) — Время окончания обработки файла
* `exception` ([String](../../sql-reference/data-types/)) — Исключение, возникшее во время обработки

{/*AUTOGENERATED_END*/ }