---
description: 'Системная таблица, содержащая метрики, которые можно вычислить мгновенно
  или которые имеют текущее значение.'
keywords: ['системная таблица', 'метрики']
slug: /operations/system-tables/metrics
title: 'system.metrics'
doc_type: 'reference'
---

import SystemTableCloud from '@site/i18n/ru/docusaurus-plugin-content-docs/current/_snippets/_system_table_cloud.md';


# system.metrics \{#systemmetrics\}

<SystemTableCloud />

Содержит метрики, которые можно вычислить мгновенно или которые имеют текущее значение. Например, количество одновременно обрабатываемых запросов или текущую задержку реплики. Эта таблица всегда актуальна.

Столбцы:

{/*AUTOGENERATED_START*/ }

* `metric` ([String](../../sql-reference/data-types/)) — Имя метрики.
* `value` ([Int64](../../sql-reference/data-types/)) — Значение метрики.
* `description` ([String](../../sql-reference/data-types/)) — Описание метрики.

{/*AUTOGENERATED_END*/ }

Все поддерживаемые метрики можно найти в исходном файле [src/Common/CurrentMetrics.cpp](https://github.com/ClickHouse/ClickHouse/blob/master/src/Common/CurrentMetrics.cpp).

**Пример**

```sql
SELECT * FROM system.metrics LIMIT 10
```

```text
┌─metric───────────────────────────────┬─value─┬─description────────────────────────────────────────────────────────────┐
│ Query                                │     1 │ Number of executing queries                                            │
│ Merge                                │     0 │ Number of executing background merges                                  │
│ PartMutation                         │     0 │ Number of mutations (ALTER DELETE/UPDATE)                              │
│ ReplicatedFetch                      │     0 │ Number of data parts being fetched from replicas                       │
│ ReplicatedSend                       │     0 │ Number of data parts being sent to replicas                            │
│ ReplicatedChecks                     │     0 │ Number of data parts checking for consistency                          │
│ BackgroundMergesAndMutationsPoolTask │     0 │ Number of active merges and mutations in an associated background pool │
│ BackgroundFetchesPoolTask            │     0 │ Number of active fetches in an associated background pool              │
│ BackgroundCommonPoolTask             │     0 │ Number of active tasks in an associated background pool                │
│ BackgroundMovePoolTask               │     0 │ Number of active tasks in BackgroundProcessingPool for moves           │
└──────────────────────────────────────┴───────┴────────────────────────────────────────────────────────────────────────┘
```


## Описание метрик \{#metric-descriptions\}

{/*AUTOGENERATED_DESCRIPTIONS_START*/ }


### ASTFuzzerAccumulatedFragments \{#astfuzzeraccumulatedfragments\}

Количество фрагментов AST, накопленных AST fuzzer на стороне сервера для использования в будущих мутациях.

### ActiveTimersInQueryProfiler \{#activetimersinqueryprofiler\}

Количество активных локальных для потока таймеров в QueryProfiler

### AddressesActive \{#addressesactive\}

Общее количество адресов, используемых для создания подключений в пулах подключений

### AddressesBanned \{#addressesbanned\}

Общее количество адресов, заблокированных как неисправные при создании подключений через пулы подключений

### AggregatorThreads \{#aggregatorthreads\}

Количество потоков в пуле потоков Aggregator.

### AggregatorThreadsActive \{#aggregatorthreadsactive\}

Количество потоков в пуле потоков Aggregator, выполняющих задачу.

### AggregatorThreadsScheduled \{#aggregatorthreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков Aggregator.

### AsyncInsertCacheSize \{#asyncinsertcachesize\}

Количество hash id для асинхронных insert в кэше

### AsynchronousInsertQueueBytes \{#asynchronousinsertqueuebytes\}

Количество байтов в очереди AsynchronousInsert, ожидающих обработки.

### AsynchronousInsertQueueSize \{#asynchronousinsertqueuesize\}

Количество ожидающих задач в очереди AsynchronousInsert.

### AsynchronousInsertThreads \{#asynchronousinsertthreads\}

Количество потоков в пуле потоков AsynchronousInsert.

### AsynchronousInsertThreadsActive \{#asynchronousinsertthreadsactive\}

Количество потоков в пуле потоков AsynchronousInsert, которые выполняют задачу.

### AsynchronousInsertThreadsScheduled \{#asynchronousinsertthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков AsynchronousInsert.

### AsynchronousReadWait \{#asynchronousreadwait\}

Количество потоков, ожидающих асинхронного чтения.

### AttachedDatabase \{#attacheddatabase\}

Активные базы данных.

### AttachedDictionary \{#attacheddictionary\}

Активные словари.

### AttachedReplicatedTable \{#attachedreplicatedtable\}

Активные реплицированные таблицы.

### AttachedTable \{#attachedtable\}

Активные таблицы.

### AttachedView \{#attachedview\}

Активные представления.

### AvroSchemaCacheBytes \{#avroschemacachebytes\}

Размер кэша схем Avro в байтах

### AvroSchemaCacheCells \{#avroschemacachecells\}

Количество схем Avro в кэше

### AvroSchemaRegistryCacheBytes \{#avroschemaregistrycachebytes\}

Размер кэша реестра схем Avro в байтах

### AvroSchemaRegistryCacheCells \{#avroschemaregistrycachecells\}

Количество записей в кэше реестра схем Avro

### AzureRequests \{#azurerequests\}

Количество запросов Azure, выполняемых в данный момент

### BackgroundBufferFlushSchedulePoolSize \{#backgroundbufferflushschedulepoolsize\}

Лимит на количество задач в BackgroundBufferFlushSchedulePool

### BackgroundBufferFlushSchedulePoolTask \{#backgroundbufferflushschedulepooltask\}

Количество активных задач в BackgroundBufferFlushSchedulePool. Этот пул используется для периодического сброса Buffer

### BackgroundCommonPoolSize \{#backgroundcommonpoolsize\}

Лимит на количество задач в соответствующем фоновом пуле

### BackgroundCommonPoolTask \{#backgroundcommonpooltask\}

Количество активных задач в соответствующем фоновом пуле

### BackgroundDistributedSchedulePoolSize \{#backgrounddistributedschedulepoolsize\}

Лимит на количество задач в BackgroundDistributedSchedulePool

### BackgroundDistributedSchedulePoolTask \{#backgrounddistributedschedulepooltask\}

Количество активных задач в BackgroundDistributedSchedulePool. Этот пул используется для распределённой отправки данных, выполняемой в фоновом режиме.

### BackgroundFetchesPoolSize \{#backgroundfetchespoolsize\}

Лимит на количество одновременных операций получения данных в соответствующем фоновом пуле

### BackgroundFetchesPoolTask \{#backgroundfetchespooltask\}

Количество активных операций получения данных в соответствующем фоновом пуле

### BackgroundMergesAndMutationsPoolSize \{#backgroundmergesandmutationspoolsize\}

Лимит на количество активных слияний и мутаций в соответствующем фоновом пуле

### BackgroundMergesAndMutationsPoolTask \{#backgroundmergesandmutationspooltask\}

Количество активных слияний и мутаций в соответствующем фоновом пуле

### BackgroundMessageBrokerSchedulePoolSize \{#backgroundmessagebrokerschedulepoolsize\}

Лимит на количество задач в BackgroundMessageBrokerSchedulePool для потоковой передачи сообщений

### BackgroundMessageBrokerSchedulePoolTask \{#backgroundmessagebrokerschedulepooltask\}

Количество активных задач в BackgroundMessageBrokerSchedulePool для потоковой обработки сообщений

### BackgroundMovePoolSize \{#backgroundmovepoolsize\}

Лимит на количество задач для перемещений в BackgroundProcessingPool

### BackgroundMovePoolTask \{#backgroundmovepooltask\}

Количество активных задач перемещения в BackgroundProcessingPool

### BackgroundSchedulePoolSize \{#backgroundschedulepoolsize\}

Лимит на количество задач в BackgroundSchedulePool. Этот пул используется для периодических задач ReplicatedMergeTree, таких как очистка старых частей данных, изменение частей данных, переинициализация реплики и т. д.

### BackgroundSchedulePoolTask \{#backgroundschedulepooltask\}

Количество активных задач в BackgroundSchedulePool. Этот пул используется для периодических задач ReplicatedMergeTree, таких как очистка старых частей данных, изменение частей данных, переинициализация реплики и т. д.

### BackupsIOThreads \{#backupsiothreads\}

Количество потоков в пуле потоков BackupsIO.

### BackupsIOThreadsActive \{#backupsiothreadsactive\}

Количество потоков в пуле потоков BackupsIO, выполняющих задачу.

### BackupsIOThreadsScheduled \{#backupsiothreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков BackupsIO.

### BackupsThreads \{#backupsthreads\}

Количество потоков в пуле потоков, используемом для BACKUP.

### BackupsThreadsActive \{#backupsthreadsactive\}

Количество потоков в пуле потоков BACKUP, выполняющих задачу.

### BackupsThreadsScheduled \{#backupsthreadsscheduled\}

Количество задач BACKUP в очереди или выполняющихся.

### BcryptCacheBytes \{#bcryptcachebytes\}

Общий размер кэша аутентификации bcrypt в байтах

### Размер кэша bcrypt \{#bcryptcachesize\}

Общее число записей в кэше аутентификации bcrypt

### BlobCopierThreads \{#blobcopierthreads\}

Количество потоков в пуле потоков фонового процесса репликации диска объектного хранилища

### BlobCopierThreadsActive \{#blobcopierthreadsactive\}

Количество потоков в пуле потоков фонового процесса репликации диска объектного хранилища, выполняющих задачу

### BlobCopierThreadsScheduled \{#blobcopierthreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков процесса фоновой репликации диска Объектного хранилища

### BlobKillerThreads \{#blobkillerthreads\}

Количество потоков в пуле потоков процесса фонового удаления для диска объектного хранилища

### BlobKillerThreadsActive \{#blobkillerthreadsactive\}

Количество потоков в пуле потоков процесса фонового удаления на диске Объектного хранилища, выполняющих задачу

### BlobKillerThreadsScheduled \{#blobkillerthreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков фонового процесса удаления для диска объектного хранилища

### BrokenDisks \{#brokendisks\}

Количество дисков, помеченных как неисправные при проверке дисков.

### BrokenDistributedBytesToInsert \{#brokendistributedbytestoinsert\}

Количество байтов для асинхронной вставки в distributed таблицы, помеченных как повреждённые. Количество байтов по каждому сегменту суммируется.

### BrokenDistributedFilesToInsert \{#brokendistributedfilestoinsert\}

Количество файлов, помеченных как повреждённые, для асинхронной вставки в distributed таблицы. Количество файлов по каждому сегменту суммируется.

### BuildVectorSimilarityIndexThreads \{#buildvectorsimilarityindexthreads\}

Количество потоков в пуле потоков построения индекса векторного сходства.

### BuildVectorSimilarityIndexThreadsActive \{#buildvectorsimilarityindexthreadsactive\}

Количество потоков в пуле потоков построения индекса векторного сходства, которые выполняют задачу.

### BuildVectorSimilarityIndexThreadsScheduled \{#buildvectorsimilarityindexthreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков построения индекса векторного сходства.

### CacheDetachedFileSegments \{#cachedetachedfilesegments\}

Количество существующих отсоединённых сегментов файлов кэша

### CacheDictionaryThreads \{#cachedictionarythreads\}

Количество потоков в пуле потоков CacheDictionary.

### CacheDictionaryThreadsActive \{#cachedictionarythreadsactive\}

Количество потоков в пуле потоков CacheDictionary, которые выполняют задачу.

### CacheDictionaryThreadsScheduled \{#cachedictionarythreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков CacheDictionary.

### CacheDictionaryUpdateQueueBatches \{#cachedictionaryupdatequeuebatches\}

Количество «пакетов» (наборов ключей) в очереди обновления CacheDictionaries.

### CacheDictionaryUpdateQueueKeys \{#cachedictionaryupdatequeuekeys\}

Точное количество ключей в очереди обновления кэширующих словарей.

### CacheFileSegments \{#cachefilesegments\}

Количество существующих сегментов кэш-файлов

### CacheWarmerBytesInProgress \{#cachewarmerbytesinprogress\}

Общий размер сегментов удалённых файлов, ожидающих асинхронной загрузки в файловый кэш.

### ColumnsDescriptionsCacheSize \{#columnsdescriptionscachesize\}

Размер кэша ColumnsDescriptions (отдельный кэш для каждой таблицы)

### CompiledExpressionCacheBytes \{#compiledexpressioncachebytes\}

Общее количество байт, используемых для кэша JIT-скомпилированного кода

### CompiledExpressionCacheCount \{#compiledexpressioncachecount\}

Общее количество записей в кэше JIT-скомпилированного кода

### Сжатие \{#compressing\}

Количество операций сжатия с использованием внутренних кодеков

### CompressionThread \{#compressionthread\}

Количество потоков в пулах потоков, отвечающих за сжатие.

### CompressionThreadActive \{#compressionthreadactive\}

Количество потоков в пулах потоков сжатия, которые выполняют задачу.

### CompressionThreadScheduled \{#compressionthreadscheduled\}

Количество задач в очереди или выполняющихся в пулах потоков сжатия.

### ConcurrencyControlAcquired \{#concurrencycontrolacquired\}

Общее количество выделенных CPU-слотов

### ConcurrencyControlAcquiredNonCompeting \{#concurrencycontrolacquirednoncompeting\}

Общее число полученных CPU-слотов, которые не считаются конкурирующими (первый поток при использовании планировщика fair&#95;round&#95;robin)

### ConcurrencyControlPreempted \{#concurrencycontrolpreempted\}

Общее количество вытесненных потоков, ожидающих CPU-слот

### ConcurrencyControlScheduled \{#concurrencycontrolscheduled\}

Общее количество запросов на CPU-слоты, которые в данный момент находятся в планировании

### ConcurrencyControlSoftLimit \{#concurrencycontrolsoftlimit\}

Значение мягкого лимита на количество CPU-слотов

### ConcurrentHashJoinPoolThreads \{#concurrenthashjoinpoolthreads\}

Количество потоков в пуле потоков для concurrent hash join.

### ConcurrentHashJoinPoolThreadsActive \{#concurrenthashjoinpoolthreadsactive\}

Количество потоков в пуле потоков concurrent hash join, выполняющих задачу.

### ConcurrentHashJoinPoolThreadsScheduled \{#concurrenthashjoinpoolthreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков для параллельного hash join.

### ConcurrentQueryAcquired \{#concurrentqueryacquired\}

Общее количество выделенных слотов для запросов

### ConcurrentQueryScheduled \{#concurrentqueryscheduled\}

Общее количество запросов на выделение слотов для запросов, которые в данный момент планируются

### ContextLockWait \{#contextlockwait\}

Количество потоков, ожидающих освобождения блокировки в Context. Это глобальная блокировка.

### CoordinatedMergesCoordinatorAssignedMerges \{#coordinatedmergescoordinatorassignedmerges\}

Количество назначенных слияний в состоянии «координатор слияний».

### CoordinatedMergesCoordinatorRunningMerges \{#coordinatedmergescoordinatorrunningmerges\}

Количество выполняющихся слияний, видимых координатору слияний.

### CoordinatedMergesWorkerAssignedMerges \{#coordinatedmergesworkerassignedmerges\}

Количество назначенных слияний в состоянии merge worker.

### CreatedTimersInQueryProfiler \{#createdtimersinqueryprofiler\}

Количество таймеров, локальных для потока, созданных в QueryProfiler

### DDLWorkerThreads \{#ddlworkerthreads\}

Количество потоков в пуле потоков DDLWorker для запросов ON CLUSTER.

### DDLWorkerThreadsActive \{#ddlworkerthreadsactive\}

Количество потоков в пуле потоков DDLWORKER, выполняющих задачу для запросов ON CLUSTER.

### DDLWorkerThreadsScheduled \{#ddlworkerthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков DDLWORKER для запросов ON CLUSTER.

### DNSAddressesCacheBytes \{#dnsaddressescachebytes\}

Размер кэша DNS-адресов в байтах

### DNSAddressesCacheSize \{#dnsaddressescachesize\}

Количество кэшированных DNS-адресов

### DNSHostsCacheBytes \{#dnshostscachebytes\}

Размер кэша DNS-хостов в байтах

### DNSHostsCacheSize \{#dnshostscachesize\}

Количество DNS-хостов, хранящихся в кэше

### DatabaseBackupThreads \{#databasebackupthreads\}

Количество потоков в пуле потоков DatabaseBackup.

### DatabaseBackupThreadsActive \{#databasebackupthreadsactive\}

Количество потоков в пуле потоков DatabaseBackup, которые выполняют задачу.

### DatabaseBackupThreadsScheduled \{#databasebackupthreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков DatabaseBackup.

### DatabaseCatalogThreads \{#databasecatalogthreads\}

Количество потоков в пуле потоков DatabaseCatalog.

### DatabaseCatalogThreadsActive \{#databasecatalogthreadsactive\}

Количество потоков в пуле потоков DatabaseCatalog, выполняющих задачу.

### DatabaseCatalogThreadsScheduled \{#databasecatalogthreadsscheduled\}

Количество задач в пуле потоков DatabaseCatalog, находящихся в очереди или выполняющихся.

### DatabaseOnDiskThreads \{#databaseondiskthreads\}

Количество потоков в пуле потоков DatabaseOnDisk.

### DatabaseOnDiskThreadsActive \{#databaseondiskthreadsactive\}

Количество потоков в пуле потоков DatabaseOnDisk, выполняющих задачу.

### DatabaseOnDiskThreadsScheduled \{#databaseondiskthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков DatabaseOnDisk.

### DatabaseReplicatedCreateTablesThreads \{#databasereplicatedcreatetablesthreads\}

Количество потоков в пуле потоков для создания таблиц в DatabaseReplicated.

### DatabaseReplicatedCreateTablesThreadsActive \{#databasereplicatedcreatetablesthreadsactive\}

Количество активных потоков в пуле потоков, используемом для создания таблиц в DatabaseReplicated.

### DatabaseReplicatedCreateTablesThreadsScheduled \{#databasereplicatedcreatetablesthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков для создания таблиц в DatabaseReplicated.

### Декомпрессия \{#decompressing\}

Число операций декомпрессии с использованием внутренних кодеков сжатия

### DelayedInserts \{#delayedinserts\}

Количество запросов INSERT, выполнение которых замедляется из-за большого числа активных частей данных в партиции таблицы MergeTree.

### DeltaLakeSnapshotCacheSizeElements \{#deltalakesnapshotcachesizeelements\}

Размер кэша снимков DeltaLake в элементах.

### DestroyAggregatesThreads \{#destroyaggregatesthreads\}

Количество потоков в пуле потоков для уничтожения состояний агрегатных функций.

### DestroyAggregatesThreadsActive \{#destroyaggregatesthreadsactive\}

Количество потоков в пуле потоков уничтожения состояний агрегатных функций, которые выполняют задачу.

### DestroyAggregatesThreadsScheduled \{#destroyaggregatesthreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков для уничтожения состояний агрегатов.

### DictCacheRequests \{#dictcacherequests\}

Количество выполняющихся запросов к источникам данных словарей типа кэш.

### DiskConnectionsStored \{#diskconnectionsstored\}

Общее количество сессий, хранящихся в пуле сессий дисков

### DiskConnectionsTotal \{#diskconnectionstotal\}

Общее количество всех сессий для дисков: хранящихся в пуле и активно используемых в данный момент

### DiskObjectStorageAsyncThreads \{#diskobjectstorageasyncthreads\}

Устаревшая метрика; не показывает никаких данных.

### DiskObjectStorageAsyncThreadsActive \{#diskobjectstorageasyncthreadsactive\}

Устаревшая метрика; не показывает никаких данных.

### DiskPlainRewritableAzureDirectoryMapSize \{#diskplainrewritableazuredirectorymapsize\}

Количество записей соответствий локальных и удалённых путей в хранимом в памяти map `plain_rewritable` для AzureObjectStorage.

### DiskPlainRewritableAzureFileCount \{#diskplainrewritableazurefilecount\}

Количество файловых записей в хранимом в памяти map `plain_rewritable` для AzureObjectStorage.

### DiskPlainRewritableLocalDirectoryMapSize \{#diskplainrewritablelocaldirectorymapsize\}

Количество записей соответствий локальных и удалённых путей в хранимой в памяти map `plain_rewritable` для LocalObjectStorage.

### DiskPlainRewritableLocalFileCount \{#diskplainrewritablelocalfilecount\}

Количество файловых записей в map `plain_rewritable`, хранящейся в памяти, для LocalObjectStorage.

### DiskPlainRewritableS3DirectoryMapSize \{#diskplainrewritables3directorymapsize\}

Количество записей соответствий локальных и удалённых путей в хранимом в памяти map `plain_rewritable` для S3ObjectStorage.

### DiskPlainRewritableS3FileCount \{#diskplainrewritables3filecount\}

Количество файловых записей в map `plain_rewritable`, хранящейся в памяти, для S3ObjectStorage.

### DiskS3NoSuchKeyErrors \{#disks3nosuchkeyerrors\}

Количество ошибок `NoSuchKey`, возникающих при чтении данных из облачного хранилища S3 через диски ClickHouse.

### DiskSpaceReservedForMerge \{#diskspacereservedformerge\}

Дисковое пространство, зарезервированное для текущих фоновых слияний. Оно немного превышает суммарный размер частей, которые сейчас сливаются.

### DistrCacheAllocatedConnections \{#distrcacheallocatedconnections\}

Текущее количество выделенных подключений в пуле подключений распределённого кэша

### DistrCacheBorrowedConnections \{#distrcacheborrowedconnections\}

Количество подключений, в данный момент взятых из пула подключений распределённого кэша

### DistrCacheOpenedConnections \{#distrcacheopenedconnections\}

Количество открытых подключений к распределённому кэшу

### DistrCacheReadBuffers \{#distrcachereadbuffers\}

Количество буферов чтения распределённого кэша

### DistrCacheReadRequests \{#distrcachereadrequests\}

Количество выполненных запросов Read к распределённому кэшу

### DistrCacheRegisteredServers \{#distrcacheregisteredservers\}

Количество зарегистрированных серверов распределённого кэша

### DistrCacheRegisteredServersCurrentAZ \{#distrcacheregisteredserverscurrentaz\}

Количество зарегистрированных серверов распределённого кэша в текущей зоне доступности

### DistrCacheServerConnections \{#distrcacheserverconnections\}

Количество открытых подключений к серверу ClickHouse со стороны распределённого кэша

### DistrCacheServerRegistryConnections \{#distrcacheserverregistryconnections\}

Количество активных подключений к серверу ClickHouse со стороны распределённого кэша (не помеченных как отменённые из-за достижения лимита)

### DistrCacheServerS3CachedClients \{#distrcacheservers3cachedclients\}

Количество кэшированных S3-клиентов в распределённом кэше

### DistrCacheSharedLimitCount \{#distrcachesharedlimitcount\}

Количество открытых подключений в соответствии с DistributedCache::ConnectionPool::SharedLimit

### DistrCacheUsedConnections \{#distrcacheusedconnections\}

Количество подключений к распределённому кэшу, используемых в данный момент

### DistrCacheWriteBuffers \{#distrcachewritebuffers\}

Количество буферов записи распределённого кэша

### DistrCacheWriteRequests \{#distrcachewriterequests\}

Количество выполненных запросов на запись в распределённый кэш

### DistributedBytesToInsert \{#distributedbytestoinsert\}

Количество байтов, ожидающих обработки для асинхронной вставки в distributed таблицы. Суммируется количество байтов по каждому сегменту.

### DistributedFilesToInsert \{#distributedfilestoinsert\}

Количество файлов, ожидающих обработки для асинхронной вставки в distributed таблицы. Количество файлов по каждому сегменту суммируется.

### DistributedIndexAnalysisThreads \{#distributedindexanalysisthreads\}

Количество потоков в пуле потоков, используемых для анализа распределённого индекса.

### DistributedIndexAnalysisThreadsActive \{#distributedindexanalysisthreadsactive\}

Количество потоков в пуле потоков анализа распределённого индекса, выполняющих задачу.

### DistributedIndexAnalysisThreadsScheduled \{#distributedindexanalysisthreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков для анализа распределённого индекса.

### DistributedInsertThreads \{#distributedinsertthreads\}

Количество потоков, используемых для INSERT в таблицу Distributed.

### DistributedInsertThreadsActive \{#distributedinsertthreadsactive\}

Количество потоков, используемых для INSERT в Distributed, когда они выполняют задачу.

### DistributedInsertThreadsScheduled \{#distributedinsertthreadsscheduled\}

Количество задач, находящихся в очереди или выполняющихся, используемых для INSERT в Distributed.

### DistributedSend \{#distributedsend\}

Количество подключений к удалённым серверам для отправки данных, вставленных в distributed таблицы. Работает как в синхронном, так и в асинхронном режиме.

### DropDistributedCacheThreads \{#dropdistributedcachethreads\}

Количество потоков в пуле потоков, обслуживающем запрос drop распределённого кэша.

### DropDistributedCacheThreadsActive \{#dropdistributedcachethreadsactive\}

Количество активных потоков в пуле потоков для запроса DROP DISTRIBUTED CACHE.

### DropDistributedCacheThreadsScheduled \{#dropdistributedcachethreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков для drop распределённого кэша.

### EphemeralNode \{#ephemeralnode\}

Количество эфемерных узлов в ZooKeeper.

### FilesystemCacheDelayedCleanupElements \{#filesystemcachedelayedcleanupelements\}

Элементы файлового кэша в очереди отложенной фоновой очистки

### FilesystemCacheDownloadQueueElements \{#filesystemcachedownloadqueueelements\}

Элементы файлового кэша в очереди на загрузку

### FilesystemCacheElements \{#filesystemcacheelements\}

Элементы файлового кэша (сегменты файла)

### FilesystemCacheHoldFileSegments \{#filesystemcacheholdfilesegments\}

Сегменты файлов файлового кэша, которые в настоящее время удерживаются и не подлежат освобождению

### FilesystemCacheKeys \{#filesystemcachekeys\}

Количество ключей в файловом кэше

### FilesystemCacheReadBuffers \{#filesystemcachereadbuffers\}

Количество активных буферов кэша

### FilesystemCacheReserveThreads \{#filesystemcachereservethreads\}

Количество потоков, резервирующих место в кэше

### FilesystemCacheSize \{#filesystemcachesize\}

Размер файлового кэша в байтах

### FilesystemCacheSizeLimit \{#filesystemcachesizelimit\}

Лимит размера файлового кэша в байтах

### FilteringMarksWithPrimaryKey \{#filteringmarkswithprimarykey\}

Количество потоков, которые в данный момент фильтруют диапазоны меток по первичному ключу

### FilteringMarksWithSecondaryKeys \{#filteringmarkswithsecondarykeys\}

Количество потоков, которые в данный момент фильтруют диапазоны меток по вторичным ключам

### FormatParsingThreads \{#formatparsingthreads\}

Количество потоков в пуле потоков, используемом для разбора входных данных.

### FormatParsingThreadsActive \{#formatparsingthreadsactive\}

Количество потоков в пуле потоков, используемом для разбора входных данных и выполняющих задачу.

### FormatParsingThreadsScheduled \{#formatparsingthreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков, используемом для разбора входных данных.

### FreezePartThreads \{#freezepartthreads\}

Количество потоков в пуле потоков, используемых для заморозки частей данных.

### FreezePartThreadsActive \{#freezepartthreadsactive\}

Количество активных потоков в пуле потоков, используемых для заморозки частей данных.

### FreezePartThreadsScheduled \{#freezepartthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков для замораживания частей данных.

### GlobalThread \{#globalthread\}

Количество потоков в глобальном пуле потоков.

### GlobalThreadActive \{#globalthreadactive\}

Количество потоков в глобальном пуле потоков, выполняющих задачу.

### GlobalThreadScheduled \{#globalthreadscheduled\}

Количество задач в очереди или выполняющихся задач в глобальном пуле потоков.

### HTTPConnection \{#httpconnection\}

Количество подключений к HTTP-серверу

### HTTPConnectionsStored \{#httpconnectionsstored\}

Общее число сессий, хранящихся в пуле сессий для HTTP-хостов

### HTTPConnectionsTotal \{#httpconnectionstotal\}

Общее количество всех сессий: находящихся в пуле и активно используемых в данный момент для HTTP-хостов

### HashedDictionaryThreads \{#hasheddictionarythreads\}

Количество потоков в пуле потоков HashedDictionary.

### HashedDictionaryThreadsActive \{#hasheddictionarythreadsactive\}

Количество потоков в пуле потоков HashedDictionary, выполняющих задачу.

### HashedDictionaryThreadsScheduled \{#hasheddictionarythreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков HashedDictionary.

### HiveFilesCacheBytes \{#hivefilescachebytes\}

Размер кэша Hive в байтах

### HiveFilesCacheFiles \{#hivefilescachefiles\}

Количество кэшированных файлов в кэше Hive

### HiveMetadataFilesCacheBytes \{#hivemetadatafilescachebytes\}

Размер кэша метаданных Hive в байтах

### HiveMetadataFilesCacheFiles \{#hivemetadatafilescachefiles\}

Количество кэшированных файлов в кэше метаданных Hive

### IDiskCopierThreads \{#idiskcopierthreads\}

Количество потоков, используемых для копирования данных между дисками разных типов.

### IDiskCopierThreadsActive \{#idiskcopierthreadsactive\}

Количество потоков, выполняющих задачу по копированию данных между дисками разных типов.

### IDiskCopierThreadsScheduled \{#idiskcopierthreadsscheduled\}

Количество задача в очереди или выполняющийся задача для копирования данных между дисками разных типов.

### IOPrefetchThreads \{#ioprefetchthreads\}

Количество потоков в пуле потоков предварительной выборки ввода-вывода.

### IOPrefetchThreadsActive \{#ioprefetchthreadsactive\}

Количество потоков в пуле потоков предварительной выборки ввода-вывода, выполняющих задачу.

### IOPrefetchThreadsScheduled \{#ioprefetchthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков предварительной выборки ввода-вывода.

### IOThreads \{#iothreads\}

Количество потоков в пуле потоков ввода-вывода

### Активные потоки ввода-вывода \{#iothreadsactive\}

Количество потоков в пуле потоков ввода-вывода, выполняющих задачу.

### IOThreadsScheduled \{#iothreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков ввода-вывода.

### IOUringInFlightEvents \{#iouringinflightevents\}

Количество SQE io&#95;uring в обработке

### IOUringPendingEvents \{#iouringpendingevents\}

Количество SQE io&#95;uring, ожидающих отправки

### IOWriterThreads \{#iowriterthreads\}

Количество потоков в пуле потоков записи ввода-вывода.

### IOWriterThreadsActive \{#iowriterthreadsactive\}

Количество потоков в пуле потоков записи ввода-вывода, выполняющих задачу.

### IOWriterThreadsScheduled \{#iowriterthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков записи ввода-вывода.

### IcebergCatalogThreads \{#icebergcatalogthreads\}

Количество потоков в пуле потоков IcebergCatalog.

### IcebergCatalogThreadsActive \{#icebergcatalogthreadsactive\}

Количество потоков в пуле потоков IcebergCatalog, выполняющих задачу.

### IcebergCatalogThreadsScheduled \{#icebergcatalogthreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков IcebergCatalog.

### IcebergMetadataFilesCacheBytes \{#icebergmetadatafilescachebytes\}

Размер кэша метаданных Iceberg в байтах

### IcebergMetadataFilesCacheFiles \{#icebergmetadatafilescachefiles\}

Количество кэшированных файлов в кэше метаданных Iceberg

### IndexMarkCacheBytes \{#indexmarkcachebytes\}

Общий размер кэша меток вторичных индексов в байтах

### Файлы меток в кэше меток индекса \{#indexmarkcachefiles\}

Общее число файлов меток вторичных индексов, хранящихся в кэше меток

### IndexUncompressedCacheBytes \{#indexuncompressedcachebytes\}

Общий размер несжатого кэша для вторичных индексов в байтах. Несжатый кэш обычно не повышает производительность, поэтому его использования по возможности следует избегать

### IndexUncompressedCacheCells \{#indexuncompressedcachecells\}

Общее количество записей в несжатом кэше для вторичных индексов. Каждая запись представляет собой распакованный блок данных. Несжатый кэш обычно не повышает производительность, поэтому его использования по большей части следует избегать

### InterserverConnection \{#interserverconnection\}

Количество подключений от других реплик для получения частей

### IsServerShuttingDown \{#isservershuttingdown\}

Показывает, завершает ли сервер работу: 0 = нет, 1 = да

### KafkaAssignedPartitions \{#kafkaassignedpartitions\}

Количество партиций, которые в данный момент назначены таблицам Kafka

### KafkaBackgroundReads \{#kafkabackgroundreads\}

Количество фоновых операций чтения, выполняемых в данный момент (заполняющих materialized views из Kafka)

### KafkaConsumers \{#kafkaconsumers\}

Количество активных консьюмеров Kafka

### KafkaConsumersInUse \{#kafkaconsumersinuse\}

Количество консьюмеров, которые в настоящее время используются для прямого или фонового чтения

### KafkaConsumersWithAssignment \{#kafkaconsumerswithassignment\}

Количество активных консьюмеров Kafka, у которых назначены какие-либо партиции.

### KafkaLibrdkafkaThreads \{#kafkalibrdkafkathreads\}

Количество активных потоков librdkafka

### KafkaProducers \{#kafkaproducers\}

Число созданных активных продюсеров Kafka

### KafkaWrites \{#kafkawrites\}

Количество вставок в Kafka, выполняемых в данный момент

### KeeperAliveConnections \{#keeperaliveconnections\}

Количество активных подключений

### KeeperOutstandingRequests \{#keeperoutstandingrequests\}

Количество ожидающих обработки запросов

### LicenseRemainingSeconds \{#licenseremainingseconds\}

Оставшееся количество секунд действия лицензии.

### LocalThread \{#localthread\}

Устарело. Количество потоков в локальных пулах потоков. Потоки для локальных пулов потоков выделяются из глобального пула потоков.

### LocalThreadActive \{#localthreadactive\}

Устарело. Количество потоков в локальных пулах потоков, выполняющих задачу.

### LocalThreadScheduled \{#localthreadscheduled\}

Устарело. Количество задача в очереди или выполняющийся задача в локальных пулах потоков.

### MMapCacheCells \{#mmapcachecells\}

Количество файлов, открытых с помощью `mmap` (отображённых в памяти). Используется для запросов, в которых параметр `local_filesystem_read_method` установлен в `mmap`. Файлы, открытые с помощью `mmap`, хранятся в кэше, чтобы избежать дорогостоящих сбросов TLB.

### MMappedFileBytes \{#mmappedfilebytes\}

Суммарный размер областей файлов, отображённых в память.

### MMappedFiles \{#mmappedfiles\}

Общее количество файлов, отображённых в память.

### MarkCacheBytes \{#markcachebytes\}

Суммарный размер кэша меток в байтах

### MarkCacheFiles \{#markcachefiles\}

Общее количество файлов меток, хранящихся в кэше меток

### MarksLoaderThreads \{#marksloaderthreads\}

Количество потоков в пуле потоков для загрузки меток.

### MarksLoaderThreadsActive \{#marksloaderthreadsactive\}

Количество потоков в пуле потоков для загрузки меток, выполняющих задачу.

### MarksLoaderThreadsScheduled \{#marksloaderthreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков для загрузки меток.

### MaxAllocatedEphemeralLockSequentialNumber \{#maxallocatedephemerallocksequentialnumber\}

Максимальный порядковый номер, выделенный для эфемерных znode блокировок в ZooKeeper. В первую очередь зависит от номеров блоков.

### MaxDDLEntryID \{#maxddlentryid\}

Максимальный идентификатор DDL-записи, обработанной DDLWorker.

### MaxPushedDDLEntryID \{#maxpushedddlentryid\}

Максимальная DDL-запись DDLWorker, отправленная в ZooKeeper.

### MemoryTracking \{#memorytracking\}

Общий объем памяти (в байтах), выделенной сервером.

### MemoryTrackingUncorrected \{#memorytrackinguncorrected\}

Общий объём памяти (в байтах), выделенной сервером без корректировки по RSS.

### Merge \{#merge\}

Количество выполняемых фоновых слияний

### MergeJoinBlocksCacheBytes \{#mergejoinblockscachebytes\}

Общее количество байтов, используемых блоками в кэше MergeJoin

### MergeJoinBlocksCacheCount \{#mergejoinblockscachecount\}

Общее количество кэшированных блоков в MergeJoin

### MergeParts \{#mergeparts\}

Количество исходных частей, участвующих в выполняемых в данный момент фоновых слияниях

### MergeTreeAllRangesAnnouncementsSent \{#mergetreeallrangesannouncementssent\}

Текущее количество объявлений о наборе частей данных (для таблиц MergeTree), которые в данный момент передаются с удалённого сервера на инициирующий сервер. Измеряется на стороне удалённого сервера.

### MergeTreeBackgroundExecutorThreads \{#mergetreebackgroundexecutorthreads\}

Количество потоков в пуле потоков MergeTreeBackgroundExecutor.

### MergeTreeBackgroundExecutorThreadsActive \{#mergetreebackgroundexecutorthreadsactive\}

Количество потоков из пула MergeTreeBackgroundExecutor, выполняющих задачу.

### MergeTreeBackgroundExecutorThreadsScheduled \{#mergetreebackgroundexecutorthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков MergeTreeBackgroundExecutor.

### MergeTreeDataSelectExecutorThreads \{#mergetreedataselectexecutorthreads\}

Количество потоков в пуле потоков MergeTreeDataSelectExecutor.

### MergeTreeDataSelectExecutorThreadsActive \{#mergetreedataselectexecutorthreadsactive\}

Количество потоков в пуле потоков MergeTreeDataSelectExecutor, выполняющих задачу.

### MergeTreeDataSelectExecutorThreadsScheduled \{#mergetreedataselectexecutorthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков MergeTreeDataSelectExecutor.

### MergeTreeFetchPartitionThreads \{#mergetreefetchpartitionthreads\}

Количество потоков для ALTER TABLE FETCH PARTITION

### MergeTreeFetchPartitionThreadsActive \{#mergetreefetchpartitionthreadsactive\}

Количество потоков, используемых командой ALTER TABLE FETCH PARTITION для извлечения части

### MergeTreeFetchPartitionThreadsScheduled \{#mergetreefetchpartitionthreadsscheduled\}

Количество поставленных в очередь или выполняемых операций получения частей в ALTER TABLE FETCH PARTITION

### MergeTreeOutdatedPartsLoaderThreads \{#mergetreeoutdatedpartsloaderthreads\}

Количество потоков в пуле для загрузки устаревших частей данных.

### MergeTreeOutdatedPartsLoaderThreadsActive \{#mergetreeoutdatedpartsloaderthreadsactive\}

Количество активных потоков в пуле потоков для загрузки устаревших частей.

### MergeTreeOutdatedPartsLoaderThreadsScheduled \{#mergetreeoutdatedpartsloaderthreadsscheduled\}

Количество задач в пуле потоков для загрузки устаревших частей данных, находящихся в очереди или выполняющихся.

### MergeTreePartsCleanerThreads \{#mergetreepartscleanerthreads\}

Количество потоков в пуле потоков очистки частей MergeTree.

### MergeTreePartsCleanerThreadsActive \{#mergetreepartscleanerthreadsactive\}

Количество потоков в пуле потоков очистки частей MergeTree, выполняющих задачу.

### MergeTreePartsCleanerThreadsScheduled \{#mergetreepartscleanerthreadsscheduled\}

Количество задач в пуле потоков очистки частей MergeTree, находящихихся в очереди или выполняющихся.

### MergeTreePartsLoaderThreads \{#mergetreepartsloaderthreads\}

Количество потоков в пуле потоков загрузки частей MergeTree.

### MergeTreePartsLoaderThreadsActive \{#mergetreepartsloaderthreadsactive\}

Количество потоков в пуле потоков загрузчика частей MergeTree, выполняющих задачу.

### MergeTreePartsLoaderThreadsScheduled \{#mergetreepartsloaderthreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков загрузки частей MergeTree.

### MergeTreeReadTaskRequestsSent \{#mergetreereadtaskrequestssent\}

Текущее количество выполняющихся callback-запросов, отправляемых с удалённого сервера обратно на инициирующий сервер для выбора задачи чтения (для таблиц MergeTree). Измеряется на стороне удалённого сервера.

### MergeTreeSnapshotCommitThreads \{#mergetreesnapshotcommitthreads\}

Количество потоков, используемых для фиксации снимка

### MergeTreeSnapshotCommitThreadsActive \{#mergetreesnapshotcommitthreadsactive\}

Количество активных потоков, используемых для фиксации снимка

### MergeTreeSnapshotCommitThreadsScheduled \{#mergetreesnapshotcommitthreadsscheduled\}

Количество потоков, запланированных для фиксации снимка

### MergeTreeSubcolumnsReaderThreads \{#mergetreesubcolumnsreaderthreads\}

Количество потоков в пуле потоков, используемом для чтения подстолбцов в MergeTree.

### MergeTreeSubcolumnsReaderThreadsActive \{#mergetreesubcolumnsreaderthreadsactive\}

Количество потоков в пуле потоков, используемом для чтения подстолбцов в MergeTree, которые в данный момент выполняют задачу.

### MergeTreeSubcolumnsReaderThreadsScheduled \{#mergetreesubcolumnsreaderthreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков, используемом для чтения подстолбцов в MergeTree.

### MergeTreeUnexpectedPartsLoaderThreads \{#mergetreeunexpectedpartsloaderthreads\}

Количество потоков в пуле для загрузки неожиданных частей данных.

### MergeTreeUnexpectedPartsLoaderThreadsActive \{#mergetreeunexpectedpartsloaderthreadsactive\}

Количество активных потоков в пуле потоков для загрузки неожиданных частей данных.

### MergeTreeUnexpectedPartsLoaderThreadsScheduled \{#mergetreeunexpectedpartsloaderthreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков для загрузки неожиданных частей данных.

### MergesMutationsMemoryTracking \{#mergesmutationsmemorytracking\}

Общий объём памяти (в байтах), выделенной фоновыми задачами — слияниями и мутациями.

### MetadataFromKeeperCacheObjects \{#metadatafromkeepercacheobjects\}

Количество объектов в дисковом кэше метаданных.

### Перемещения \{#move\}

Количество перемещений, выполняющихся в данный момент

### MySQLConnection \{#mysqlconnection\}

Число клиентских подключений по протоколу MySQL

### NamedCollection \{#namedcollection\}

Число именованных коллекций

### NetworkReceive \{#networkreceive\}

Количество потоков, принимающих данные из сети. Учитывается только сетевое взаимодействие ClickHouse, без учёта сторонних библиотек.

### NetworkSend \{#networksend\}

Количество потоков, отправляющих данные по сети. Учитывается только сетевое взаимодействие, связанное с ClickHouse, но не со сторонними библиотеками.

### ObjectStorageAzureThreads \{#objectstorageazurethreads\}

Количество потоков в пуле потоков AzureObjectStorage.

### ObjectStorageAzureThreadsActive \{#objectstorageazurethreadsactive\}

Количество потоков в пуле потоков AzureObjectStorage, выполняющих задачу.

### ObjectStorageAzureThreadsScheduled \{#objectstorageazurethreadsscheduled\}

Количество задач в пуле потоков AzureObjectStorage, находящихся в очереди или выполняющихся.

### ObjectStorageQueueMetadataCacheSizeBytes \{#objectstoragequeuemetadatacachesizebytes\}

Размер кэша метаданных ObjectStorageQueue в байтах.

### ObjectStorageQueueMetadataCacheSizeElements \{#objectstoragequeuemetadatacachesizeelements\}

Размер кэша метаданных ObjectStorageQueue в элементах.

### ObjectStorageQueueRegisteredServers \{#objectstoragequeueregisteredservers\}

Количество серверов, зарегистрированных в StorageS3(Azure)Queue

### ObjectStorageQueueShutdownThreads \{#objectstoragequeueshutdownthreads\}

Количество потоков в пуле для завершения работы очереди объектного хранилища.

### ObjectStorageQueueShutdownThreadsActive \{#objectstoragequeueshutdownthreadsactive\}

Количество потоков в пуле завершения работы очереди объектного хранилища, выполняющих задачу.

### ObjectStorageQueueShutdownThreadsScheduled \{#objectstoragequeueshutdownthreadsscheduled\}

Количество задач, поставленных в очередь или выполняющихся в пуле завершения работы очереди объектного хранилища.

### ObjectStorageS3Threads \{#objectstorages3threads\}

Количество потоков в пуле потоков S3ObjectStorage.

### ObjectStorageS3ThreadsActive \{#objectstorages3threadsactive\}

Количество потоков из пула потоков S3ObjectStorage, выполняющих задачу.

### ObjectStorageS3ThreadsScheduled \{#objectstorages3threadsscheduled\}

Количество задач в пуле потоков S3ObjectStorage, находящихся в очереди или выполняющихся.

### OpenFileForRead \{#openfileforread\}

Количество файлов, открытых для чтения

### OpenFileForWrite \{#openfileforwrite\}

Количество файлов, открытых для записи

### OutdatedPartsLoadingThreads \{#outdatedpartsloadingthreads\}

Количество потоков в пуле потоков, используемых для загрузки устаревших частей данных.

### OutdatedPartsLoadingThreadsActive \{#outdatedpartsloadingthreadsactive\}

Количество активных потоков в пуле потоков, используемых для загрузки устаревших частей данных.

### OutdatedPartsLoadingThreadsScheduled \{#outdatedpartsloadingthreadsscheduled\}

Количество задач в пуле потоков, находящихся в очереди или выполняющихся, для загрузки устаревших частей данных.

### PageCacheBytes \{#pagecachebytes\}

Общий размер кэша страниц в пространстве пользователя, в байтах

### PageCacheCells \{#pagecachecells\}

Общее количество записей в кэше страниц в пространстве пользователя

### ParallelCompressedWriteBufferThreads \{#parallelcompressedwritebufferthreads\}

Количество потоков во всех экземплярах ParallelCompressedWriteBuffer — эти потоки выполняют параллельное сжатие и запись

### ParallelCompressedWriteBufferWait \{#parallelcompressedwritebufferwait\}

Количество потоков во всех экземплярах ParallelCompressedWriteBuffer, которые в данный момент ожидают, пока буфер станет доступен для записи

### ParallelFormattingOutputFormatThreads \{#parallelformattingoutputformatthreads\}

Количество потоков в пуле потоков ParallelFormattingOutputFormatThreads.

### ParallelFormattingOutputFormatThreadsActive \{#parallelformattingoutputformatthreadsactive\}

Количество потоков в пуле потоков ParallelFormattingOutputFormatThreads, выполняющих задачу.

### ParallelFormattingOutputFormatThreadsScheduled \{#parallelformattingoutputformatthreadsscheduled\}

Количество задач в очереди или выполняющихся задач в пуле потоков ParallelFormattingOutputFormatThreads.

### ParallelWithQueryActiveThreads \{#parallelwithqueryactivethreads\}

Количество активных потоков в пуле потоков для обработки запросов PARALLEL WITH.

### ParallelWithQueryScheduledThreads \{#parallelwithqueryscheduledthreads\}

Количество находящихся в очереди или выполняющихся задач в пуле потоков для обработки запросов PARALLEL WITH.

### ParallelWithQueryThreads \{#parallelwithquerythreads\}

Количество потоков в пуле потоков, используемых для обработки запросов PARALLEL WITH.

### ParquetEncoderThreads \{#parquetencoderthreads\}

Количество потоков в пуле потоков ParquetBlockOutputFormat.

### ParquetEncoderThreadsActive \{#parquetencoderthreadsactive\}

Количество потоков в пуле потоков ParquetBlockOutputFormat, выполняющих задачу.

### ParquetEncoderThreadsScheduled \{#parquetencoderthreadsscheduled\}

Количество задач, ожидающих в очереди или выполняющихся в пуле потоков ParquetBlockOutputFormat.

### ParquetMetadataCacheBytes \{#parquetmetadatacachebytes\}

Размер кэша метаданных Parquet в байтах

### ParquetMetadataCacheFiles \{#parquetmetadatacachefiles\}

Количество файлов, кэшированных в кэше метаданных Parquet

### PartMutation \{#partmutation\}

Количество мутаций (ALTER DELETE/UPDATE)

### PartsActive \{#partsactive\}

Активная часть данных, используемая текущими и будущими SELECT-запросами.

### PartsCommitted \{#partscommitted\}

Устаревшая метрика. См. PartsActive.

### PartsCompact \{#partscompact\}

Компактные части.

### PartsDeleteOnDestroy \{#partsdeleteondestroy\}

Часть была перемещена на другой диск и должна быть удалена в своём деструкторе.

### PartsDeleting \{#partsdeleting\}

Неактивная часть данных со счётчиком ссылок, равным 1; в данный момент удаляется очистителем.

### PartsOutdated \{#partsoutdated\}

Неактивная часть данных, но может использоваться только текущими запросами SELECT; может быть удалена после завершения этих запросов.

### PartsPreActive \{#partspreactive\}

Часть находится в data&#95;parts, но не используется в SELECT-запросах.

### PartsPreCommitted \{#partsprecommitted\}

Устаревший показатель. См. PartsPreActive.

### PartsTemporary \{#partstemporary\}

Часть сейчас создаётся и отсутствует в списке data&#95;parts.

### PartsWide \{#partswide\}

Части в широком формате.

### PendingAsyncInsert \{#pendingasyncinsert\}

Количество асинхронных insert-запросов, ожидающих сброса.

### PolygonDictionaryThreads \{#polygondictionarythreads\}

Количество потоков в пуле потоков для словарей полигонов.

### PolygonDictionaryThreadsActive \{#polygondictionarythreadsactive\}

Количество активных потоков в пуле потоков для полигональных словарей.

### PolygonDictionaryThreadsScheduled \{#polygondictionarythreadsscheduled\}

Количество задач в пуле потоков для полигональных словарей, находящихся в очереди или выполняющихся.

### PostgreSQLConnection \{#postgresqlconnection\}

Количество клиентских подключений по протоколу PostgreSQL

### PrimaryIndexCacheBytes \{#primaryindexcachebytes\}

Общий размер кэша первичного индекса в байтах

### PrimaryIndexCacheFiles \{#primaryindexcachefiles\}

Общее количество файлов индекса, хранящихся в кэше первичного индекса

### Запрос \{#query\}

Количество выполняемых запросов

### QueryCacheBytes \{#querycachebytes\}

Общий размер кэша запросов в байтах

### QueryCacheEntries \{#querycacheentries\}

Общее количество записей в кэше запросов

### QueryConditionCacheBytes \{#queryconditioncachebytes\}

Общий размер кэша условий запроса в байтах

### QueryConditionCacheEntries \{#queryconditioncacheentries\}

Общее количество записей в кэше условий запроса

### QueryNonInternal \{#querynoninternal\}

Количество выполняющихся пользовательских запросов (запросов, инициированных пользователями, за исключением внутренних запросов ClickHouse)

### QueryPipelineExecutorThreads \{#querypipelineexecutorthreads\}

Количество потоков в пуле потоков PipelineExecutor.

### QueryPipelineExecutorThreadsActive \{#querypipelineexecutorthreadsactive\}

Количество потоков из пула потоков PipelineExecutor, выполняющих задачу.

### QueryPipelineExecutorThreadsScheduled \{#querypipelineexecutorthreadsscheduled\}

Количество задач в пуле потоков PipelineExecutor, находящихся в очереди или выполняющихся.

### QueryPreempted \{#querypreempted\}

Количество выполняющихся задач, которые приостановлены и ожидают возобновления из-за настройки &#39;priority&#39;.

### QueryThread \{#querythread\}

Количество потоков, обрабатывающих запросы

### RWLockActiveReaders \{#rwlockactivereaders\}

Количество потоков, удерживающих блокировку чтения в RWLock таблицы.

### RWLockActiveWriters \{#rwlockactivewriters\}

Количество потоков, удерживающих блокировку на запись в RWLock таблицы.

### RWLockWaitingReaders \{#rwlockwaitingreaders\}

Количество потоков, ожидающих чтения блокировки RWLock таблицы.

### RWLockWaitingWriters \{#rwlockwaitingwriters\}

Количество потоков, ожидающих разрешения на запись в RWLock таблицы.

### Чтение \{#read\}

Количество системных вызовов чтения (read, pread, io&#95;getevents и т. д.), находящихся в процессе выполнения

### ReadTaskRequestsSent \{#readtaskrequestssent\}

Текущее количество callback-запросов в обработке, отправленных с удалённого сервера обратно на инициирующий сервер для выбора задачи чтения (для табличной функции s3Cluster и аналогичных случаев). Измеряется на стороне удалённого сервера.

### ReadonlyDisks \{#readonlydisks\}

Количество дисков, помеченных как доступные только для чтения во время проверки дисков.

### ReadonlyReplica \{#readonlyreplica\}

Количество реплицируемых таблиц, которые в настоящий момент находятся в режиме только для чтения из-за повторной инициализации после потери сессии ZooKeeper или из-за запуска без настроенного ZooKeeper.

### RefreshableViews \{#refreshableviews\}

Количество materialized views с периодическим обновлением (REFRESH)

### RefreshingViews \{#refreshingviews\}

Количество materialized views, которые сейчас обновляются

### RemoteRead \{#remoteread\}

Количество выполняемых чтений с удалённым считывателем

### ReplicaReady \{#replicaready\}

Указывает, готова ли реплика к выполнению запросов: 0 = нет, 1 = да

### ReplicatedChecks \{#replicatedchecks\}

Количество частей данных, проверяемых на согласованность

### ReplicatedFetch \{#replicatedfetch\}

Количество частей данных, загружаемых с реплики

### ReplicatedSend \{#replicatedsend\}

Количество частей данных, отправляемых на реплики

### RestartReplicaThreads \{#restartreplicathreads\}

Количество потоков в пуле потоков команды RESTART REPLICA.

### RestartReplicaThreadsActive \{#restartreplicathreadsactive\}

Количество потоков в пуле потоков RESTART REPLICA, выполняющих задачу.

### RestartReplicaThreadsScheduled \{#restartreplicathreadsscheduled\}

Количество задач, находящихся в очереди или выполняющихся, в пуле потоков RESTART REPLICA.

### RestoreThreads \{#restorethreads\}

Количество потоков в пуле потоков, используемом для RESTORE.

### RestoreThreadsActive \{#restorethreadsactive\}

Количество потоков в пуле потоков для RESTORE, выполняющих задачу.

### RestoreThreadsScheduled \{#restorethreadsscheduled\}

Количество задач в очереди или выполняющихся для RESTORE.

### Ревизия \{#revision\}

Ревизия сервера. Это число увеличивается с каждым релизом или кандидатом в релизы, за исключением патч-релизов.

### S3CachedCredentialsProviders \{#s3cachedcredentialsproviders\}

Общее число кэшированных поставщиков учетных данных

### Запросы к S3 \{#s3requests\}

Количество запросов к S3

### SchedulerIOReadScheduled \{#schedulerioreadscheduled\}

Количество операций чтения ввода-вывода, запланированных в настоящий момент

### SchedulerIOWriteScheduled \{#scheduleriowritescheduled\}

Количество операций записи ввода-вывода, запланированных в настоящий момент

### SendExternalTables \{#sendexternaltables\}

Количество подключений, через которые данные внешних таблиц отправляются на удалённые серверы. Внешние таблицы используются для реализации операторов GLOBAL IN и GLOBAL JOIN в распределённых подзапросах.

### SendScalars \{#sendscalars\}

Количество подключений, отправляющих скалярные данные на удалённые серверы.

### SharedCatalogDropDetachLocalTablesErrors \{#sharedcatalogdropdetachlocaltableserrors\}

Количество ошибок, возникших при попытке удаления или отсоединения локальных таблиц в Shared Catalog.

### SharedCatalogDropLocalThreads \{#sharedcatalogdroplocalthreads\}

Количество потоков в пуле потоков, используемых для удаления локальных таблиц в Shared Catalog.

### SharedCatalogDropLocalThreadsActive \{#sharedcatalogdroplocalthreadsactive\}

Количество активных потоков в пуле потоков, выполняющем удаление локальных таблиц в Shared Catalog.

### SharedCatalogDropLocalThreadsScheduled \{#sharedcatalogdroplocalthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков для удаления локальных таблиц в Shared Catalog.

### SharedCatalogDropZooKeeperThreads \{#sharedcatalogdropzookeeperthreads\}

Количество потоков в пуле потоков для удаления объекта в ZooKeeper в Shared Catalog.

### SharedCatalogDropZooKeeperThreadsActive \{#sharedcatalogdropzookeeperthreadsactive\}

Количество активных потоков в пуле потоков, отвечающих за удаление объектов в ZooKeeper для Shared Catalog.

### SharedCatalogDropZooKeeperThreadsScheduled \{#sharedcatalogdropzookeeperthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков для удаления объекта в ZooKeeper в Shared Catalog.

### SharedCatalogNumberOfObjectsInState \{#sharedcatalognumberofobjectsinstate\}

Количество объектов, находящихся в текущем состоянии Shared Catalog.

### SharedCatalogStateApplicationThreads \{#sharedcatalogstateapplicationthreads\}

Количество потоков в пуле потоков, используемых для применения состояния в Shared Catalog.

### SharedCatalogStateApplicationThreadsActive \{#sharedcatalogstateapplicationthreadsactive\}

Количество активных потоков в пуле потоков, используемом для применения состояния в Shared Catalog.

### SharedCatalogStateApplicationThreadsScheduled \{#sharedcatalogstateapplicationthreadsscheduled\}

Количество задач, находящихся в очереди или выполняющихся, в пуле потоков, используемом для применения состояния в Shared Catalog.

### SharedDatabaseCatalogTablesInLocalDropDetachQueue \{#shareddatabasecatalogtablesinlocaldropdetachqueue\}

Количество таблиц в очереди на локальное удаление или отсоединение в Shared Catalog.

### SharedMergeTreeAssignedCurrentParts \{#sharedmergetreeassignedcurrentparts\}

Количество частей, заблокированных операцией слияния или мутации

### SharedMergeTreeBrokenCondemnedPartsInKeeper \{#sharedmergetreebrokencondemnedpartsinkeeper\}

Сколько записей о поврежденных частях, помеченных к удалению, хранится в Keeper

### SharedMergeTreeCondemnedPartsInKeeper \{#sharedmergetreecondemnedpartsinkeeper\}

Сколько записей о частях, помеченных на удаление, хранится в Keeper

### SharedMergeTreeFetch \{#sharedmergetreefetch\}

Количество выполняющихся fetch-операций

### SharedMergeTreeMaxActiveReplicas \{#sharedmergetreemaxactivereplicas\}

Максимальное количество активных реплик, зарегистрированных в Keeper

### SharedMergeTreeMaxInactiveReplicas \{#sharedmergetreemaxinactivereplicas\}

Максимальное количество неактивных реплик, зарегистрированных в Keeper

### SharedMergeTreeMaxPartitions \{#sharedmergetreemaxpartitions\}

Максимальное количество партиций, зарегистрированных в Keeper среди всех таблиц SharedMergeTree

### SharedMergeTreeMaxReplicas \{#sharedmergetreemaxreplicas\}

Максимальное количество реплик, зарегистрированных в Keeper во всех таблицах. Обратите внимание: это может не совпадать с суммой SharedMergeTreeMaxActiveReplicas и SharedMergeTreeMaxInactiveReplicas

### SharedMergeTreeMinActiveReplicas \{#sharedmergetreeminactivereplicas\}

Минимальное количество активных реплик, зарегистрированных в Keeper

### SharedMergeTreeMinInactiveReplicas \{#sharedmergetreemininactivereplicas\}

Минимальное количество неактивных реплик, зарегистрированных в Keeper

### SharedMergeTreeMinPartitions \{#sharedmergetreeminpartitions\}

Минимальное количество партиций, зарегистрированных в Keeper во всех таблицах SharedMergeTree

### SharedMergeTreeMinReplicas \{#sharedmergetreeminreplicas\}

Минимальное количество реплик, зарегистрированных в Keeper, среди всех таблиц. Обратите внимание, что оно может не совпадать с суммой SharedMergeTreeMinActiveReplicas и SharedMergeTreeMinInactiveReplicas

### SharedMergeTreeOutdatedPartsInKeeper \{#sharedmergetreeoutdatedpartsinkeeper\}

Количество записей об устаревших частях, хранящихся в Keeper

### SharedMergeTreeThreads \{#sharedmergetreethreads\}

Количество потоков в пулах потоков внутренних механизмов SharedMergeTree

### SharedMergeTreeThreadsActive \{#sharedmergetreethreadsactive\}

Количество потоков в пулах потоков внутренних компонентов SharedMergeTree, выполняющих задачу

### SharedMergeTreeThreadsScheduled \{#sharedmergetreethreadsscheduled\}

Количество потоков в пулах потоков внутренних компонентов SharedMergeTree, находящихся в очереди или активных

### StartupScriptsExecutionState \{#startupscriptsexecutionstate\}

Состояние выполнения стартовых скриптов: 0 = не завершено, 1 = успешно, 2 = сбой.

### StartupSystemTablesThreads \{#startupsystemtablesthreads\}

Количество потоков в пуле потоков StartupSystemTables.

### StartupSystemTablesThreadsActive \{#startupsystemtablesthreadsactive\}

Количество потоков в пуле потоков StartupSystemTables, выполняющих задачу.

### StartupSystemTablesThreadsScheduled \{#startupsystemtablesthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков StartupSystemTables.

### StatelessWorkerThreads \{#statelessworkerthreads\}

Количество потоков в пуле потоков stateless worker.

### StatelessWorkerThreadsActive \{#statelessworkerthreadsactive\}

Количество потоков в пуле потоков stateless worker, выполняющих задачу.

### StatelessWorkerThreadsScheduled \{#statelessworkerthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков stateless worker.

### StorageBufferBytes \{#storagebufferbytes\}

Количество байт в буферах таблиц Buffer

### StorageBufferFlushThreads \{#storagebufferflushthreads\}

Количество потоков, выполняющих фоновый сброс данных в StorageBuffer

### StorageBufferFlushThreadsActive \{#storagebufferflushthreadsactive\}

Количество потоков, выполняющих задачу фонового сброса в StorageBuffer

### Запланированные потоки фонового сброса StorageBuffer \{#storagebufferflushthreadsscheduled\}

Количество потоков, поставленных в очередь или активных, для фонового сброса в StorageBuffer

### StorageBufferRows \{#storagebufferrows\}

Количество строк в буферах таблиц типа Buffer

### StorageConnectionsStored \{#storageconnectionsstored\}

Общее количество сессий, хранящихся в пуле сессий хранилищ

### StorageConnectionsTotal \{#storageconnectionstotal\}

Общее количество всех сессий для хранилищ: находящихся в пуле и активно используемых в данный момент

### StorageDistributedThreads \{#storagedistributedthreads\}

Количество потоков в пуле потоков StorageDistributed.

### StorageDistributedThreadsActive \{#storagedistributedthreadsactive\}

Количество потоков в пуле потоков StorageDistributed, выполняющих задачу.

### StorageDistributedThreadsScheduled \{#storagedistributedthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков StorageDistributed.

### StorageHiveThreads \{#storagehivethreads\}

Количество потоков в пуле потоков StorageHive.

### StorageHiveThreadsActive \{#storagehivethreadsactive\}

Количество потоков в пуле потоков StorageHive, выполняющих задачу.

### StorageHiveThreadsScheduled \{#storagehivethreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков StorageHive.

### StorageObjectStorageThreads \{#storageobjectstoragethreads\}

Количество потоков в пулах потоков удалённых движков таблиц.

### StorageObjectStorageThreadsActive \{#storageobjectstoragethreadsactive\}

Количество потоков в пуле потоков удалённых движков таблиц, выполняющих задачу.

### StorageObjectStorageThreadsScheduled \{#storageobjectstoragethreadsscheduled\}

Количество задача в очереди или выполняющийся задача в пуле потоков удалённых табличных движков.

### StorageS3Threads \{#storages3threads\}

Количество потоков в пуле потоков StorageS3.

### StorageS3ThreadsActive \{#storages3threadsactive\}

Количество потоков в пуле потоков StorageS3, выполняющих задачу.

### StorageS3ThreadsScheduled \{#storages3threadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков StorageS3.

### SystemDatabaseReplicasThreads \{#systemdatabasereplicasthreads\}

Количество потоков в пуле потоков system.database&#95;replicas.

### SystemDatabaseReplicasThreadsActive \{#systemdatabasereplicasthreadsactive\}

Количество потоков в пуле потоков system.database&#95;replicas, выполняющих задачу.

### SystemDatabaseReplicasThreadsScheduled \{#systemdatabasereplicasthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков system.database&#95;replicas.

### SystemReplicasThreads \{#systemreplicasthreads\}

Количество потоков в пуле потоков system.replicas.

### SystemReplicasThreadsActive \{#systemreplicasthreadsactive\}

Количество потоков в пуле потоков `system.replicas`, выполняющих задачу.

### SystemReplicasThreadsScheduled \{#systemreplicasthreadsscheduled\}

Количество задача в очереди или выполняющийся задача в пуле потоков system.replicas.

### TCPConnection \{#tcpconnection\}

Количество подключений к TCP-серверу (клиентов с native-интерфейсом); также включает межсерверные подключения для распределённых запросов

### TablesLoaderBackgroundThreads \{#tablesloaderbackgroundthreads\}

Количество потоков в фоновом пуле потоков загрузчика таблиц.

### TablesLoaderBackgroundThreadsActive \{#tablesloaderbackgroundthreadsactive\}

Количество потоков в фоновом пуле потоков загрузчика таблиц, выполняющих задачу.

### TablesLoaderBackgroundThreadsScheduled \{#tablesloaderbackgroundthreadsscheduled\}

Количество задач в очереди или выполняющихся в фоновом пуле потоков загрузчика таблиц.

### TablesLoaderForegroundThreads \{#tablesloaderforegroundthreads\}

Количество потоков в основном пуле потоков загрузчика таблиц.

### TablesLoaderForegroundThreadsActive \{#tablesloaderforegroundthreadsactive\}

Количество потоков в пуле потоков переднего плана загрузчика таблиц, которые выполняют задачу.

### TablesLoaderForegroundThreadsScheduled \{#tablesloaderforegroundthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков переднего плана загрузчика таблиц.

### TablesToDropQueueSize \{#tablestodropqueuesize\}

Количество удалённых таблиц, ожидающих фонового удаления данных.

### TaskTrackerThreads \{#tasktrackerthreads\}

Количество потоков, используемых средством отслеживания удалённых задач распределённого запроса.

### TaskTrackerThreadsActive \{#tasktrackerthreadsactive\}

Количество потоков в пуле трекера удалённых задач распределённого запроса, которые выполняют задачу.

### TaskTrackerThreadsScheduled \{#tasktrackerthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков трекера удалённых задач распределённого запроса.

### TemporaryFilesForAggregation \{#temporaryfilesforaggregation\}

Количество временных файлов, созданных при внешней агрегации

### TemporaryFilesForJoin \{#temporaryfilesforjoin\}

Количество временных файлов, созданных для JOIN

### TemporaryFilesForMerge \{#temporaryfilesformerge\}

Количество временных файлов для вертикального слияния

### TemporaryFilesForSort \{#temporaryfilesforsort\}

Количество временных файлов, созданных при внешней сортировке

### TemporaryFilesUnknown \{#temporaryfilesunknown\}

Количество временных файлов, созданных без известной причины

### TextIndexHeaderCacheBytes \{#textindexheadercachebytes\}

Размер кэша заголовков текстового индекса в байтах

### TextIndexHeaderCacheCells \{#textindexheadercachecells\}

Количество записей в кэше заголовков текстового индекса

### TextIndexPostingsCacheBytes \{#textindexpostingscachebytes\}

Размер кэша списков вхождений текстового индекса в байтах

### TextIndexPostingsCacheCells \{#textindexpostingscachecells\}

Количество записей в кэше списков вхождений текстового индекса

### TextIndexTokensCacheBytes \{#textindextokenscachebytes\}

Размер кэша токенов текстового индекса в байтах

### TextIndexTokensCacheCells \{#textindextokenscachecells\}

Количество записей в кэше токенов текстового индекса

### ThreadPoolFSReaderThreads \{#threadpoolfsreaderthreads\}

Количество потоков в пуле потоков для local_filesystem_read_method=threadpool.

### ThreadPoolFSReaderThreadsActive \{#threadpoolfsreaderthreadsactive\}

Количество потоков из пула потоков для `local_filesystem_read_method=threadpool`, выполняющих задачу.

### ThreadPoolFSReaderThreadsScheduled \{#threadpoolfsreaderthreadsscheduled\}

Количество задача в очереди или выполняющийся задача в пуле потоков для local_filesystem_read_method=threadpool.

### ThreadPoolRemoteFSReaderThreads \{#threadpoolremotefsreaderthreads\}

Количество потоков в пуле потоков для remote_filesystem_read_method=threadpool.

### ThreadPoolRemoteFSReaderThreadsActive \{#threadpoolremotefsreaderthreadsactive\}

Количество потоков в пуле потоков для `remote_filesystem_read_method=threadpool`, выполняющих задачу.

### ThreadPoolRemoteFSReaderThreadsScheduled \{#threadpoolremotefsreaderthreadsscheduled\}

Количество задач в очереди или выполняющихся в пуле потоков при `remote_filesystem_read_method=threadpool`.

### ThreadsInOvercommitTracker \{#threadsinovercommittracker\}

Количество потоков, ожидающих в OvercommitTracker

### TotalTemporaryFiles \{#totaltemporaryfiles\}

Количество созданных временных файлов

### UncompressedCacheBytes \{#uncompressedcachebytes\}

Общий размер несжатого кэша в байтах. Несжатый кэш обычно не повышает производительность, поэтому по возможности его следует избегать.

### UncompressedCacheCells \{#uncompressedcachecells\}

Общее количество записей в несжатом кэше. Каждая запись представляет собой распакованный блок данных. Несжатый кэш обычно не повышает производительность, поэтому по возможности его следует избегать

### VectorSimilarityIndexCacheBytes \{#vectorsimilarityindexcachebytes\}

Размер кэша индекса векторного сходства в байтах

### VectorSimilarityIndexCacheCells \{#vectorsimilarityindexcachecells\}

Количество записей в кэше индекса векторного сходства

### VersionInteger \{#versioninteger\}

Версия сервера в виде одного целого числа в системе счисления с основанием 1000. Например, версия 11.22.33 преобразуется в 11022033.

### Записать \{#write\}

Количество системных вызовов записи (`write`, `pwrite`, `io_getevents` и т. д.), выполняющихся в данный момент

### ZooKeeperConnectionLossStartedTimestampSeconds \{#zookeeperconnectionlossstartedtimestampseconds\}

Unix-метка времени в секундах на момент потери соединения с ZooKeeper, или 0, если соединение установлено успешно.

### ZooKeeperRequest \{#zookeeperrequest\}

Количество запросов к ZooKeeper в обработке.

### ZooKeeperSession \{#zookeepersession\}

Количество сессий (подключений) к ZooKeeper. Должно быть не более одной, поскольку использование более одного подключения к ZooKeeper может приводить к ошибкам из-за отсутствия линеаризуемости (устаревшие чтения), которое допускает модель согласованности ZooKeeper.

### ZooKeeperSessionExpired \{#zookeepersessionexpired\}

Количество глобальных сессий ZooKeeper с истекшим сроком действия.

### ZooKeeperWatch \{#zookeeperwatch\}

Количество отслеживаний (подписок на события) в ZooKeeper.

{/*AUTOGENERATED_DESCRIPTIONS_END*/ }

**См. также**

* [system.asynchronous&#95;metrics](/operations/system-tables/asynchronous_metrics) — Содержит периодически вычисляемые метрики.
* [system.events](/operations/system-tables/events) — Содержит сведения о ряде произошедших событий.
* [system.metric&#95;log](/operations/system-tables/metric_log) — Содержит историю значений метрик из таблиц `system.metrics` и `system.events`.
* [Мониторинг](../../operations/monitoring.md) — Основные принципы мониторинга ClickHouse.
