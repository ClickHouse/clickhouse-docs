---
description: 'Системная таблица, содержащая единственную строку с единственным столбцом `dummy` типа UInt8 со значением 0. Аналогична таблице `DUAL`, используемой в других СУБД.'
keywords: ['системная таблица', 'one']
slug: /operations/system-tables/one
title: 'system.one'
doc_type: 'reference'
---

# system.one

Эта таблица содержит одну строку с одним столбцом `dummy` типа UInt8 со значением 0.

Эта таблица используется, если в запросе `SELECT` не указано предложение `FROM`.

Это аналог таблицы `DUAL`, используемой в других СУБД.

**Пример**

```sql
SELECT * FROM system.one LIMIT 10;
```

```response
┌─dummy─┐
│     0 │
└───────┘

Получена 1 строка. Прошло: 0.001 сек.
```
