---
slug: /use-cases/observability/clickstack/sdks/elixir
pagination_prev: null
pagination_next: null
sidebar_position: 1
description: 'Elixir SDK для ClickStack — стек наблюдаемости ClickHouse'
title: 'Elixir'
doc_type: 'guide'
keywords: ['Elixir ClickStack SDK', 'наблюдаемость в Elixir', 'HyperDX Elixir', 'Elixir SDK для логирования', 'интеграция Elixir с ClickStack']
---

<table>
  <tbody>
    <tr>
      <td className="pe-2">✅ логи</td>
      <td className="pe-2">✖️ метрики</td>
      <td className="pe-2">✖️ трейсы</td>
    </tr>
  </tbody>
</table>
_🚧 Инструментация OpenTelemetry для метрик и трассировки скоро появится!_



## Начало работы

### Установка пакета ClickStack для backend-логгера

Пакет можно установить, добавив `hyperdx` в список зависимостей в файле
`mix.exs`:

```elixir
def deps do
  [
    {:hyperdx, "~> 0.1.6"}
  ]
end
```

### Настройте логгер

Добавьте следующее в файл `config.exs`:


```elixir
# config/releases.exs

config :logger,
  level: :info,
  backends: [:console, {Hyperdx.Backend, :hyperdx}]
```

### Настройте переменные окружения

Далее вам нужно будет настроить следующие переменные окружения в вашей командной оболочке, чтобы отправлять телеметрию в ClickStack:

```shell
export HYPERDX_API_KEY='<ВАШ_API_КЛЮЧ_ДЛЯ_ПРИЕМА_ДАННЫХ>' \
OTEL_SERVICE_NAME='<НАЗВАНИЕ_ВАШЕГО_ПРИЛОЖЕНИЯ_ИЛИ_СЕРВИСА>'
```

*Переменная окружения `OTEL_SERVICE_NAME` используется для идентификации вашего сервиса
в приложении HyperDX. Вы можете задать для неё любое имя.*
