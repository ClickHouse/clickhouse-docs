---
slug: /clickhouse-operator/install/helm
title: 'Установка оператора ClickHouse с помощью Helm'
keywords: ['kubernetes']
description: 'В этом руководстве описана установка оператора ClickHouse с использованием Helm-чартов.'
doc_type: 'guide'
sidebar_label: 'Helm'
---

В этом руководстве описана установка оператора ClickHouse с использованием Helm-чартов.

## Предварительные требования \{#prerequisites\}

- Кластер Kubernetes версии v1.33.0 или новее
- Helm версии v3.0 или новее
- kubectl, настроенный для взаимодействия с вашим кластером

## Установите Helm \{#install-helm\}

Если у вас не установлен Helm:

```bash
curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
```

Проверьте установку:

```bash
helm version
```


## Установите оператор \{#install-the-operator\}

:::note
По умолчанию Helm-чарт разворачивает ClickHouse Operator с включёнными вебхуками и требует предварительной установки cert-manager.
:::

### Из OCI-репозитория Helm \{#from-oci-helm-repository\}

Установите последнюю версию

```bash
    helm install clickhouse-operator oci://ghcr.io/clickhouse/clickhouse-operator-helm \
       --create-namespace \
       -n clickhouse-operator-system
```

Установите конкретную версию оператора

```bash
    helm install clickhouse-operator oci://ghcr.io/clickhouse/clickhouse-operator-helm \
       --create-namespace \
       -n clickhouse-operator-system \
       --set-json="manager.container.tag=<operator version>
```


### Из локального чарта \{#from-local-chart\}

Клонируйте репозиторий и выполните установку из локального чарта:

```bash
git clone https://github.com/ClickHouse/clickhouse-operator.git
cd clickhouse-operator
helm install clickhouse-operator ./dist/chart
```


### Параметры конфигурации \{#configuration-options\}

Для расширенных параметров конфигурации см. файл [values.yaml](https://github.com/ClickHouse/clickhouse-operator/blob/main/dist/chart/values.yaml) в файле Helm-чарта