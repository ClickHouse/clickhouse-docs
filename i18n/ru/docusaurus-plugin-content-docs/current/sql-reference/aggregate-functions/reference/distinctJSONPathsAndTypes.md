---
description: 'Вычисляет список различных путей и соответствующих им типов, содержащихся в JSON'
slug: /sql-reference/aggregate-functions/reference/distinctJSONPathsAndTypes
title: 'distinctJSONPathsAndTypes'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## distinctJSONPathsAndTypes \{#distinctJSONPathsAndTypes\}

Введено в версии: v24.9

Вычисляет список уникальных путей и их типов данных, хранящихся в столбце [JSON](https://clickhouse.com/docs/sql-reference/data-types/newjson).

:::note
Если декларация JSON содержит пути с указанными типами, эти пути всегда будут включены в результат функций `distinctJSONPaths/distinctJSONPathsAndTypes`, даже если во входных данных отсутствовали значения для этих путей.
:::

**Синтаксис**

```sql
distinctJSONPathsAndTypes(json)
```

**Аргументы**

* `json` — столбец JSON. [`JSON`](/sql-reference/data-types/newjson)

**Возвращаемое значение**

Возвращает отсортированное отображение путей и типов. [`Map(String, Array(String))`](/sql-reference/data-types/map)

**Примеры**

**Базовое использование со смешанными типами**

```sql title=Query
DROP TABLE IF EXISTS test_json;
CREATE TABLE test_json(json JSON) ENGINE = Memory;
INSERT INTO test_json VALUES ('{"a" : 42, "b" : "Hello"}'), ('{"b" : [1, 2, 3], "c" : {"d" : {"e" : "2020-01-01"}}}'), ('{"a" : 43, "c" : {"d" : {"f" : [{"g" : 42}]}}}');

SELECT distinctJSONPathsAndTypes(json) FROM test_json;
```

```response title=Response
┌─distinctJSONPathsAndTypes(json)───────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ {'a':['Int64'],'b':['Array(Nullable(Int64))','String'],'c.d.e':['Date'],'c.d.f':['Array(JSON(max_dynamic_types=16, max_dynamic_paths=256))']} │
└───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

**С объявлёнными JSON-путями**

```sql title=Query
DROP TABLE IF EXISTS test_json;
CREATE TABLE test_json(json JSON(a UInt32)) ENGINE = Memory;
INSERT INTO test_json VALUES ('{"b" : "Hello"}'), ('{"b" : "World", "c" : [1, 2, 3]}');

SELECT distinctJSONPathsAndTypes(json) FROM test_json;
```

```response title=Response
┌─distinctJSONPathsAndTypes(json)────────────────────────────────┐
│ {'a':['UInt32'],'b':['String'],'c':['Array(Nullable(Int64))']} │
└────────────────────────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/ }
