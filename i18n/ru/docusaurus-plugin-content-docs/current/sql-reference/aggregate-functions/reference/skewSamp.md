---
description: 'Вычисляет выборочный коэффициент асимметрии последовательности.'
slug: /sql-reference/aggregate-functions/reference/skewsamp
title: 'skewSamp'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## skewSamp \{#skewSamp\}

Добавлена в версии v20.1

Вычисляет [выборочную асимметрию](https://en.wikipedia.org/wiki/Skewness) последовательности.

Представляет собой несмещённую оценку асимметрии случайной величины, если переданные значения образуют выборку этой величины.

**Синтаксис**

```sql
skewSamp(expr)
```

**Аргументы**

* `expr` — выражение, возвращающее число. [`Expression`](/sql-reference/data-types/special-data-types/expression)

**Возвращаемое значение**

Возвращает коэффициент асимметрии заданного распределения. Если `n <= 1` (`n` — размер выборки), функция возвращает `nan`. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Симметричное распределение**

```sql title=Query
SELECT skewSamp(number) FROM numbers(100);
```

```response title=Response
┌─skewSamp(number)─┐
│                0 │
└──────────────────┘
```

**Распределение с правосторонней (положительной) асимметрией**

```sql title=Query
SELECT skewSamp(x) FROM (SELECT pow(number, 2) AS x FROM numbers(10));
```

```response title=Response
┌────────skewSamp(x)─┐
│ 0.5751042382747413 │
└────────────────────┘
```

{/*AUTOGENERATED_END*/ }
