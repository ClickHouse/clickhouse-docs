---
description: 'Применяет t-критерий Стьюдента к выборкам из двух генеральных совокупностей.'
sidebar_label: 'studentTTest'
slug: /sql-reference/aggregate-functions/reference/studentttest
title: 'studentTTest'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## studentTTest \{#studentTTest\}

Впервые появилась в версии v21.1

Применяет t-критерий Стьюдента к выборкам из двух генеральных совокупностей.

Значения обеих выборок находятся в столбце `sample_data`. Если `sample_index` равен 0, то значение в этой строке относится к выборке из первой генеральной совокупности. В противном случае оно относится к выборке из второй генеральной совокупности.
Нулевая гипотеза: математические ожидания совокупностей равны. Предполагается нормальное распределение с равными дисперсиями.

**См. также**

* [t-критерий Стьюдента](https://en.wikipedia.org/wiki/Student%27s_t-test)
* [функция welchTTest](/sql-reference/aggregate-functions/reference/welchttest)

**Синтаксис**

```sql
studentTTest([confidence_level])(sample_data, sample_index)
```

**Параметры**

* `confidence_level` — необязательный параметр. Уровень доверия для вычисления доверительных интервалов. [`Float`](/sql-reference/data-types/float)

**Аргументы**

* `sample_data` — данные выборки. [`Integer`](/sql-reference/data-types/int-uint) или [`Float`](/sql-reference/data-types/float) или [`Decimal`](/sql-reference/data-types/decimal)
* `sample_index` — индекс выборки. [`Integer`](/sql-reference/data-types/int-uint)

**Возвращаемое значение**

Возвращает кортеж из двух или четырёх элементов (если указан необязательный `confidence_level`): рассчитанная t-статистика, рассчитанное p-значение, [нижняя граница доверительного интервала], [верхняя граница доверительного интервала]. [`Tuple(Float64, Float64)`](/sql-reference/data-types/tuple) или [`Tuple(Float64, Float64, Float64, Float64)`](/sql-reference/data-types/tuple)

**Примеры**

**Базовое использование**

```sql title=Query
SELECT studentTTest(sample_data, sample_index) FROM student_ttest;
```

```response title=Response
┌─studentTTest(sample_data, sample_index)───┐
│ (-0.21739130434783777,0.8385421208415731) │
└───────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/ }

**См. также**

* [t-критерий Стьюдента](https://en.wikipedia.org/wiki/Student%27s_t-test)
* [функция welchTTest](/sql-reference/aggregate-functions/reference/welchttest)
