---
description: 'Результат равен квадратному корню от varSamp. В отличие от неё, эта функция использует численно устойчивый алгоритм.'
slug: /sql-reference/aggregate-functions/reference/stddevsampstable
title: 'stddevSampStable'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## stddevSampStable \{#stddevSampStable\}

Впервые появилась в версии: v1.1

Результат равен квадратному корню из [varSamp](../../../sql-reference/aggregate-functions/reference/varSamp.md). В отличие от [stddevSamp](../reference/stddevSamp.md) эта функция использует численно устойчивый алгоритм. Она работает медленнее, но обеспечивает меньшую вычислительную погрешность.

**Синтаксис**

```sql
stddevSampStable(x)
```

**Аргументы**

* `x` — значения, для которых требуется вычислить квадратный корень выборочной дисперсии. [`(U)Int*`](/sql-reference/data-types/int-uint) или [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает квадратный корень выборочной дисперсии от `x`. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Базовое использование**

```sql title=Query
DROP TABLE IF EXISTS test_data;
CREATE TABLE test_data
(
    population UInt8,
)
ENGINE = Log;

INSERT INTO test_data VALUES (3),(3),(3),(4),(4),(5),(5),(7),(11),(15);

SELECT
    stddevSampStable(population)
FROM test_data;
```

```response title=Response
┌─stddevSampStable(population)─┐
│                            4 │
└──────────────────────────────┘
```

{/*AUTOGENERATED_END*/ }
