---
description: 'Результат равен квадратному корню из результата varSamp'
slug: /sql-reference/aggregate-functions/reference/stddevsamp
title: 'stddevSamp'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## stddevSamp \{#stddevSamp\}

Введена в версии: v1.1

Возвращает выборочное стандартное отклонение последовательности числовых данных.
Результат равен квадратному корню из [`varSamp`](/sql-reference/aggregate-functions/reference/varSamp).

:::note
Эта функция использует численно неустойчивый алгоритм.
Если вам нужна [численная устойчивость](https://en.wikipedia.org/wiki/Numerical_stability) вычислений, используйте функцию [`stddevSampStable`](/sql-reference/aggregate-functions/reference/stddevsampstable). Она работает медленнее, но обеспечивает меньшую вычислительную погрешность.
:::

**Синтаксис**

```sql
stddevSamp(x)
```

**Псевдонимы**: `STDDEV_SAMP`

**Аргументы**

* `x` — значения, для которых нужно вычислить квадратный корень выборочной дисперсии. [`(U)Int*`](/sql-reference/data-types/int-uint) или [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает квадратный корень выборочной дисперсии для `x`. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Вычисление выборочного стандартного отклонения**

```sql title=Query
DROP TABLE IF EXISTS test_data;
CREATE TABLE test_data
(
    population UInt8,
)
ENGINE = Log;

INSERT INTO test_data VALUES (3),(3),(3),(4),(4),(5),(5),(7),(11),(15);

SELECT
    stddevSamp(population)
FROM test_data;
```

```response title=Response
┌─stddevSamp(population)─┐
│                      4 │
└────────────────────────┘
```

{/*AUTOGENERATED_END*/ }
