---
description: 'Применяет побитовую операцию `AND` к последовательности чисел.'
slug: /sql-reference/aggregate-functions/reference/groupbitand
title: 'groupBitAnd'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## groupBitAnd \{#groupBitAnd\}

Введена в версии: v1.1

Применяет операцию побитового И к последовательности чисел.

**Синтаксис**

```sql
groupBitAnd(expr)
```

**Псевдонимы**: `BIT_AND`

**Аргументы**

* `expr` — выражение типа `(U)Int*`. [`(U)Int*`](/sql-reference/data-types/int-uint)

**Возвращаемое значение**

Возвращает значение типа `(U)Int*`. [`(U)Int*`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример операции побитового AND**

```sql title=Query
CREATE TABLE t (num UInt32) ENGINE = Memory;
INSERT INTO t VALUES (44), (28), (13), (85);

-- Test data:
-- binary     decimal
-- 00101100 = 44
-- 00011100 = 28
-- 00001101 = 13
-- 01010101 = 85

SELECT groupBitAnd(num) FROM t;
```

```response title=Response
-- Result:
-- binary     decimal
-- 00000100 = 4

┌─groupBitAnd(num)─┐
│                4 │
└──────────────────┘
```

{/*AUTOGENERATED_END*/ }
