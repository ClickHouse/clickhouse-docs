---
description: 'Побитовые или агрегатные вычисления для беззнакового целочисленного столбца; возвращают кардинальность множества типа UInt64. При добавлении суффикса -State возвращается объект bitmap'
slug: /sql-reference/aggregate-functions/reference/groupbitmap
title: 'groupBitmap'
doc_type: 'reference'
---

# groupBitmap \{#groupbitmap\}

Bitmap- или агрегатные вычисления по беззнаковому целочисленному столбцу возвращают кардинальность множества в виде значения типа UInt64, а при добавлении суффикса `-State` возвращают [объект bitmap](../../../sql-reference/functions/bitmap-functions.md).

```sql
groupBitmap(expr)
```

**Аргументы**

`expr` – выражение, результат которого имеет тип `UInt*`.

**Возвращаемое значение**

Значение типа `UInt64`.

**Пример**

Тестовые данные:

```text
UserID
1
1
2
3
```

Запрос:

```sql
SELECT groupBitmap(UserID) AS num FROM t
```

Результат:

```text
num
3
```

{/*AUTOGENERATED_START*/ }

{/*AUTOGENERATED_END*/ }
