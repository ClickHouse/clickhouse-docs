---
description: 'Вычисляет сумму чисел и одновременно подсчитывает количество строк. Функция используется оптимизатором запросов ClickHouse: если в запросе есть несколько функций `sum`, `count` или `avg`, их можно заменить одной функцией `sumCount`, чтобы переиспользовать результаты вычислений. Необходимость в её явном использовании возникает редко.'
slug: /sql-reference/aggregate-functions/reference/sumcount
title: 'sumCount'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## sumCount \{#sumCount\}

Появилась в версии: v21.6

Вычисляет сумму чисел и одновременно подсчитывает количество строк. Функция используется оптимизатором запросов ClickHouse: если в запросе есть несколько функций `sum`, `count` или `avg`, их можно заменить одной функцией `sumCount`, чтобы переиспользовать результаты вычислений. Необходимость в её явном использовании возникает редко.

**См. также**

* [`optimize_syntax_fuse_functions`](../../../operations/settings/settings.md#optimize_syntax_fuse_functions) настройка.

**Синтаксис**

```sql
sumCount(x)
```

**Аргументы**

* `x` — входное значение. [`(U)Int*`](/sql-reference/data-types/int-uint) или [`Float`](/sql-reference/data-types/float) или [`Decimal`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает кортеж `(sum, count)`, где `sum` — сумма чисел, а `count` — количество строк с не-NULL значениями. [`Tuple`](/sql-reference/data-types/tuple)

**Примеры**

**Базовое использование**

```sql title=Query
CREATE TABLE s_table (x Int8) ENGINE = Log;
INSERT INTO s_table SELECT number FROM numbers(0, 20);
INSERT INTO s_table VALUES (NULL);
SELECT sumCount(x) FROM s_table;
```

```response title=Response
┌─sumCount(x)─┐
│ (190,20)    │
└─────────────┘
```

{/*AUTOGENERATED_END*/ }

**См. также**

* настройку [optimize&#95;syntax&#95;fuse&#95;functions](../../../operations/settings/settings.md#optimize_syntax_fuse_functions).
