---
description: 'Возвращает пересечение указанных массивов (все элементы этих массивов,
  которые присутствуют во всех из них).'
slug: /sql-reference/aggregate-functions/reference/grouparrayintersect
title: 'groupArrayIntersect'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## groupArrayIntersect \{#groupArrayIntersect\}

Появилась в версии v24.2

Возвращает пересечение заданных массивов (все элементы, которые присутствуют во всех этих массивах).

**Синтаксис**

```sql
groupArrayIntersect(x)
```

**Аргументы**

* `x` — аргумент (имя столбца или выражение). [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает массив, содержащий элементы, присутствующие во всех массивах. [`Array`](/sql-reference/data-types/array)

**Примеры**

**Пример использования**

```sql title=Query
-- Create table with Memory engine
CREATE TABLE numbers (
    a Array(Int32)
) ENGINE = Memory;

-- Insert sample data
INSERT INTO numbers VALUES
    ([1,2,4]),
    ([1,5,2,8,-1,0]),
    ([1,5,7,5,8,2]);

SELECT groupArrayIntersect(a) AS intersection FROM numbers;
```

```response title=Response
┌─intersection──────┐
│ [1, 2]            │
└───────────────────┘
```

{/*AUTOGENERATED_END*/ }
