---
description: 'Функция `theilsU` вычисляет коэффициент неопределённости Тейлса U (Theils'' U),
  значение, которое показывает степень связи между двумя столбцами в таблице.'
slug: /sql-reference/aggregate-functions/reference/theilsu
title: 'theilsU'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## theilsU \{#theilsU\}

Введена в версии: v22.1

Функция `theilsU` вычисляет [коэффициент неопределённости U Тейла](https://en.wikipedia.org/wiki/Contingency_table#Uncertainty_coefficient) — величину, которая измеряет степень связи между двумя столбцами в таблице.
Её значения лежат в диапазоне от −1.0 (100% отрицательной связи, или полная инверсия) до +1.0 (100% положительной связи, или полное соответствие).
Значение 0.0 указывает на отсутствие связи.

**Синтаксис**

```sql
theilsU(column1, column2)
```

**Аргументы**

* `column1` — первый столбец для сравнения. [`Any`](/sql-reference/data-types)
* `column2` — второй столбец для сравнения. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает значение в диапазоне от -1 до 1. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Query
SELECT theilsU(a, b)
FROM (
    SELECT
        number % 10 AS a,
        number % 4 AS b
    FROM
        numbers(150)
);
```

```response title=Response
┌────────theilsU(a, b)─┐
│ -0.30195720557678846 │
└──────────────────────┘
```

{/*AUTOGENERATED_END*/ }
