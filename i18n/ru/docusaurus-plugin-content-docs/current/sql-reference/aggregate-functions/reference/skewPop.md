---
description: 'Вычисляет коэффициент асимметрии последовательности.'
slug: /sql-reference/aggregate-functions/reference/skewpop
title: 'skewPop'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## skewPop \{#skewPop\}

Введено в версии: v20.1

Вычисляет [асимметрию распределения](https://en.wikipedia.org/wiki/Skewness) последовательности значений.

**Синтаксис**

```sql
skewPop(expr)
```

**Аргументы**

* `expr` — выражение, возвращающее число. [`Expression`](/sql-reference/data-types/special-data-types/expression)

**Возвращаемое значение**

Возвращает коэффициент асимметрии заданного распределения. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Симметричное распределение**

```sql title=Query
SELECT skewPop(number) FROM numbers(100);
```

```response title=Response
┌─skewPop(number)─┐
│               0 │
└─────────────────┘
```

**Распределение с правосторонней асимметрией**

```sql title=Query
SELECT skewPop(x) FROM (SELECT pow(number, 2) AS x FROM numbers(10));
```

```response title=Response
┌─────────skewPop(x)─┐
│ 0.6735701055423582 │
└────────────────────┘
```

{/*AUTOGENERATED_END*/ }
