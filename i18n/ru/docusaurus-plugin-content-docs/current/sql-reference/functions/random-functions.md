---
description: 'Документация по функциям для генерации случайных чисел'
sidebar_label: 'Случайные числа'
slug: /sql-reference/functions/random-functions
title: 'Функции для генерации случайных чисел'
doc_type: 'reference'
---



# Функции для генерации случайных чисел

Все функции в этом разделе принимают ноль или один аргумент. Единственное назначение аргумента (если он указан) — предотвратить [устранение общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination), чтобы два различных вызова одной и той же функции генерации случайных чисел в пределах одной строки возвращали разные случайные значения.

Связанные материалы

- Руководство: [Генерация случайных данных в ClickHouse](/guides/generating-test-data)
- Блог: [Генерация случайных данных в ClickHouse](https://clickhouse.com/blog/generating-random-test-distribution-data-for-clickhouse)

:::note
Случайные числа генерируются с помощью некриптографических алгоритмов.
:::

:::note
Документация ниже автоматически генерируется из системной таблицы `system.functions`.
:::

<!--
The inner content of the tags below are replaced at doc framework build time with
docs generated from system.functions. Please do not modify or remove the tags.
See: https://github.com/ClickHouse/clickhouse-docs/blob/main/contribute/autogenerated-documentation-from-source.md
-->


<!--AUTOGENERATED_START-->

## fuzzBits {#fuzzBits}

Введена в версии: v20.5

Инвертирует биты входной строки `s` с вероятностью `p` для каждого бита.

**Синтаксис**

```sql
fuzzBits(s, p)
```

**Аргументы**

- `s` — строка типа String или FixedString, для которой выполняется инвертирование битов. [`String`](/sql-reference/data-types/string) или [`FixedString`](/sql-reference/data-types/fixedstring)
- `p` — вероятность инвертирования каждого бита, число в диапазоне от `0.0` до `1.0`. [`Float*`](/sql-reference/data-types/float)

**Возвращаемое значение**

Возвращает строку с инвертированными битами того же типа, что и `s`. [`String`](/sql-reference/data-types/string) или [`FixedString`](/sql-reference/data-types/fixedstring)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT fuzzBits(materialize('abacaba'), 0.1)
FROM numbers(3)
```

```response title=Результат
┌─fuzzBits(materialize('abacaba'), 0.1)─┐
│ abaaaja                               │
│ a*cjab+                               │
│ aeca2A                                │
└───────────────────────────────────────┘
```


## rand {#rand}

Введено в версии: v1.1

Возвращает случайное число типа `UInt32` с равномерным распределением.

Использует линейный конгруэнтный генератор с начальным состоянием, полученным от системы. Это означает, что хотя результат выглядит случайным, он не является истинно случайным и может быть предсказуемым при известном начальном состоянии.
Для сценариев, где требуется истинная случайность, рекомендуется использовать альтернативные методы, такие как системные вызовы или интеграцию с внешними библиотеками.

**Синтаксис**

```sql
rand([x])
```

**Псевдонимы**: `rand32`

**Аргументы**

- `x` — Необязательный и игнорируемый аргумент. Единственная цель аргумента — предотвратить [устранение общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination) при многократном использовании одного и того же вызова функции в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает случайное число типа `UInt32`. [`UInt32`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT rand();
```

```response title=Результат
1569354847
```


## rand64 {#rand64}

Введена в версии: v1.1

Возвращает псевдослучайное число типа `UInt64` с равномерным распределением.

Использует линейный конгруэнтный генератор с начальным состоянием, полученным от системы. Это означает, что хотя результат выглядит случайным, он не является истинно случайным и может быть предсказуемым при известном начальном состоянии.
Для сценариев, где критически важна истинная случайность, рекомендуется использовать альтернативные методы, такие как системные вызовы или интеграцию с внешними библиотеками.

**Синтаксис**

```sql
rand64([x])
```

**Аргументы**

- `x` — Необязательный и игнорируемый аргумент. Единственная цель аргумента — предотвратить [устранение общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination), когда один и тот же вызов функции используется несколько раз в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает псевдослучайное число типа UInt64 с равномерным распределением. [`UInt64`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Query
SELECT rand64();
```

```response title=Response
15030268859237645412
```


## randBernoulli {#randBernoulli}

Введена в версии: v22.10

Возвращает случайное число типа Float64 из [распределения Бернулли](https://en.wikipedia.org/wiki/Bernoulli_distribution).

**Синтаксис**

```sql
randBernoulli(probability[, x])
```

**Аргументы**

- `probability` — Вероятность успеха, значение от `0` до `1`. [`Float64`](/sql-reference/data-types/float)
- `x` — Необязательный игнорируемый аргумент. Единственное назначение аргумента — предотвращение [устранения общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination) при многократном использовании одного и того же вызова функции в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Случайное число типа Float64 из указанного распределения Бернулли. [`UInt64`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT randBernoulli(.75) FROM numbers(5)
```

```response title=Результат
┌─randBernoulli(0.75)─┐
│                   1 │
│                   1 │
│                   0 │
│                   1 │
│                   1 │
└─────────────────────┘
```


## randBinomial {#randBinomial}

Введена в версии: v22.10

Возвращает случайное число типа Float64 из [биномиального распределения](https://en.wikipedia.org/wiki/Binomial_distribution).

**Синтаксис**

```sql
randBinomial(experiments, probability[, x])
```

**Аргументы**

- `experiments` — Число экспериментов [`UInt64`](/sql-reference/data-types/int-uint)
- `probability` — Вероятность успеха в каждом эксперименте, значение от `0` до `1` [`Float64`](/sql-reference/data-types/float)
- `x` — Необязательный игнорируемый аргумент. Единственная цель аргумента — предотвратить [устранение общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination) при многократном использовании одного и того же вызова функции в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает случайное число типа Float64 из указанного биномиального распределения. [`UInt64`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT randBinomial(100, .75) FROM numbers(5)
```

```response title=Результат
┌─randBinomial(100, 0.75)─┐
│                      74 │
│                      78 │
│                      76 │
│                      77 │
│                      80 │
└─────────────────────────┘
```


## randCanonical {#randCanonical}

Введена в версии: v22.11

Возвращает случайное число типа `Float64` с равномерным распределением в диапазоне от `0` (включительно) до `1` (не включительно).

**Синтаксис**

```sql
randCanonical([x])
```

**Аргументы**

- `x` — Необязательный аргумент, который игнорируется. Единственное назначение аргумента — предотвратить [устранение общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination) при многократном использовании одного и того же вызова функции в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Случайное число типа Float64. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT randCanonical();
```

```response title=Результат
0.345217890123456
```


## randChiSquared {#randChiSquared}

Введена в версии: v22.10

Возвращает случайное число типа Float64 из [распределения хи-квадрат](https://en.wikipedia.org/wiki/Chi-squared_distribution).

**Синтаксис**

```sql
randChiSquared(degree_of_freedom[, x])
```

**Аргументы**

- `degree_of_freedom` — Число степеней свободы. [`Float64`](/sql-reference/data-types/float)
- `x` — Необязательный игнорируемый аргумент. Единственное назначение этого аргумента — предотвращение [устранения общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination) при многократном использовании одного и того же вызова функции в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает случайное число типа Float64 из указанного распределения хи-квадрат. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT randChiSquared(10) FROM numbers(5)
```

```response title=Результат
┌─randChiSquared(10)─┐
│ 10.015463656521543 │
│  9.621799919882768 │
│   2.71785015634699 │
│ 11.128188665931908 │
│  4.902063104425469 │
└────────────────────┘
```


## randConstant {#randConstant}

Введена в версии: v1.1

Генерирует одно случайное значение, которое остается константным для всех строк в рамках текущего выполнения запроса.

Эта функция:

- Возвращает одно и то же случайное значение для каждой строки в пределах одного запроса
- Генерирует различные значения при разных выполнениях запросов

Полезна для применения единообразных случайных начальных значений или идентификаторов ко всем строкам набора данных.

**Синтаксис**

```sql
randConstant([x])
```

**Аргументы**

- `x` — Необязательный и игнорируемый аргумент. Единственная цель аргумента — предотвратить [устранение общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination), когда один и тот же вызов функции используется несколько раз в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает столбец типа `UInt32`, содержащий одно и то же случайное значение в каждой строке. [`UInt32`](/sql-reference/data-types/int-uint)

**Примеры**

**Базовое использование**

```sql title=Запрос
SELECT randConstant() AS random_value;
```

```response title=Ответ
| random_value |
|--------------|
| 1234567890   |
```

**Использование с параметром**

```sql title=Запрос
SELECT randConstant(10) AS random_value;
```

```response title=Ответ
| random_value |
|--------------|
| 9876543210   |
```


## randExponential {#randExponential}

Добавлено в версии: v22.10

Возвращает случайное число типа Float64, сгенерированное из [экспоненциального распределения](https://en.wikipedia.org/wiki/Exponential_distribution).

**Синтаксис**

```sql
randExponential(lambda[, x])
```

**Аргументы**

- `lambda` — параметр интенсивности (лямбда) распределения [`Float64`](/sql-reference/data-types/float)
- `x` — необязательный игнорируемый аргумент. Единственная цель этого аргумента — предотвратить [устранение общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination) при многократном использовании одного и того же вызова функции в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает случайное число типа Float64, сгенерированное из указанного экспоненциального распределения. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT randExponential(1/10) FROM numbers(5)
```

```response title=Результат
┌─randExponential(divide(1, 10))─┐
│              44.71628934340778 │
│              4.211013337903262 │
│             10.809402553207766 │
│              15.63959406553284 │
│             1.8148392319860158 │
└────────────────────────────────┘
```


## randFisherF {#randFisherF}

Добавлено в версии: v22.10

Возвращает случайное число типа Float64 из [F-распределения](https://en.wikipedia.org/wiki/F-distribution).

**Синтаксис**

```sql
randFisherF(d1, d2[, x])
```

**Аргументы**

- `d1` — число степеней свободы d1 в `X = (S1 / d1) / (S2 / d2)`. [`Float64`](/sql-reference/data-types/float)
- `d2` — число степеней свободы d2 в `X = (S1 / d1) / (S2 / d2)`. [`Float64`](/sql-reference/data-types/float)
- `x` — необязательный и игнорируемый аргумент. Единственная цель этого аргумента — предотвратить [устранение общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination) при многократном использовании одного и того же вызова функции в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Случайное число типа Float64 из указанного F-распределения. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT randFisherF(10, 3) FROM numbers(5)
```

```response title=Результат
┌─randFisherF(10, 20)─┐
│  0.7204609609506184 │
│  0.9926258472572916 │
│  1.4010752726735863 │
│ 0.34928401507025556 │
│  1.8216216009473598 │
└─────────────────────┘
```


## randLogNormal {#randLogNormal}

Введена в версии: v22.10

Возвращает случайное число типа Float64 из [логнормального распределения](https://en.wikipedia.org/wiki/Log-normal_distribution).

**Синтаксис**

```sql
randLogNormal(mean, stddev[, x])
```

**Аргументы**

- `mean` — Среднее значение распределения. [`Float64`](/sql-reference/data-types/float)
- `stddev` — Стандартное отклонение распределения. [`Float64`](/sql-reference/data-types/float)
- `x` — Необязательный игнорируемый аргумент. Единственная цель аргумента — предотвратить [устранение общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination) при многократном использовании одного и того же вызова функции в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает случайное число типа Float64 из указанного логнормального распределения. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Query
SELECT randLogNormal(100, 5) FROM numbers(5)
```

```response title=Response
┌─randLogNormal(100, 5)─┐
│  1.295699673937363e48 │
│  9.719869109186684e39 │
│  6.110868203189557e42 │
│  9.912675872925529e39 │
│ 2.3564708490552458e42 │
└───────────────────────┘
```


## randNegativeBinomial {#randNegativeBinomial}

Введена в версии: v22.10

Возвращает случайное число типа Float64 из [отрицательного биномиального распределения](https://en.wikipedia.org/wiki/Negative_binomial_distribution).

**Синтаксис**

```sql
randNegativeBinomial(experiments, probability[, x])
```

**Аргументы**

- `experiments` — Количество экспериментов. [`UInt64`](/sql-reference/data-types/int-uint)
- `probability` — Вероятность неудачи в каждом эксперименте, значение от `0` до `1`. [`Float64`](/sql-reference/data-types/float)
- `x` — Необязательный игнорируемый аргумент. Единственная цель аргумента — предотвратить [устранение общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination), когда один и тот же вызов функции используется несколько раз в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает случайное число типа Float64 из указанного отрицательного биномиального распределения. [`UInt64`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Query
SELECT randNegativeBinomial(100, .75) FROM numbers(5)
```

```response title=Response
┌─randNegativeBinomial(100, 0.75)─┐
│                              33 │
│                              32 │
│                              39 │
│                              40 │
│                              50 │
└─────────────────────────────────┘
```


## randNormal {#randNormal}

Добавлена в версии: v22.10

Возвращает случайное число типа Float64 из [нормального распределения](https://en.wikipedia.org/wiki/Normal_distribution).

**Синтаксис**

```sql
randNormal(mean, stddev[, x])
```

**Аргументы**

- `mean` — Среднее значение распределения [`Float64`](/sql-reference/data-types/float)
- `stddev` — Среднеквадратичное отклонение распределения [`Float64`](/sql-reference/data-types/float)
- `x` — Необязательный игнорируемый аргумент. Единственное назначение этого аргумента — предотвращение [устранения общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination) при многократном использовании одного и того же вызова функции в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Случайное число типа Float64 из указанного нормального распределения. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Query
SELECT randNormal(10, 2) FROM numbers(5)
```

```response title=Response
┌──randNormal(10, 2)─┐
│ 13.389228911709653 │
│  8.622949707401295 │
│ 10.801887062682981 │
│ 4.5220192605895315 │
│ 10.901239123982567 │
└────────────────────┘
```


## randPoisson {#randPoisson}

Введено в версии: v22.10

Возвращает случайное число типа Float64 из [распределения Пуассона](https://en.wikipedia.org/wiki/Poisson_distribution).

**Синтаксис**

```sql
randPoisson(n[, x])
```

**Аргументы**

- `n` — Среднее число событий. [`UInt64`](/sql-reference/data-types/int-uint)
- `x` — Необязательный игнорируемый аргумент. Единственная цель аргумента — предотвратить [устранение общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination) при многократном использовании одного и того же вызова функции в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает случайное число типа Float64 из указанного распределения Пуассона. [`UInt64`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT randPoisson(10) FROM numbers(5)
```

```response title=Результат
┌─randPoisson(10)─┐
│               8 │
│               8 │
│               7 │
│              10 │
│               6 │
└─────────────────┘
```


## randStudentT {#randStudentT}

Введена в версии: v22.10

Возвращает случайное число типа Float64 из [t-распределения Стьюдента](https://en.wikipedia.org/wiki/Student%27s_t-distribution).

**Синтаксис**

```sql
randStudentT(degree_of_freedom[, x])
```

**Аргументы**

- `degree_of_freedom` — Число степеней свободы. [`Float64`](/sql-reference/data-types/float)
- `x` — Необязательный и игнорируемый аргумент. Единственная цель аргумента — предотвратить [устранение общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination), когда один и тот же вызов функции используется в запросе несколько раз. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает случайное число типа Float64 из указанного t-распределения Стьюдента. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT randStudentT(10) FROM numbers(5)
```

```response title=Результат
┌─────randStudentT(10)─┐
│   1.2217309938538725 │
│   1.7941971681200541 │
│ -0.28192176076784664 │
│   0.2508897721303792 │
│  -2.7858432909761186 │
└──────────────────────┘
```


## randUniform {#randUniform}

Введена в версии: v22.10

Возвращает случайное число типа Float64, равномерно распределённое на интервале $[\min, \max]$.

**Синтаксис**

```sql
randUniform(min, max[, x])
```

**Аргументы**

- `min` — левая граница диапазона (включительно). [`Float64`](/sql-reference/data-types/float)
- `max` — правая граница диапазона (включительно). [`Float64`](/sql-reference/data-types/float)
- `x` — необязательный игнорируемый аргумент. Единственная цель этого аргумента — предотвратить [устранение общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination) при многократном использовании одного и того же вызова функции в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает случайное число, равномерно распределённое на интервале, образованном значениями `min` и `max`. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT randUniform(5.5, 10) FROM numbers(5)
```

```response title=Результат
┌─randUniform(5.5, 10)─┐
│    8.094978491443102 │
│   7.3181248914450885 │
│    7.177741903868262 │
│    6.483347380953762 │
│    6.122286382885112 │
└──────────────────────┘
```


## randomFixedString {#randomFixedString}

Введена в версии: v20.5

Генерирует случайную строку фиксированного размера с указанным количеством символов.
Возвращаемые символы не обязательно являются ASCII-символами, т.е. они могут быть непечатаемыми.

**Синтаксис**

```sql
randomFixedString(length)
```

**Аргументы**

- `length` — длина строки в байтах. [`UInt*`](/sql-reference/data-types/int-uint)

**Возвращаемое значение**

Возвращает строку, заполненную случайными байтами. [`FixedString`](/sql-reference/data-types/fixedstring)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT randomFixedString(13) AS rnd, toTypeName(rnd)
```

```response title=Результат
┌─rnd──────┬─toTypeName(randomFixedString(13))─┐
│ j▒h㋖HɨZ'▒ │ FixedString(13)                 │
└──────────┴───────────────────────────────────┘
```


## randomPrintableASCII {#randomPrintableASCII}

Введена в версии: v20.1

Генерирует случайную строку из печатаемых [ASCII](https://en.wikipedia.org/wiki/ASCII#Printable_characters) символов указанной длины.

Если передать `length < 0`, поведение функции не определено.

**Синтаксис**

```sql
randomPrintableASCII(length[, x])
```

**Аргументы**

- `length` — длина строки в байтах. [`(U)Int*`](/sql-reference/data-types/int-uint)
- `x` — необязательный и игнорируемый аргумент. Единственная цель аргумента — предотвратить [устранение общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination), когда один и тот же вызов функции используется несколько раз в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает строку со случайным набором печатаемых ASCII символов. [`String`](/sql-reference/data-types/string)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT number, randomPrintableASCII(30) AS str, length(str) FROM system.numbers LIMIT 3
```

```response title=Ответ
┌─number─┬─str────────────────────────────┬─length(randomPrintableASCII(30))─┐
│      0 │ SuiCOSTvC0csfABSw=UcSzp2.`rv8x │                               30 │
│      1 │ 1Ag NlJ &RCN:*>HVPG;PE-nO"SUFD │                               30 │
│      2 │ /"+<"with:=LjJ Vm!c&hI*m#XTfzz │                               30 │
└────────┴────────────────────────────────┴──────────────────────────────────┘
```


## randomString {#randomString}

Введена в версии: v20.5

Генерирует случайную строку заданной длины.
Возвращаемые символы не обязательно являются ASCII-символами, т.е. могут быть непечатаемыми.

**Синтаксис**

```sql
randomString(length[, x])
```

**Аргументы**

- `length` — Длина строки в байтах. [`(U)Int*`](/sql-reference/data-types/int-uint)
- `x` — Необязательный игнорируемый аргумент. Единственное назначение этого аргумента — предотвращение [устранения общих подвыражений](/sql-reference/functions/overview#common-subexpression-elimination) при многократном использовании одного и того же вызова функции в запросе. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает строку, заполненную случайными байтами. [`String`](/sql-reference/data-types/string)

**Примеры**

**Пример использования**

```sql title=Query
SELECT randomString(5) AS str FROM numbers(2)
```

```response title=Response
���
�v6B�
```


## randomStringUTF8 {#randomStringUTF8}

Введена в версии: v20.5

Генерирует случайную строку в кодировке [UTF-8](https://en.wikipedia.org/wiki/UTF-8) с заданным количеством кодовых точек.
Кодовые точки из неназначенных [плоскостей](<https://en.wikipedia.org/wiki/Plane_(Unicode)>) (плоскости с 4 по 13) не возвращаются.
Возможна ситуация, когда клиент, взаимодействующий с сервером ClickHouse, не сможет корректно отобразить полученную строку UTF-8.

**Синтаксис**

```sql
randomStringUTF8(length)
```

**Аргументы**

- `length` — длина строки в кодовых точках. [`(U)Int*`](/sql-reference/data-types/int-uint)

**Возвращаемое значение**

Возвращает строку, заполненную случайными кодовыми точками UTF-8. [`String`](/sql-reference/data-types/string)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT randomStringUTF8(13)
```

```response title=Результат
┌─randomStringUTF8(13)─┐
│ 𘤗𙉝д兠庇󡅴󱱎󦐪􂕌𔊹𓰛       │
└──────────────────────┘
```

<!--AUTOGENERATED_END-->
