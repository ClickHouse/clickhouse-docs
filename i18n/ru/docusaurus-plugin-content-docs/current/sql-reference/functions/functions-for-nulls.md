---
description: 'Документация по функциям для работы с Nullable-значениями'
sidebar_label: 'Nullable'
slug: /sql-reference/functions/functions-for-nulls
title: 'Функции для работы с Nullable-значениями'
keywords: ['nullable', 'functions']
doc_type: 'reference'
---



# Функции для работы с nullable-значениями

<!--
The inner content of the tags below are replaced at doc framework build time with
docs generated from system.functions. Please do not modify or remove the tags.
See: https://github.com/ClickHouse/clickhouse-docs/blob/main/contribute/autogenerated-documentation-from-source.md
-->


<!--AUTOGENERATED_START-->

## assumeNotNull {#assumeNotNull}

Introduced in: v1.1

Возвращает соответствующее значение типа, не допускающего `NULL`, для значения типа [`Nullable`](../data-types/nullable.md).
Если исходное значение равно `NULL`, может быть возвращён произвольный результат.

См. также функции [`ifNull`](#ifNull) и [`coalesce`](#coalesce).

**Синтаксис**

```sql
assumeNotNull(x)
```

**Аргументы**

- `x` — исходное значение любого типа, допускающего `NULL`. [`Nullable(T)`](/sql-reference/data-types/nullable)

**Возвращаемое значение**

Возвращает значение, не допускающее `NULL`, если исходное значение не было `NULL`, в противном случае — произвольное значение, если входное значение равно `NULL`. [`Any`](/sql-reference/data-types)

**Примеры**

**Пример использования**

```sql title=Query
CREATE TABLE t_null (x Int8, y Nullable(Int8))
ENGINE=MergeTree()
ORDER BY x;

INSERT INTO t_null VALUES (1, NULL), (2, 3);

SELECT assumeNotNull(y) FROM table;
SELECT toTypeName(assumeNotNull(y)) FROM t_null;
```

```response title=Response
┌─assumeNotNull(y)─┐
│                0 │
│                3 │
└──────────────────┘
┌─toTypeName(assumeNotNull(y))─┐
│ Int8                         │
│ Int8                         │
└──────────────────────────────┘
```


## coalesce {#coalesce}

Введена в версии: v1.1

Возвращает первый слева аргумент, отличный от `NULL`.

**Синтаксис**

```sql
coalesce(x[, y, ...])
```

**Аргументы**

- `x[, y, ...]` — любое количество параметров простого типа. Все параметры должны иметь совместимые типы данных. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает первый аргумент, отличный от `NULL`, или `NULL`, если все аргументы равны `NULL`. [`Any`](/sql-reference/data-types) или [`NULL`](/sql-reference/syntax#null)

**Примеры**

**Пример использования**

```sql title=Query
-- Рассмотрим список контактов, который может содержать несколько способов связи с клиентом.

CREATE TABLE aBook
(
    name String,
    mail Nullable(String),
    phone Nullable(String),
    telegram Nullable(UInt32)
)
ENGINE = MergeTree
ORDER BY tuple();

INSERT INTO aBook VALUES ('client 1', NULL, '123-45-67', 123), ('client 2', NULL, NULL, NULL);

-- Поля mail и phone имеют тип String, но поле telegram имеет тип UInt32, поэтому его необходимо преобразовать в String.

-- Получаем первый доступный способ связи с клиентом из списка контактов

SELECT name, coalesce(mail, phone, CAST(telegram,'Nullable(String)')) FROM aBook;
```

```response title=Response
┌─name─────┬─coalesce(mail, phone, CAST(telegram, 'Nullable(String)'))─┐
│ client 1 │ 123-45-67                                                 │
│ client 2 │ ᴺᵁᴸᴸ                                                      │
└──────────┴───────────────────────────────────────────────────────────┘
```


## firstNonDefault {#firstNonDefault}

Введена в версии: v25.9

Возвращает первое значение, отличное от значения по умолчанию, из набора аргументов

**Синтаксис**

```sql

```

**Аргументы**

- `arg1` — Первый проверяемый аргумент
- `arg2` — Второй проверяемый аргумент
- `...` — Дополнительные проверяемые аргументы

**Возвращаемое значение**

Тип результата является супертипом всех аргументов

**Примеры**

**целые числа**

```sql title=Запрос
SELECT firstNonDefault(0, 1, 2)
```

```response title=Результат
1
```

**строки**

```sql title=Запрос
SELECT firstNonDefault('', 'hello', 'world')
```

```response title=Результат
'hello'
```

**значения null**

```sql title=Запрос
SELECT firstNonDefault(NULL, 0 :: UInt8, 1 :: UInt8)
```

```response title=Результат
1
```

**nullable ноль**

```sql title=Запрос
SELECT firstNonDefault(NULL, 0 :: Nullable(UInt8), 1 :: Nullable(UInt8))
```

```response title=Результат
0
```


## ifNull {#ifNull}

Introduced in: v1.1

Возвращает альтернативное значение, если первый аргумент равен `NULL`.

**Синтаксис**

```sql
ifNull(x, alt)
```

**Аргументы**

- `x` — проверяемое значение. [`Any`](/sql-reference/data-types)
- `alt` — значение, возвращаемое функцией, если `x` равен `NULL`. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает значение `x`, если оно не равно `NULL`, иначе — `alt`. [`Any`](/sql-reference/data-types)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT ifNull('a', 'b'), ifNull(NULL, 'b');
```

```response title=Результат
┌─ifNull('a', 'b')─┬─ifNull(NULL, 'b')─┐
│ a                │ b                 │
└──────────────────┴───────────────────┘
```


## isNotNull {#isNotNull}

Введена в версии: v1.1

Проверяет, не является ли аргумент `NULL`.

См. также: оператор [`IS NOT NULL`](/sql-reference/operators#is_not_null).

**Синтаксис**

```sql
isNotNull(x)
```

**Аргументы**

- `x` — значение непроизводного типа данных. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает `1`, если `x` не является `NULL`, иначе `0`. [`UInt8`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Запрос
CREATE TABLE t_null
(
  x Int32,
  y Nullable(Int32)
)
ENGINE = MergeTree
ORDER BY tuple();

INSERT INTO t_null VALUES (1, NULL), (2, 3);

SELECT x FROM t_null WHERE isNotNull(y);
```

```response title=Результат
┌─x─┐
│ 2 │
└───┘
```


## isNull {#isNull}

Введена в версии: v1.1

Проверяет, является ли аргумент `NULL`.

См. также: оператор [`IS NULL`](/sql-reference/operators#is_null).

**Синтаксис**

```sql
isNull(x)
```

**Аргументы**

- `x` — значение несоставного типа данных. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает `1`, если `x` равен `NULL`, иначе `0`. [`UInt8`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Запрос
CREATE TABLE t_null
(
  x Int32,
  y Nullable(Int32)
)
ENGINE = MergeTree
ORDER BY tuple();

INSERT INTO t_null VALUES (1, NULL), (2, 3);

SELECT x FROM t_null WHERE isNull(y);
```

```response title=Результат
┌─x─┐
│ 1 │
└───┘
```


## isNullable {#isNullable}

Введена в версии: v22.7

Проверяет, является ли тип данных аргумента `Nullable` (т. е. допускает значения `NULL`).

**Синтаксис**

```sql
isNullable(x)
```

**Аргументы**

- `x` — значение любого типа данных. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает `1`, если `x` имеет тип данных `Nullable`, в противном случае `0`. [`UInt8`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Запрос
CREATE TABLE tab (
    ordinary_col UInt32,
    nullable_col Nullable(UInt32)
)
ENGINE = MergeTree
ORDER BY tuple();
INSERT INTO tab (ordinary_col, nullable_col) VALUES (1,1), (2, 2), (3,3);
SELECT isNullable(ordinary_col), isNullable(nullable_col) FROM tab;
```

```response title=Результат
┌───isNullable(ordinary_col)──┬───isNullable(nullable_col)──┐
│                           0 │                           1 │
│                           0 │                           1 │
│                           0 │                           1 │
└─────────────────────────────┴─────────────────────────────┘
```


## isZeroOrNull {#isZeroOrNull}

Введена в версии: v20.3

Проверяет, является ли аргумент нулём (`0`) или `NULL`.

**Синтаксис**

```sql
isZeroOrNull(x)
```

**Аргументы**

- `x` — числовое значение. [`UInt`](/sql-reference/data-types/int-uint)

**Возвращаемое значение**

Возвращает `1`, если `x` равен `NULL` или нулю, в противном случае — `0`. [`UInt8/16/32/64`](/sql-reference/data-types/int-uint) или [`Float32/Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
CREATE TABLE t_null
(
  x Int32,
  y Nullable(Int32)
)
ENGINE = MergeTree
ORDER BY tuple();

INSERT INTO t_null VALUES (1, NULL), (2, 0), (3, 3);

SELECT x FROM t_null WHERE isZeroOrNull(y);
```

```response title=Результат
┌─x─┐
│ 1 │
│ 2 │
└───┘
```


## nullIf {#nullIf}

Введена в версии: v1.1

Возвращает `NULL`, если оба аргумента равны.

**Синтаксис**

```sql
nullIf(x, y)
```

**Аргументы**

- `x` — первое значение. [`Any`](/sql-reference/data-types)
- `y` — второе значение. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает `NULL`, если оба аргумента равны, в противном случае возвращает первый аргумент. [`NULL`](/sql-reference/syntax#null) или [`Nullable(x)`](/sql-reference/data-types/nullable)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT nullIf(1, 1), nullIf(1, 2);
```

```response title=Результат
┌─nullIf(1, 1)─┬─nullIf(1, 2)─┐
│         ᴺᵁᴸᴸ │            1 │
└──────────────┴──────────────┘
```


## toNullable {#toNullable}

Введена в версии: v1.1

Преобразует тип переданного аргумента в `Nullable`.

**Синтаксис**

```sql
toNullable(x)
```

**Аргументы**

- `x` — значение любого непроизводного типа. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает входное значение с типом `Nullable`. [`Nullable(Any)`](/sql-reference/data-types/nullable)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT toTypeName(10), toTypeName(toNullable(10));
```

```response title=Результат
┌─toTypeName(10)─┬─toTypeName(toNullable(10))─┐
│ UInt8          │ Nullable(UInt8)            │
└────────────────┴────────────────────────────┘
```

<!--AUTOGENERATED_END-->
