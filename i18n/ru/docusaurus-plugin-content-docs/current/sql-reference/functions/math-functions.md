---
description: 'Документация по математическим функциям'
sidebar_label: 'Математические'
slug: /sql-reference/functions/math-functions
title: 'Математические функции'
doc_type: 'reference'
---



# Математические функции

<!--
The inner content of the tags below are replaced at doc framework build time with
docs generated from system.functions. Please do not modify or remove the tags.
See: https://github.com/ClickHouse/clickhouse-docs/blob/main/contribute/autogenerated-documentation-from-source.md
-->


<!--AUTOGENERATED_START-->

## acos {#acos}

Введена в версии: v1.1

Возвращает арккосинус аргумента.

**Синтаксис**

```sql
acos(x)
```

**Аргументы**

- `x` — Значение, для которого вычисляется арккосинус. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Арккосинус x. [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT acos(0.5);
```

```response title=Ответ
1.0471975511965979
```


## acosh {#acosh}

Введена в версии: v20.12

Возвращает обратный гиперболический косинус (ареакосинус гиперболический).

**Синтаксис**

```sql
acosh(x)
```

**Аргументы**

- `x` — гиперболический косинус угла. Значения из интервала: `1 ≤ x < +∞`. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает угол в радианах. Значения из интервала: `0 ≤ acosh(x) < +∞`. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT acosh(1)
```

```response title=Результат
0
```


## asin {#asin}

Введена в версии: v1.1

Вычисляет арксинус заданного аргумента.
Для аргументов в диапазоне `[-1, 1]` возвращает значение в диапазоне `[-pi() / 2, pi() / 2]`.

**Синтаксис**

```sql
asin(x)
```

**Аргументы**

- `x` — Аргумент, для которого вычисляется арксинус. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает значение арксинуса аргумента `x` [`Float64`](/sql-reference/data-types/float)

**Примеры**

**обратная функция**

```sql title=Запрос
SELECT asin(1.0) = pi() / 2, sin(asin(1)), asin(sin(1))
```

```response title=Результат
1 1 1
```

**float32**

```sql title=Запрос
SELECT toTypeName(asin(1.0::Float32))
```

```response title=Результат
Float64
```

**nan**

```sql title=Запрос
SELECT asin(1.1), asin(-2), asin(inf), asin(nan)
```

```response title=Результат
nan nan nan nan
```


## asinh {#asinh}

Введена в версии: v20.12

Возвращает обратный гиперболический синус (ареасинус гиперболический).

**Синтаксис**

```sql
asinh(x)
```

**Аргументы**

- `x` — значение, для которого вычисляется обратный гиперболический синус. Значения из интервала: `-∞ < x < +∞`. [`(U)Int*`](/sql-reference/data-types/int-uint) или [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает угол в радианах. Значения из интервала: `-∞ < asinh(x) < +∞`. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Базовое использование**

```sql title=Query
SELECT asinh(0)
```

```response title=Response
0
```


## atan {#atan}

Введена в версии: v1.1

Возвращает арктангенс аргумента.

**Синтаксис**

```sql
atan(x)
```

**Аргументы**

- `x` — Значение, для которого вычисляется арктангенс. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Арктангенс `x`. [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT atan(1);
```

```response title=Результат
0.7853981633974483
```


## atan2 {#atan2}

Введена в версии: v20.12

Возвращает значение функции atan2 — угол в евклидовой плоскости, выраженный в радианах, между положительным направлением оси x и лучом, проведённым к точке `(x, y) ≠ (0, 0)`.

**Синтаксис**

```sql
atan2(y, x)
```

**Аргументы**

- `y` — координата y точки, через которую проходит луч. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)
- `x` — координата x точки, через которую проходит луч. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает угол `θ`, для которого выполняется условие `-π < θ ≤ π`, в радианах. Тип: [`Float64`](/sql-reference/data-types/float).

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT atan2(1, 1)
```

```response title=Результат
0.7853981633974483
```


## atanh {#atanh}

Введена в версии: v20.12

Возвращает обратный гиперболический тангенс (ареатангенс).

**Синтаксис**

```sql
atanh(x)
```

**Аргументы**

- `x` — гиперболический тангенс угла. Значения из интервала: -1 < x < 1. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает угол в радианах. Значения из интервала: -∞ < atanh(x) < +∞. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT atanh(0)
```

```response title=Результат
0
```


## cbrt {#cbrt}

Введено в версии: v1.1

Возвращает кубический корень из аргумента.

**Синтаксис**

```sql
cbrt(x)
```

**Аргументы**

- `x` — Значение, для которого вычисляется кубический корень. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает кубический корень из `x`. [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT cbrt(8);
```

```response title=Результат
2
```


## cos {#cos}

Введена в версии: v1.1

Возвращает косинус аргумента.

**Синтаксис**

```sql
cos(x)
```

**Аргументы**

- `x` — Угол в радианах. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает косинус `x`. [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT cos(0);
```

```response title=Результат
1
```


## cosh {#cosh}

Введена в версии: v20.12

Возвращает гиперболический косинус аргумента.

**Синтаксис**

```sql
cosh(x)
```

**Аргументы**

- `x` — угол в радианах. Значения из интервала: `-∞ < x < +∞`. [`(U)Int*`](/sql-reference/data-types/int-uint) или [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает значения из интервала: `1 ≤ cosh(x) < +∞` [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Базовое использование**

```sql title=Запрос
SELECT cosh(0)
```

```response title=Результат
1
```


## degrees {#degrees}

Добавлено в версии: v22.2

Преобразует радианы в градусы.

**Синтаксис**

```sql
degrees(x)
```

**Аргументы**

- `x` — входное значение в радианах. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает значение `x` в градусах. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Базовое использование**

```sql title=Запрос
SELECT degrees(3.141592653589793)
```

```response title=Результат
180
```


## e {#e}

Введено в версии: v1.1

Возвращает число Эйлера (e).

**Синтаксис**

```sql
e()
```

**Аргументы**

- Отсутствуют.

**Возвращаемое значение**

Возвращает число Эйлера типа [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT e();
```

```response title=Результат
2.718281828459045
```


## erf {#erf}

Введено в версии: v1.1

Если `x` неотрицательно, то `erf(x/(σ√2))` представляет собой вероятность того, что случайная величина с нормальным распределением и стандартным отклонением `σ` примет значение, отличающееся от математического ожидания более чем на `x`.

**Синтаксис**

```sql
erf(x)
```

**Аргументы**

- `x` — значение, для которого вычисляется функция ошибок. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает значение функции ошибок. [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Правило трёх сигм**

```sql title=Запрос
SELECT erf(3 / sqrt(2))
```

```response title=Результат
┌─erf(divide(3, sqrt(2)))─┐
│      0.9973002039367398 │
└─────────────────────────┘
```


## erfc {#erfc}

Введено в версии: v1.1

Возвращает число, близкое к `1-erf(x)`, без потери точности для больших значений `x`.

**Синтаксис**

```sql
erfc(x)
```

**Аргументы**

- `x` — Значение, для которого вычисляется дополнительная функция ошибок. [`(U)Int*`](/sql-reference/data-types/int-uint) или [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает значение дополнительной функции ошибок [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT erfc(0);
```

```response title=Результат
1
```


## exp {#exp}

Введено в версии: v1.1

Возвращает число e, возведённое в степень `x`, где `x` — аргумент функции.

**Синтаксис**

```sql
exp(x)
```

**Аргументы**

- `x` — Показатель степени. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает `e^x` типа [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Базовое использование**

```sql title=Запрос
SELECT round(exp(-1), 4)
```

```response title=Результат
┌─round(exp(-1), 4)─┐
│            0.3679 │
└───────────────────┘
```


## exp10 {#exp10}

Введена в версии: v1.1

Возвращает 10 в степени заданного аргумента.

**Синтаксис**

```sql
exp10(x)
```

**Аргументы**

- `x` — Показатель степени. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает 10^x. [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT exp10(2);
```

```response title=Результат
100
```


## exp2 {#exp2}

Введена в версии: v1.1

Возвращает 2 в степени заданного аргумента.

**Синтаксис**

```sql
exp2(x)
```

**Аргументы**

- `x` — Показатель степени. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает 2^x. Тип: [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT exp2(3);
```

```response title=Результат
8
```


## factorial {#factorial}

Введена в версии: v22.11

Вычисляет факториал целого числа.
Факториал 0 равен 1. Аналогично функция `factorial()` возвращает `1` для любого отрицательного значения.
Максимальное положительное значение входного аргумента — `20`; значение `21` или больше вызовет исключение.

**Синтаксис**

```sql
factorial(n)
```

**Аргументы**

- `n` — целое число, для которого вычисляется факториал. Максимальное значение — 20. [`(U)Int8/16/32/64`](/sql-reference/data-types/int-uint)

**Возвращаемое значение**

Возвращает факториал входного значения типа UInt64. Возвращает 1 для входного значения 0 или любого отрицательного значения. [`UInt64`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Query
factorial(10)
```

```response title=Response
3628800
```


## hypot {#hypot}

Введена в версии: v20.12

Возвращает длину гипотенузы прямоугольного треугольника.
Функция hypot позволяет избежать проблем, возникающих при возведении в квадрат очень больших или очень малых чисел.

**Синтаксис**

```sql
hypot(x, y)
```

**Аргументы**

- `x` — первый катет прямоугольного треугольника. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)
- `y` — второй катет прямоугольного треугольника. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Длина гипотенузы прямоугольного треугольника. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Базовое использование**

```sql title=Запрос
SELECT hypot(1, 1)
```

```response title=Результат
1.4142135623730951
```


## intExp10 {#intExp10}

Введена в версии: v1.1

Аналогична функции [exp10](#exp10), но возвращает число типа `UInt64`.

**Синтаксис**

```sql
intExp10(x)
```

**Аргументы**

- `x` — показатель степени. [`Int*`](/sql-reference/data-types/int-uint), [`UInt*`](/sql-reference/data-types/int-uint) или [`Float*`](/sql-reference/data-types/float)

**Возвращаемое значение**

Возвращает 10^x. [`UInt64`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT intExp10(2);
```

```response title=Результат
100
```


## intExp2 {#intExp2}

Добавлена в версии: v1.1

Аналогична функции [exp2](#exp2), но возвращает число типа `UInt64`.

**Синтаксис**

```sql
intExp2(x)
```

**Аргументы**

- `x` — Показатель степени. [`Int*`](/sql-reference/data-types/int-uint), [`UInt*`](/sql-reference/data-types/int-uint) или [`Float*`](/sql-reference/data-types/float)

**Возвращаемое значение**

Возвращает 2^x. [`UInt64`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT intExp2(3);
```

```response title=Результат
8
```


## lgamma {#lgamma}

Добавлено в версии: v1.1

Возвращает логарифм гамма-функции.

**Синтаксис**

```sql
lgamma(x)
```

**Аргументы**

- `x` — Число, для которого вычисляется логарифм гамма-функции. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает логарифм гамма-функции от `x`. [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT lgamma(5);
```

```response title=Результат
3.1780538303479458
```


## log {#log}

Введена в версии: v1.1

Возвращает натуральный логарифм аргумента.

**Синтаксис**

```sql
log(x)
```

**Псевдонимы**: `ln`

**Аргументы**

- `x` — Число, для которого вычисляется натуральный логарифм. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Натуральный логарифм `x`. [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT log(10);
```

```response title=Результат
2.302585092994046
```


## log10 {#log10}

Введена в версии: v1.1

Возвращает десятичный логарифм аргумента.

**Синтаксис**

```sql
log10(x)
```

**Аргументы**

- `x` — Число, для которого вычисляется десятичный логарифм. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Десятичный логарифм `x`. [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT log10(100);
```

```response title=Результат
2
```


## log1p {#log1p}

Введена в версии: v20.12

Вычисляет log(1+x).
Вычисление log1p(x) более точное, чем log(1+x), для малых значений `x`.

**Синтаксис**

```sql
log1p(x)
```

**Аргументы**

- `x` — значения из интервала: `-1 < x < +∞`. [`(U)Int*`](/sql-reference/data-types/int-uint) или [`Float*`](/sql-reference/data-types/float), или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает значения из интервала: -∞ < log1p(x) < +∞ [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Query
SELECT log1p(0)
```

```response title=Response
0
```


## log2 {#log2}

Введена в версии: v1.1

Возвращает двоичный логарифм аргумента.

**Синтаксис**

```sql
log2(x)
```

**Аргументы**

- `x` — Число, для которого вычисляется двоичный логарифм. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Двоичный логарифм `x`. [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT log2(8);
```

```response title=Результат
3
```


## pi {#pi}

Введена в версии: v1.1

Возвращает число пи (π).

**Синтаксис**

```sql
pi()
```

**Аргументы**

- Нет.

**Возвращаемое значение**

Возвращает число пи типа [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT pi();
```

```response title=Результат
3.141592653589793
```


## pow {#pow}

Введена в версии: v1.1

Возвращает x, возведённое в степень y.

**Синтаксис**

```sql
pow(x, y)
```

**Псевдонимы**: `power`

**Аргументы**

- `x` — Основание. [`(U)Int8/16/32/64`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)
- `y` — Показатель степени. [`(U)Int8/16/32/64`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает x^y. Тип: [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Query
SELECT pow(2, 3);
```

```response title=Response
8
```


## radians {#radians}

Введена в версии: v22.2

Преобразует градусы в радианы.

**Синтаксис**

```sql
radians(x)
```

**Аргументы**

- `x` — входное значение в градусах. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает значение в радианах. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT radians(180)
```

```response title=Результат
3.141592653589793
```


## sign {#sign}

Введена в версии: v21.2

Возвращает знак действительного числа.

**Синтаксис**

```sql
sign(x)
```

**Аргументы**

- `x` — значения от -∞ до +∞. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Decimal*`](/sql-reference/data-types/decimal) или [`Float*`](/sql-reference/data-types/float)

**Возвращаемое значение**

Возвращает `-1` при `x < 0`, `0` при `x = 0`, `1` при `x > 0`. [`Int8`](/sql-reference/data-types/int-uint)

**Примеры**

**Знак нуля**

```sql title=Запрос
SELECT sign(0)
```

```response title=Результат
0
```

**Знак положительного числа**

```sql title=Запрос
SELECT sign(1)
```

```response title=Результат
1
```

**Знак отрицательного числа**

```sql title=Запрос
SELECT sign(-1)
```

```response title=Результат
-1
```


## sin {#sin}

Introduced in: v

Возвращает синус аргумента.

**Синтаксис**

```sql
sin(x)
```

**Аргументы**

- `x` — Число, для которого вычисляется синус. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Синус `x`.

**Примеры**

**Простой пример**

```sql title=Query
SELECT sin(1.23)
```

```response title=Response
0.9424888019316975
```


## sinh {#sinh}

Введено в версии: v20.12

Возвращает гиперболический синус.

**Синтаксис**

```sql
sinh(x)
```

**Аргументы**

- `x` — угол в радианах. Значения из интервала: -∞ < x < +∞. [`(U)Int*`](/sql-reference/data-types/int-uint) или [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает значения из интервала: -∞ < sinh(x) < +∞. [`Float64`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT sinh(0)
```

```response title=Результат
0
```


## sqrt {#sqrt}

Введена в версии: v1.1

Возвращает квадратный корень аргумента.

**Синтаксис**

```sql
sqrt(x)
```

**Аргументы**

- `x` — Число, квадратный корень которого требуется вычислить. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Квадратный корень из x. [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT sqrt(16);
```

```response title=Результат
4
```


## tan {#tan}

Введена в версии: v1.1

Возвращает тангенс аргумента.

**Синтаксис**

```sql
tan(x)
```

**Аргументы**

- `x` — Угол в радианах. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает тангенс `x`. [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT tan(0);
```

```response title=Результат
0
```


## tanh {#tanh}

Введена в версии: v20.1

Возвращает гиперболический тангенс.

**Синтаксис**

```sql
tanh(x)
```

**Аргументы**

- `x` — угол в радианах. Значения из интервала: -∞ < x < +∞. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Возвращает значения из интервала: -1 < tanh(x) < 1. [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT tanh(0)
```

```response title=Результат
0
```


## tgamma {#tgamma}

Введена в версии: v1.1

Возвращает значение гамма-функции.

**Синтаксис**

```sql
tgamma(x)
```

**Аргументы**

- `x` — Число, для которого вычисляется гамма-функция. [`(U)Int*`](/sql-reference/data-types/int-uint), [`Float*`](/sql-reference/data-types/float) или [`Decimal*`](/sql-reference/data-types/decimal)

**Возвращаемое значение**

Значение гамма-функции. [`Float*`](/sql-reference/data-types/float)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT tgamma(5);
```

```response title=Результат
24
```


## widthBucket {#widthBucket}

Введена в версии: v23.3

Возвращает номер интервала, в который попадает параметр `operand` в гистограмме, состоящей из `count` интервалов равной ширины, охватывающих диапазон от `low` до `high`. Возвращает 0, если `operand` меньше `low`, и возвращает `count`+1, если `operand` больше или равен `high`.
Также существует регистронезависимый псевдоним `WIDTH_BUCKET` для обеспечения совместимости с другими базами данных.

**Синтаксис**

```sql
widthBucket(operand, low, high, count)
```

**Псевдонимы**: `width_bucket`

**Аргументы**

- `operand` — Значение, для которого определяется интервал. [`(U)Int8/16/32/64`](/sql-reference/data-types/int-uint)
- `low` — Нижняя граница диапазона гистограммы. [`(U)Int8/16/32/64`](/sql-reference/data-types/int-uint)
- `high` — Верхняя граница диапазона гистограммы. [`(U)Int8/16/32/64`](/sql-reference/data-types/int-uint)
- `count` — Количество интервалов равной ширины. Не может быть равно нулю. [`UInt8/16/32/64`](/sql-reference/data-types/int-uint)

**Возвращаемое значение**

Возвращает номер интервала в виде целого числа. Возвращает 0, если operand < low, возвращает count+1, если operand >= high. [`UInt8/16/32/64`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Запрос
widthBucket(10.15, -8.6, 23, 18)
```

```response title=Результат
11
```

<!--AUTOGENERATED_END-->
