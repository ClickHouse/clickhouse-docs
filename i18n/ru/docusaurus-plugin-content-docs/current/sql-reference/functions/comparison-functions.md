---
description: 'Справочник по функциям сравнения'
sidebar_label: 'Сравнение'
slug: /sql-reference/functions/comparison-functions
title: 'Функции сравнения'
doc_type: 'reference'
---



# Функции сравнения



## Правила сравнения {#comparison-rules}

Функции сравнения, описанные ниже, возвращают `0` или `1` типа [UInt8](/sql-reference/data-types/int-uint). Сравнивать можно только значения внутри одной группы (например, `UInt16` и `UInt64`), но не между группами (например, `UInt16` и `DateTime`).
Возможно сравнение чисел со строками, а также строк с датами и дат со временем.
Для кортежей и массивов используется лексикографическое сравнение, то есть сравнение выполняется для каждого соответствующего элемента левого и правого кортежа/массива.

Можно сравнивать следующие типы:

- числа и десятичные числа
- строки и строки фиксированной длины
- даты
- даты со временем
- кортежи (лексикографическое сравнение)
- массивы (лексикографическое сравнение)

:::note
Строки сравниваются побайтово. Это может привести к неожиданным результатам, если одна из строк содержит многобайтовые символы в кодировке UTF-8.
Строка S1, содержащая другую строку S2 в качестве префикса, считается длиннее S2.
:::

<!--
Внутреннее содержимое тегов ниже заменяется во время сборки документации
на документацию, сгенерированную из system.functions. Пожалуйста, не изменяйте и не удаляйте теги.
See: https://github.com/ClickHouse/clickhouse-docs/blob/main/contribute/autogenerated-documentation-from-source.md
-->


<!--AUTOGENERATED_START-->

## equals {#equals}

Введена в версии: v1.1

Сравнивает два значения на равенство.

**Синтаксис**

```sql
equals(a, b)
        -- a = b
        -- a == b
```

**Аргументы**

- `a` — первое значение.<sup>[\*](#comparison-rules)</sup>
- `b` — второе значение.<sup>[\*](#comparison-rules)</sup>

**Возвращаемое значение**

Возвращает `1`, если `a` равно `b`, иначе `0`. [`UInt8`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT 1 = 1, 1 = 2;
```

```response title=Результат
┌─equals(1, 1)─┬─equals(1, 2)─┐
│            1 │            0 │
└──────────────┴──────────────┘
```


## greater {#greater}

Введено в версии: v1.1

Сравнивает два значения по отношению «больше».

**Синтаксис**

```sql
greater(a, b)
    -- a > b
```

**Аргументы**

- `a` — Первое значение.<sup>[\*](#comparison-rules)</sup>
- `b` — Второе значение.<sup>[\*](#comparison-rules)</sup>

**Возвращаемое значение**

Возвращает `1`, если `a` больше `b`, иначе `0`. [`UInt8`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Query
SELECT 2 > 1, 1 > 2;
```

```response title=Response
┌─greater(2, 1)─┬─greater(1, 2)─┐
│             1 │             0 │
└───────────────┴───────────────┘
```


## greaterOrEquals {#greaterOrEquals}

Введена в версии: v1.1

Сравнивает два значения по отношению «больше или равно».

**Синтаксис**

```sql
greaterOrEquals(a, b)
    -- a >= b
```

**Аргументы**

- `a` — первое значение.<sup>[\*](#comparison-rules)</sup>
- `b` — второе значение.<sup>[\*](#comparison-rules)</sup>

**Возвращаемое значение**

Возвращает `1`, если `a` больше или равно `b`, иначе `0`. [`UInt8`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT 2 >= 1, 2 >= 2, 1 >= 2;
```

```response title=Результат
┌─greaterOrEquals(2, 1)─┬─greaterOrEquals(2, 2)─┬─greaterOrEquals(1, 2)─┐
│                     1 │                     1 │                     0 │
└───────────────────────┴───────────────────────┴───────────────────────┘
```


## isDistinctFrom {#isDistinctFrom}

Введено в версии: v25.9

        Выполняет null-безопасное сравнение «не равно» между двумя значениями.
        Возвращает `true`, если значения различаются (не равны), в том числе когда одно значение равно NULL, а другое — нет.
        Возвращает `false`, если значения равны или оба равны NULL.

**Синтаксис**

```sql
isDistinctFrom(x, y)
```

**Аргументы**

- `x` — первое значение для сравнения. Может иметь любой тип данных ClickHouse. [`Any`](/sql-reference/data-types)
- `y` — второе значение для сравнения. Может иметь любой тип данных ClickHouse. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает `true`, если два значения различаются, при этом NULL считаются сравнимыми:

- Возвращает `true`, если x != y.
- Возвращает `true`, если ровно одно из значений x или y равно NULL.
- Возвращает `false`, если x = y или оба значения x и y равны NULL. [`Bool`](/sql-reference/data-types/boolean)

**Примеры**

**Базовое использование с числами и NULL**

```sql title=Запрос
SELECT
    isDistinctFrom(1, 2) AS result_1,
    isDistinctFrom(1, 1) AS result_2,
    isDistinctFrom(NULL, 1) AS result_3,
    isDistinctFrom(NULL, NULL) AS result_4
```

```response title=Результат
┌─result_1─┬─result_2─┬─result_3─┬─result_4─┐
│        1 │        0 │        1 │        0 │
└──────────┴──────────┴──────────┴──────────┘
```


## isNotDistinctFrom {#isNotDistinctFrom}

Введено в версии: v25.9

        Выполняет null-безопасное сравнение на равенство между двумя значениями.
        Возвращает `true`, если значения равны, в том числе когда оба равны NULL.
        Возвращает `false`, если значения различаются, или если ровно одно из них равно NULL.

**Синтаксис**

```sql
isNotDistinctFrom(x, y)
```

**Аргументы**

- `x` — Первое сравниваемое значение. Может иметь любой тип данных ClickHouse. [`Any`](/sql-reference/data-types)
- `y` — Второе сравниваемое значение. Может иметь любой тип данных ClickHouse. [`Any`](/sql-reference/data-types)

**Возвращаемое значение**

Возвращает `true`, если два значения равны, при этом NULL рассматриваются как сравнимые:

- Возвращает `true`, если x = y.
- Возвращает `true`, если и x, и y равны NULL.
- Возвращает `false`, если x != y, или ровно одно из x или y равно NULL. [`Bool`](/sql-reference/data-types/boolean)

**Примеры**

**Базовое использование с числами и NULL**

```sql title=Запрос
SELECT
    isNotDistinctFrom(1, 1) AS result_1,
    isNotDistinctFrom(1, 2) AS result_2,
    isNotDistinctFrom(NULL, NULL) AS result_3,
    isNotDistinctFrom(NULL, 1) AS result_4
```

```response title=Результат
┌─result_1─┬─result_2─┬─result_3─┬─result_4─┐
│        1 │        0 │        1 │        0 │
└──────────┴──────────┴──────────┴──────────┘
```


## less {#less}

Введено в версии: v1.1

Сравнивает два значения на предмет отношения «меньше».

**Синтаксис**

```sql
less(a, b)
    -- a < b
```

**Аргументы**

- `a` — первое значение.<sup>[\*](#comparison-rules)</sup>
- `b` — второе значение.<sup>[\*](#comparison-rules)</sup>

**Возвращаемое значение**

Возвращает `1`, если `a` меньше `b`, иначе `0`. Тип: [`UInt8`](/sql-reference/data-types/int-uint).

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT 1 < 2, 2 < 1;
```

```response title=Результат
┌─less(1, 2)─┬─less(2, 1)─┐
│          1 │          0 │
└────────────┴────────────┘
```


## lessOrEquals {#lessOrEquals}

Введено в версии: v1.1

Сравнивает два значения по отношению «меньше или равно».

**Синтаксис**

```sql
lessOrEquals(a, b)
-- a <= b
```

**Аргументы**

- `a` — первое значение.<sup>[\*](#comparison-rules)</sup>
- `b` — второе значение.<sup>[\*](#comparison-rules)</sup>

**Возвращаемое значение**

Возвращает `1`, если `a` меньше или равно `b`, иначе `0`. [`UInt8`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT 1 <= 2, 2 <= 2, 3 <= 2;
```

```response title=Результат
┌─lessOrEquals(1, 2)─┬─lessOrEquals(2, 2)─┬─lessOrEquals(3, 2)─┐
│                  1 │                  1 │                  0 │
└────────────────────┴────────────────────┴────────────────────┘
```


## notEquals {#notEquals}

Введена в версии: v1.1

Сравнивает два значения на неравенство.

**Синтаксис**

```sql
notEquals(a, b)
    -- a != b
    -- a <> b
```

**Аргументы**

- `a` — первое значение.<sup>[\*](#comparison-rules)</sup>
- `b` — второе значение.<sup>[\*](#comparison-rules)</sup>

**Возвращаемое значение**

Возвращает `1`, если `a` не равно `b`, иначе `0`. [`UInt8`](/sql-reference/data-types/int-uint)

**Примеры**

**Пример использования**

```sql title=Запрос
SELECT 1 != 2, 1 != 1;
```

```response title=Результат
┌─notEquals(1, 2)─┬─notEquals(1, 1)─┐
│               1 │               0 │
└─────────────────┴─────────────────┘
```

<!--AUTOGENERATED_END-->
