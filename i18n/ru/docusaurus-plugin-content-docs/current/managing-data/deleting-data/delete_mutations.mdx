---
slug: /managing-data/delete_mutations
sidebar_label: 'Delete mutations'
title: 'Мутации удаления'
hide_title: false
description: 'Страница, описывающая мутации удаления — запросы ALTER, которые изменяют данные в таблице за счёт удаления строк'
keywords: ['delete mutations', 'ALTER TABLE DELETE', 'data mutations', 'data parts rewrite', 'ReplacingMergeTree']
doc_type: 'reference'
---

Под мутациями удаления понимаются запросы `ALTER`, которые изменяют данные в таблице посредством удаления. Наиболее типичный пример — запросы вида `ALTER TABLE DELETE` и т. д. Выполнение таких запросов приводит к созданию новых мутировавших версий кусков данных. Это означает, что такие операторы инициируют перезапись целых кусков данных для всех строк, вставленных до мутации, что приводит к большому количеству операций записи.

:::info
Для операций удаления можно избежать большого количества операций записи, используя специализированные движки таблиц, такие как [ReplacingMergeTree](/guides/replacing-merge-tree) или [CollapsingMergeTree](/engines/table-engines/mergetree-family/collapsingmergetree) вместо стандартного движка таблиц MergeTree.
:::

import DeleteMutations from '@site/docs/sql-reference/statements/alter/delete.md';

<DeleteMutations />
