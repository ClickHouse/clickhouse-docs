---
title: Изменение приглашения в clickhouse-client
description: "В этой статье объясняется, как изменить строку приглашения в клиенте ClickHouse и в окне терминала clickhouse-local с :) на префикс, за которым следует :)"
date: 2023-11-16
tags: ['Настройки', 'Нативные клиенты и интерфейсы']
keywords: ['Клиент ClickHouse', 'Изменение строки приглашения']
---

import Image from "@theme/IdealImage";
import prompt_0 from "@site/static/images/knowledgebase/change-the-prompt-in-clickhouse-client/0_prompt.png";
import prompt_1 from "@site/static/images/knowledgebase/change-the-prompt-in-clickhouse-client/1_prompt.png";
import prompt_2 from "@site/static/images/knowledgebase/change-the-prompt-in-clickhouse-client/2_prompt.png";
import prompt_3 from "@site/static/images/knowledgebase/change-the-prompt-in-clickhouse-client/3_prompt.png";
import prompt_4 from "@site/static/images/knowledgebase/change-the-prompt-in-clickhouse-client/4_prompt.png";
import prompt_5 from "@site/static/images/knowledgebase/change-the-prompt-in-clickhouse-client/5_prompt.png";
import prompt_6 from "@site/static/images/knowledgebase/change-the-prompt-in-clickhouse-client/6_prompt.png";
import prompt_7 from "@site/static/images/knowledgebase/change-the-prompt-in-clickhouse-client/7_prompt.png";
import prompt_8 from "@site/static/images/knowledgebase/change-the-prompt-in-clickhouse-client/8_prompt.png";

{frontMatter.description}

{/* обрезка */}

Если вам не нравится, как `clickhouse client` и clickhouse-local отображают приглашение командной строки в окне терминала, вы можете изменить его, добавив префикс.
В этой статье объясняется, как изменить приглашение на любое нужное вам.

Приглашение по умолчанию — это имя вашего локального компьютера, за которым следует `:) `:

<Image img={prompt_0} size="md" alt="Стандартное приглашение клиента ClickHouse" />

Ниже приведены переменные, которые можно использовать в приглашении: `{user}`, `{host}`

Существует несколько способов изменить приглашение, и мы рассмотрим каждый из них.

## флаг --prompt \{#--prompt-flag\}

Первый способ изменить этот флаг — использовать параметр `--prompt`:

```bash
clickhouse --prompt 👉
```

Это добавит эмодзи с указующим пальцем перед смайликом:

<Image img={prompt_1} size="md" alt="Командная строка клиента ClickHouse с эмодзи с указующим пальцем" />

## Файл конфигурации — верхний уровень \{#config-file---top-level\}

Также вы можете указать префикс промпта в файле `config.xml`:

```xml
<config>
    <prompt>👉 </prompt>
</config>
```

```
clickhouse
```

<Image img={prompt_2} size="md" alt="Командная строка клиента ClickHouse с файлом конфигурации" />

Мы можем использовать файл конфигурации с любым именем и передать его через флаг `-C`:

```xml
<config>
    <prompt>🎄 </prompt>
</config>
```

```
clickhouse -C christmas.xml
```

<Image img={prompt_3} size="md" alt="Командная строка клиента ClickHouse с эмодзи в виде рождественской ёлки" />

Предпочитаете конфигурационные файлы в формате YAML?
Это тоже подойдёт:

```yaml
prompt: 🟡
```

```bash
clickhouse -C christmas.yaml
```

<Image img={prompt_4} size="md" alt="Приглашение клиента ClickHouse с эмодзи с жёлтым кругом" />

## Файл конфигурации - connections&#95;credentials \{#config-file---connections&#95;credentials\}

Кроме того, вы можете задать приглашение отдельно для каждого набора учетных данных подключения.
Это имеет смысл только при использовании clickhouse-client.

```xml
<config>
    <connections_credentials>
        <connection>
            <n>prod<n>
            <hostname>127.0.0.1</hostname>
            <port>9000</port>
            <secure>0</secure>
            <user>default</user>
            <prompt>\e[31m[PRODUCTION]\e[0m {user}@prod</prompt>
        </connection>
        <connection>
            <n>dev<n>
            <hostname>127.0.0.1</hostname>
            <port>9000</port>
            <secure>0</secure>
            <user>default</user>
            <prompt>\e[32m[DEVELOPMENT]\e[0m {user}@dev</prompt>
        </connection>
    </connections_credentials>
</config>
```

Затем мы можем попытаться подключиться с помощью подключения `dev`:

```bash
clickhouse client -C connections.xml --connection dev
```

<Image img={prompt_5} size="md" alt="Командная строка клиента ClickHouse с подключением к среде разработки" />

Или вариант для `prod`:

```bash
clickhouse client -C connections.xml --connection prod
```

<Image img={prompt_6} size="md" alt="Командная строка клиента ClickHouse с подключением к продакшн-среде" />

А вот YAML‑версия:

```yaml
connections_credentials:
  connection:
    - name: prod
      hostname: 127.0.0.1
      port: 9000
      secure: 0
      user: default
      prompt: "\e[35m[PRODUCTION]\e[0m {user}@{host}"
    - name: dev
      hostname: 127.0.0.1
      port: 9000
      secure: 0
      user: default
      prompt: "\e[34m[DEVELOPMENT]\e[0m {user}@{host}"
```

Используя подключение `dev`:

```bash
clickhouse client -C connections.yaml --connection dev
```

<Image img={prompt_7} size="md" alt="CLI-клиент ClickHouse с dev-подключением, заданным в YAML" />

А теперь `prod`:

```bash
clickhouse client -C connections.yaml --connection prod
```

<Image img={prompt_8} size="md" alt="Консоль клиента ClickHouse с подключением к продакшен-среде, настроенным через YAML" />
