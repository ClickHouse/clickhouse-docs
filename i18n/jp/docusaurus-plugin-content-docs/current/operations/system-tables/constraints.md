---
description: 'すべてのテーブルに存在する制約の情報を含むシステムテーブル。'
keywords: ['システムテーブル', '制約']
slug: /operations/system-tables/constraints
sidebar_label: 'system.constraints'
sidebar_position: 20
title: 'system.constraints'
doc_type: 'reference'
---

## 説明 \{#description\}

すべてのテーブルに存在する制約に関する情報が含まれています。

[一時テーブル](../../sql-reference/statements/create/table.md#temporary-tables) で定義された制約は、作成されたセッションでのみ `system.constraints` に表示されます。これらは `database` フィールドが空の状態で表示されます。

## カラム \{#columns\}

{/*AUTOGENERATED_START*/ }

* `database` ([String](../../sql-reference/data-types/)) — データベース名。
* `table` ([String](../../sql-reference/data-types/)) — テーブル名。
* `name` ([String](../../sql-reference/data-types/)) — 制約名。
* `type` ([Enum8(&#39;CHECK&#39; = 0, &#39;ASSUME&#39; = 1)](../../sql-reference/data-types/)) — 制約の種類。
* `expression` ([String](../../sql-reference/data-types/)) — 制約式。

{/*AUTOGENERATED_END*/ }

## 例 \{#example\}

```sql
SELECT * FROM system.constraints LIMIT 2 FORMAT Vertical;
```

```text
Row 1:
──────
database:    default
table:       hits
name:        check_hits
type:        CHECK
expression:  CounterID > 0

Row 2:
──────
database:    default
table:       hits
name:        assume_positive
type:        ASSUME
expression:  WatchID > 0
```