---
sidebar_label: 'データ転送'
slug: /cloud/manage/network-data-transfer
title: 'データ転送'
description: 'ClickHouse Cloud が計測するデータ転送の入出力について詳しく学びます'
---

import NetworkPricing from '@site/docs/cloud/manage/_snippets/_network_transfer_rates.md';

ClickHouse Cloud はデータ転送の入出力を計測します。  
これには、ClickHouse Cloud へのデータの入出、および地域内や地域間のデータ転送が含まれます。  
この使用状況はサービスレベルで追跡されます。この使用に基づいて、顧客はデータ転送料金が発生し、それが月次請求書に追加されます。

ClickHouse Cloud は以下のものに対して料金を請求します：
- ClickHouse Cloud から公共インターネットへのデータの出力、他のクラウドプロバイダーの他の地域へのデータ出力を含みます。  
- 同じクラウドプロバイダー内の別の地域へのデータ出力。

地域内のデータ転送やプライベートリンク/プライベートサービスコネクトの使用とデータ転送に対しては料金は発生しません。  
ただし、ユーザーに適切に料金を請求する能力に影響を与える使用パターンが確認された場合、追加のデータ転送料金の側面を実施する権利を留保します。

データ転送料金はクラウドサービスプロバイダー（CSP）や地域によって異なります。  
公共インターネットへのデータ出力の料金は、発信地域のみに基づいています。  
地域間（またはクロスリージョン）料金は、発信地域と宛先地域の両方に依存します。

**データ転送コストを最小限に抑えるためのベストプラクティス**

データの入出力時にデータ転送コストを最小限に抑えるために考慮すべきいくつかのパターンがあります。
1. Clickhouse Cloud からのデータの入出時に、可能な限り圧縮を使用して、転送するデータ量と関連するコストを最小限に抑えてください。
2. 非インライン値（例：INSERT INTO [TABLE] FROM INFILE [FILE] FORMAT NATIVE）を使用してネイティブプロトコル経由で INSERT を行う場合、ClickHouse クライアントはデータをパックするためにサーバーからメタデータを取得します。メタデータが INSERT ペイロードよりも大きい場合、サーバーの観点からは入出力の逆転現象が発生することがあります。これが受け入れられない場合、VALUES 構文でデータをインライン化するか、HTTP プロトコルを使用することを検討してください。

下の表は、公共インターネットまたは地域間の出力に対するデータ転送料金がクラウドプロバイダーと地域によってどのように異なるかを示しています。

:::note
ClickHouse Cloud は地域間の使用状況を、発信地域と宛先地域に応じて、Tier 1 から Tier 4 までの階層で計測します。下の表は、地域間データ転送の各組み合わせに対する階層を示しています。ClickHouse Cloud の請求使用状況画面では、データ転送の使用状況が階層ごとに分かれて表示されます。 
:::

<NetworkPricing/>
