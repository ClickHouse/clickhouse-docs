---
description: 'JSON に格納されているパスとその型の一覧を、重複を排除して算出します'
slug: /sql-reference/aggregate-functions/reference/distinctJSONPathsAndTypes
title: 'distinctJSONPathsAndTypes'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## distinctJSONPathsAndTypes \{#distinctJSONPathsAndTypes\}

導入バージョン: v24.9

[JSON](https://clickhouse.com/docs/sql-reference/data-types/newjson) カラムに保存されている異なるパスとその型の一覧を算出します。

:::note
JSON 宣言内に型が指定されたパスが含まれている場合、入力データにこれらのパスに対応する値が存在しない場合でも、これらのパスは常に `distinctJSONPaths/distinctJSONPathsAndTypes` 関数の結果に含まれます。
:::

**構文**

```sql
distinctJSONPathsAndTypes(json)
```

**引数**

* `json` — JSON カラム。 [`JSON`](/sql-reference/data-types/newjson)

**戻り値**

パスと型のソート済みのマップを返します。 [`Map(String, Array(String))`](/sql-reference/data-types/map)

**例**

**混在する型での基本的な使用例**

```sql title=Query
DROP TABLE IF EXISTS test_json;
CREATE TABLE test_json(json JSON) ENGINE = Memory;
INSERT INTO test_json VALUES ('{"a" : 42, "b" : "Hello"}'), ('{"b" : [1, 2, 3], "c" : {"d" : {"e" : "2020-01-01"}}}'), ('{"a" : 43, "c" : {"d" : {"f" : [{"g" : 42}]}}}');

SELECT distinctJSONPathsAndTypes(json) FROM test_json;
```

```response title=Response
┌─distinctJSONPathsAndTypes(json)───────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ {'a':['Int64'],'b':['Array(Nullable(Int64))','String'],'c.d.e':['Date'],'c.d.f':['Array(JSON(max_dynamic_types=16, max_dynamic_paths=256))']} │
└───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

**JSON パスを宣言した場合**

```sql title=Query
DROP TABLE IF EXISTS test_json;
CREATE TABLE test_json(json JSON(a UInt32)) ENGINE = Memory;
INSERT INTO test_json VALUES ('{"b" : "Hello"}'), ('{"b" : "World", "c" : [1, 2, 3]}');

SELECT distinctJSONPathsAndTypes(json) FROM test_json;
```

```response title=Response
┌─distinctJSONPathsAndTypes(json)────────────────────────────────┐
│ {'a':['UInt32'],'b':['String'],'c':['Array(Nullable(Int64))']} │
└────────────────────────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/ }
