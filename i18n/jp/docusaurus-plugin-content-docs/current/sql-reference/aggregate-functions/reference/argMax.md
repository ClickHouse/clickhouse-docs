---
description: '`val` の最大値に対応する `arg` の値を計算します。'
slug: /sql-reference/aggregate-functions/reference/argmax
title: 'argMax'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## argMax \{#argMax\}

導入バージョン: v1.1

最大の `val` 値に対応する `arg` の値を計算します。最大となる同じ `val` を持つ行が複数ある場合、どの行の `arg` が返されるかは保証されません。
`arg` と `max` の両方は[集約関数](/sql-reference/aggregate-functions/index.md)として動作し、処理中に[`Null` をスキップ](/sql-reference/aggregate-functions/index.md#null-processing)し、`Null` 以外の値が 1 つでも存在する場合は `Null` 以外の値を返します。

**関連項目**

* [Tuple](/sql-reference/data-types/tuple.md)

**構文**

```sql
argMax(arg, val)
```

**引数**

* `arg` — 最大値を求める対象となる引数。[`const String`](/sql-reference/data-types/string)
* `val` — 最大値。[`(U)Int8/16/32/64`](/sql-reference/data-types/int-uint) または [`Float*`](/sql-reference/data-types/float) または [`Date`](/sql-reference/data-types/date) または [`DateTime`](/sql-reference/data-types/datetime) または [`Tuple`](/sql-reference/data-types/tuple)

**戻り値**

最大の `val` 値に対応する `arg` の値を返します。型は `arg` の型に一致します。

**例**

**基本的な使い方**

```sql title=Query
SELECT argMax(user, salary) FROM salary;
```

```response title=Response
┌─argMax(user, salary)─┐
│ director             │
└──────────────────────┘
```

**NULL の扱いを含む詳細な例**

```sql title=Query
CREATE TABLE test
(
    a Nullable(String),
    b Nullable(Int64)
)
ENGINE = Memory AS
SELECT *
FROM VALUES(('a', 1), ('b', 2), ('c', 2), (NULL, 3), (NULL, NULL), ('d', NULL));

SELECT argMax(a, b), max(b) FROM test;
```

```response title=Response
┌─argMax(a, b)─┬─max(b)─┐
│ b            │      3 │
└──────────────┴────────┘
```

**引数での Tuple の利用**

```sql title=Query
SELECT argMax(a, (b,a)) FROM test;
```

```response title=Response
┌─argMax(a, tuple(b, a))─┐
│ c                      │
└────────────────────────┘
```

{/*AUTOGENERATED_END*/ }
