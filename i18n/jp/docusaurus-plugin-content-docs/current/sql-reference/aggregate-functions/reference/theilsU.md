---
description: '関数 `theilsU` は、Theil の U 不確実性係数を計算します。これは、テーブル内の 2 つのカラム間の関連度を測定する値です。'
slug: /sql-reference/aggregate-functions/reference/theilsu
title: 'theilsU'
doc_type: 'reference'
---

# theilsU \{#theilsu\}

`theilsU` 関数は、[Theil の U 不確実性係数](https://en.wikipedia.org/wiki/Contingency_table#Uncertainty_coefficient) を計算します。これは、テーブル内の 2 つのカラム間の関連の強さを測定する指標で、値の範囲は 0.0（関連性なし）から 1.0（完全な一致）までです。

**構文**

```sql
theilsU(column1, column2)
```

**引数**

* `column1` と `column2` は比較するカラムです

**返り値**

* 0 から 1 の間の値

**戻り値の型** は常に [Float64](../../../sql-reference/data-types/float.md) です。

**例**

次の 2 つのカラムは互いの相関が小さいため、`theilsU` の値は小さく正になります。

```sql
SELECT
    theilsU(a, b)
FROM
    (
        SELECT
            number % 10 AS a,
            number % 4 AS b
        FROM
            numbers(150)
    );
```

結果:

```response
┌────────theilsU(a, b)─┐
│  0.30195720557678846 │
└──────────────────────┘
```

{/*AUTOGENERATED_START*/ }

{/*AUTOGENERATED_END*/ }
