---
description: '`cramersV` 関数の結果は 0（変数間に関連がないことを表す）から 1 の範囲を取り、各値が他方によって完全に決定される場合にのみ 1 に達します。これは、2 つの変数間の関連性を、それらが理論上取り得る最大限の変動に対する割合として捉えることができます。'
slug: /sql-reference/aggregate-functions/reference/cramersv
title: 'cramersV'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## cramersV \{#cramersV\}

導入バージョン: v22.1

[Cramer&#39;s V](https://en.wikipedia.org/wiki/Cram%C3%A9r%27s_V)（Cramer&#39;s phi と呼ばれることもあります）は、テーブルの 2 つのカラム間の連関の強さを表す指標です。
`cramersV` 関数の結果は 0（変数間に連関がないことに対応）から 1 の範囲を取り、一方の値が他方によって完全に決定される場合にのみ 1 になります。
これは、2 つの変数間の連関を、それらが取り得る最大限の変動量に対する割合として解釈できます。

:::note
バイアス補正済みの Cramer&#39;s V については、[cramersVBiasCorrected](./cramersvbiascorrected.md) を参照してください。
:::

**構文**

```sql
cramersV(column1, column2)
```

**引数**

* `column1` — 比較対象となる 1 つ目のカラム。[`(U)Int*`](/sql-reference/data-types/int-uint) または [`Float*`](/sql-reference/data-types/float) または [`Decimal`](/sql-reference/data-types/decimal)
* `column2` — 比較対象となる 2 つ目のカラム。[`(U)Int*`](/sql-reference/data-types/int-uint) または [`Float*`](/sql-reference/data-types/float) または [`Decimal`](/sql-reference/data-types/decimal)

**返される値**

カラムの値の間に関連性がないことを表す 0 から、完全な関連性があることを表す 1 までの値を返します。[`Float64`](/sql-reference/data-types/float)

**例**

**カラム間に関連性がない場合**

```sql title=Query
SELECT
    cramersV(a, b)
FROM
    (
        SELECT
            number % 3 AS a,
            number % 5 AS b
        FROM
            numbers(150)
    )
```

```response title=Response
┌─cramersV(a, b)─┐
│              0 │
└────────────────┘
```

**カラム間の連関が強い場合**

```sql title=Query
SELECT
    cramersV(a, b)
FROM
    (
        SELECT
            number % 10 AS a,
            number % 5 AS b
        FROM
            numbers(150)
    )
```

```response title=Response
┌─────cramersV(a, b)─┐
│ 0.8944271909999159 │
└────────────────────┘
```

{/*AUTOGENERATED_END*/ }
