---
description: 'あるカラムに含まれる値のシャノンエントロピーを計算します。'
slug: /sql-reference/aggregate-functions/reference/entropy
title: 'entropy'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## entropy \{#entropy\}

導入バージョン: v20.1

列内の値に対して[シャノンエントロピー](https://en.wikipedia.org/wiki/Entropy_\(information_theory\))を計算します。

**構文**

```sql
entropy(val)
```

**引数**

* `val` — 任意の型の値を持つカラム。[`Any`](/sql-reference/data-types)

**返される値**

シャノンエントロピーを返します。[`Float64`](/sql-reference/data-types/float)

**例**

**基本的なエントロピー計算**

```sql title=Query
CREATE TABLE entropy (`vals` UInt32,`strings` String)
ENGINE = Memory;

INSERT INTO entropy VALUES (1, 'A'), (1, 'A'), (1,'A'), (1,'A'), (2,'B'), (2,'B'), (2,'C'), (2,'D');

SELECT entropy(vals), entropy(strings) FROM entropy
```

```response title=Response
┌─entropy(vals)─┬─entropy(strings)─┐
│             1 │             1.75 │
└───────────────┴──────────────────┘
```

{/*AUTOGENERATED_END*/ }
