---
description: '単回帰（1 次元の線形回帰）を実行します。'
slug: /sql-reference/aggregate-functions/reference/simplelinearregression
title: 'simpleLinearRegression'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## simpleLinearRegression \{#simpleLinearRegression\}

導入: v20.1

単回帰（1次元の線形回帰）を実行します。

**構文**

```sql
simpleLinearRegression(x, y)
```

**引数**

* `x` — 説明変数の値を含むカラム。[`Float64`](/sql-reference/data-types/float)
* `y` — 従属変数の値を含むカラム。[`Float64`](/sql-reference/data-types/float)

**返り値**

結果となる直線 `y = k*x + b` の係数 `(k, b)` を返します。[`Tuple(Float64, Float64)`](/sql-reference/data-types/tuple)

**例**

**完全な線形当てはめ**

```sql title=Query
SELECT arrayReduce('simpleLinearRegression', [0, 1, 2, 3], [0, 1, 2, 3]);
```

```response title=Response
┌─arrayReduce('simpleLinearRegression', [0, 1, 2, 3], [0, 1, 2, 3])─┐
│ (1,0)                                                             │
└───────────────────────────────────────────────────────────────────┘
```

**切片付き線形フィット**

```sql title=Query
SELECT arrayReduce('simpleLinearRegression', [0, 1, 2, 3], [3, 4, 5, 6]);
```

```response title=Response
┌─arrayReduce('simpleLinearRegression', [0, 1, 2, 3], [3, 4, 5, 6])─┐
│ (1,3)                                                             │
└───────────────────────────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/ }
