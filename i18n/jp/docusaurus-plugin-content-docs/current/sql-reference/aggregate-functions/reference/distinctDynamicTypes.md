---
description: 'Dynamic カラムに保存されているデータ型のうち、異なるものの一覧を算出します。'
slug: /sql-reference/aggregate-functions/reference/distinctdynamictypes
title: 'distinctDynamicTypes'
doc_type: 'reference'
---

{/*AUTOGENERATED_START*/ }

## distinctDynamicTypes \{#distinctDynamicTypes\}

導入バージョン: v24.9

[Dynamic](https://clickhouse.com/docs/sql-reference/data-types/dynamic) カラムに格納されているデータ型のうち、異なるものの一覧を返します。

**構文**

```sql
distinctDynamicTypes(dynamic)
```

**引数**

* `dynamic` — Dynamic カラム。 [`Dynamic`](/sql-reference/data-types/dynamic)

**戻り値**

データ型名のソートされたリストを返します。 [`Array(String)`](/sql-reference/data-types/array)

**例**

**異なる型を含む基本的な使用例**

```sql title=Query
DROP TABLE IF EXISTS test_dynamic;
CREATE TABLE test_dynamic(d Dynamic) ENGINE = Memory;
INSERT INTO test_dynamic VALUES (42), (NULL), ('Hello'), ([1, 2, 3]), ('2020-01-01'), (map(1, 2)), (43), ([4, 5]), (NULL), ('World'), (map(3, 4));

SELECT distinctDynamicTypes(d) FROM test_dynamic;
```

```response title=Response
┌─distinctDynamicTypes(d)──────────────────────────────────────────┐
│ ['Array(Int64)', 'Date', 'Int64', 'Map(UInt8, UInt8)', 'String'] │
└──────────────────────────────────────────────────────────────────┘
```

{/*AUTOGENERATED_END*/ }
