---
description: '乱数生成関数のドキュメント'
sidebar_label: '乱数'
slug: /sql-reference/functions/random-functions
title: '乱数を生成する関数'
doc_type: 'リファレンス'
---



# 乱数生成関数

このセクションのすべての関数は、0個または1個の引数を受け取ります。引数を指定する唯一の目的は、[共通部分式の除去](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。これにより、同じ行内で同じ乱数関数を2回実行した場合に、異なる乱数値が返されるようになります。

関連コンテンツ

- ガイド: [ClickHouseでランダムデータを生成する](/guides/generating-test-data)
- ブログ: [ClickHouseでランダムデータを生成する](https://clickhouse.com/blog/generating-random-test-distribution-data-for-clickhouse)

:::note
乱数は非暗号化アルゴリズムによって生成されます。
:::

:::note
以下のドキュメントは`system.functions`システムテーブルから生成されています。
:::

<!--
The inner content of the tags below are replaced at doc framework build time with
docs generated from system.functions. Please do not modify or remove the tags.
See: https://github.com/ClickHouse/clickhouse-docs/blob/main/contribute/autogenerated-documentation-from-source.md
-->


<!--AUTOGENERATED_START-->

## fuzzBits {#fuzzBits}

導入バージョン: v20.5

入力文字列 `s` の各ビットを確率 `p` で反転します。

**構文**

```sql
fuzzBits(s, p)
```

**引数**

- `s` — ビットファジングを実行する String 型または FixedString 型 [`String`](/sql-reference/data-types/string) または [`FixedString`](/sql-reference/data-types/fixedstring)
- `p` — 各ビットを反転する確率（`0.0` から `1.0` の範囲の数値） [`Float*`](/sql-reference/data-types/float)

**戻り値**

`s` と同じ型のファジング処理された文字列を返します。[`String`](/sql-reference/data-types/string) または [`FixedString`](/sql-reference/data-types/fixedstring)

**例**

**使用例**

```sql title=Query
SELECT fuzzBits(materialize('abacaba'), 0.1)
FROM numbers(3)
```

```response title=Response
┌─fuzzBits(materialize('abacaba'), 0.1)─┐
│ abaaaja                               │
│ a*cjab+                               │
│ aeca2A                                │
└───────────────────────────────────────┘
```


## rand {#rand}

導入バージョン: v1.1

一様分布に従うランダムな`UInt32`型の数値を返します。

システムから取得した初期状態を用いた線形合同法を使用しているため、ランダムに見えますが真の乱数ではなく、初期状態が既知の場合は予測可能です。
真の乱数性が重要な場合は、システムレベルの呼び出しや外部ライブラリとの統合など、代替手段の使用を検討してください。

**構文**

```sql
rand([x])
```

**エイリアス**: `rand32`

**引数**

- `x` — 省略可能で無視されます。この引数の唯一の目的は、クエリ内で同じ関数呼び出しが複数回使用される場合に[共通部分式の除去](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**戻り値**

`UInt32`型のランダムな数値を返します。[`UInt32`](/sql-reference/data-types/int-uint)

**例**

**使用例**

```sql title=クエリ
SELECT rand();
```

```response title=レスポンス
1569354847
```


## rand64 {#rand64}

導入バージョン: v1.1

一様分布に従うランダムな`UInt64`型の数値を返します。

システムから取得した初期状態を用いた線形合同法を使用しているため、ランダムに見えますが真の乱数ではなく、初期状態が既知の場合は予測可能です。
真の乱数性が重要な場合は、システムレベルの呼び出しや外部ライブラリとの統合などの代替手段の使用を検討してください。

**構文**

```sql
rand64([x])
```

**引数**

- `x` — 省略可能で無視されます。この引数の唯一の目的は、同じ関数呼び出しがクエリ内で複数回使用される場合に[共通部分式の除去](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**戻り値**

一様分布に従うランダムなUInt64型の数値を返します。[`UInt64`](/sql-reference/data-types/int-uint)

**例**

**使用例**

```sql title=クエリ
SELECT rand64();
```

```response title=レスポンス
15030268859237645412
```


## randBernoulli {#randBernoulli}

導入バージョン: v22.10

[ベルヌーイ分布](https://en.wikipedia.org/wiki/Bernoulli_distribution)から抽出されたランダムなFloat64の数値を返します。

**構文**

```sql
randBernoulli(probability[, x])
```

**引数**

- `probability` — 成功確率。`0`から`1`の間の値。[`Float64`](/sql-reference/data-types/float)
- `x` — オプション(無視されます)。この引数の唯一の目的は、クエリ内で同じ関数呼び出しが複数回使用される場合に[共通部分式の削除](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**返される値**

指定されたベルヌーイ分布から抽出されたランダムな数値。[`UInt64`](/sql-reference/data-types/int-uint)

**例**

**使用例**

```sql title=Query
SELECT randBernoulli(.75) FROM numbers(5)
```

```response title=Response
┌─randBernoulli(0.75)─┐
│                   1 │
│                   1 │
│                   0 │
│                   1 │
│                   1 │
└─────────────────────┘
```


## randBinomial {#randBinomial}

導入バージョン: v22.10

[二項分布](https://en.wikipedia.org/wiki/Binomial_distribution)から抽出されたランダムなFloat64数値を返します。

**構文**

```sql
randBinomial(experiments, probability[, x])
```

**引数**

- `experiments` — 試行回数 [`UInt64`](/sql-reference/data-types/int-uint)
- `probability` — 各試行における成功確率(`0`から`1`の間の値) [`Float64`](/sql-reference/data-types/float)
- `x` — 省略可能で無視されます。この引数の唯一の目的は、同じ関数呼び出しがクエリ内で複数回使用される際に[共通部分式の削除](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです [`Any`](/sql-reference/data-types)

**戻り値**

指定された二項分布から抽出されたランダムなFloat64数値を返します [`UInt64`](/sql-reference/data-types/int-uint)

**例**

**使用例**

```sql title=Query
SELECT randBinomial(100, .75) FROM numbers(5)
```

```response title=Response
┌─randBinomial(100, 0.75)─┐
│                      74 │
│                      78 │
│                      76 │
│                      77 │
│                      80 │
└─────────────────────────┘
```


## randCanonical {#randCanonical}

導入バージョン: v22.11

`0`（含む）から`1`（含まない）の間で一様分布するランダムな`Float64`型の数値を返します。

**構文**

```sql
randCanonical([x])
```

**引数**

- `x` — オプション引数で、値は無視されます。この引数の唯一の目的は、同じ関数呼び出しがクエリ内で複数回使用される際に[共通部分式の削除](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**戻り値**

ランダムなFloat64型の数値。[`Float64`](/sql-reference/data-types/float)

**例**

**使用例**

```sql title=Query
SELECT randCanonical();
```

```response title=Response
0.345217890123456
```


## randChiSquared {#randChiSquared}

導入バージョン: v22.10

[カイ二乗分布](https://en.wikipedia.org/wiki/Chi-squared_distribution)から抽出されたランダムなFloat64数値を返します。

**構文**

```sql
randChiSquared(degree_of_freedom[, x])
```

**引数**

- `degree_of_freedom` — 自由度。[`Float64`](/sql-reference/data-types/float)
- `x` — オプション引数で、値は無視されます。この引数の唯一の目的は、同じ関数呼び出しがクエリ内で複数回使用される場合に[共通部分式の削除](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**返り値**

指定されたカイ二乗分布から抽出されたランダムなFloat64数値を返します。[`Float64`](/sql-reference/data-types/float)

**例**

**使用例**

```sql title=Query
SELECT randChiSquared(10) FROM numbers(5)
```

```response title=Response
┌─randChiSquared(10)─┐
│ 10.015463656521543 │
│  9.621799919882768 │
│   2.71785015634699 │
│ 11.128188665931908 │
│  4.902063104425469 │
└────────────────────┘
```


## randConstant {#randConstant}

導入バージョン: v1.1

現在のクエリ実行において、すべての行で一定となる単一のランダム値を生成します。

この関数の特性:

- 単一のクエリ内のすべての行に対して同じランダム値を返します
- 別々のクエリ実行では異なる値を生成します

データセット内のすべての行に対して一貫したランダムシードや識別子を適用する際に有用です。

**構文**

```sql
randConstant([x])
```

**引数**

- `x` — 省略可能で無視されます。この引数の唯一の目的は、同じ関数呼び出しがクエリ内で複数回使用される場合に[共通部分式の除去](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**返り値**

各行に同じランダム値を含む`UInt32`型のカラムを返します。[`UInt32`](/sql-reference/data-types/int-uint)

**例**

**基本的な使用方法**

```sql title=Query
SELECT randConstant() AS random_value;
```

```response title=Response
| random_value |
|--------------|
| 1234567890   |
```

**パラメータを使用した場合**

```sql title=Query
SELECT randConstant(10) AS random_value;
```

```response title=Response
| random_value |
|--------------|
| 9876543210   |
```


## randExponential {#randExponential}

導入バージョン: v22.10

[指数分布](https://en.wikipedia.org/wiki/Exponential_distribution)から抽出されたランダムなFloat64値を返します。

**構文**

```sql
randExponential(lambda[, x])
```

**引数**

- `lambda` — 分布の率パラメータまたはラムダ値 [`Float64`](/sql-reference/data-types/float)
- `x` — オプション引数で無視されます。この引数の唯一の目的は、同じ関数呼び出しがクエリ内で複数回使用される際に[共通部分式の除去](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**返される値**

指定された指数分布から抽出されたランダムなFloat64値を返します。[`Float64`](/sql-reference/data-types/float)

**例**

**使用例**

```sql title=Query
SELECT randExponential(1/10) FROM numbers(5)
```

```response title=Response
┌─randExponential(divide(1, 10))─┐
│              44.71628934340778 │
│              4.211013337903262 │
│             10.809402553207766 │
│              15.63959406553284 │
│             1.8148392319860158 │
└────────────────────────────────┘
```


## randFisherF {#randFisherF}

導入バージョン: v22.10

[F分布](https://en.wikipedia.org/wiki/F-distribution)から抽出されたランダムなFloat64数値を返します。

**構文**

```sql
randFisherF(d1, d2[, x])
```

**引数**

- `d1` — `X = (S1 / d1) / (S2 / d2)`におけるd1の自由度。[`Float64`](/sql-reference/data-types/float)
- `d2` — `X = (S1 / d1) / (S2 / d2)`におけるd2の自由度。[`Float64`](/sql-reference/data-types/float)
- `x` — オプション引数で、値は無視されます。この引数の唯一の目的は、同じ関数呼び出しがクエリ内で複数回使用される際に[共通部分式の削除](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**返される値**

指定されたF分布から抽出されたランダムなFloat64数値 [`Float64`](/sql-reference/data-types/float)

**例**

**使用例**

```sql title=Query
SELECT randFisherF(10, 3) FROM numbers(5)
```

```response title=Response
┌─randFisherF(10, 20)─┐
│  0.7204609609506184 │
│  0.9926258472572916 │
│  1.4010752726735863 │
│ 0.34928401507025556 │
│  1.8216216009473598 │
└─────────────────────┘
```


## randLogNormal {#randLogNormal}

導入バージョン: v22.10

[対数正規分布](https://en.wikipedia.org/wiki/Log-normal_distribution)から抽出されたランダムなFloat64値を返します。

**構文**

```sql
randLogNormal(mean, stddev[, x])
```

**引数**

- `mean` — 分布の平均値。[`Float64`](/sql-reference/data-types/float)
- `stddev` — 分布の標準偏差。[`Float64`](/sql-reference/data-types/float)
- `x` — オプション(無視されます)。この引数の唯一の目的は、同じ関数呼び出しがクエリ内で複数回使用される場合に[共通部分式の削除](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**戻り値**

指定された対数正規分布から抽出されたランダムなFloat64値を返します。[`Float64`](/sql-reference/data-types/float)

**例**

**使用例**

```sql title=Query
SELECT randLogNormal(100, 5) FROM numbers(5)
```

```response title=Response
┌─randLogNormal(100, 5)─┐
│  1.295699673937363e48 │
│  9.719869109186684e39 │
│  6.110868203189557e42 │
│  9.912675872925529e39 │
│ 2.3564708490552458e42 │
└───────────────────────┘
```


## randNegativeBinomial {#randNegativeBinomial}

導入バージョン: v22.10

[負の二項分布](https://en.wikipedia.org/wiki/Negative_binomial_distribution)から抽出されたランダムなFloat64の数値を返します。

**構文**

```sql
randNegativeBinomial(experiments, probability[, x])
```

**引数**

- `experiments` — 試行回数。[`UInt64`](/sql-reference/data-types/int-uint)
- `probability` — 各試行における失敗の確率で、`0`から`1`の間の値。[`Float64`](/sql-reference/data-types/float)
- `x` — オプションで無視されます。この引数の唯一の目的は、同じ関数呼び出しがクエリ内で複数回使用される場合に[共通部分式の削除](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**戻り値**

指定された負の二項分布から抽出されたランダムなFloat64の数値を返します。[`UInt64`](/sql-reference/data-types/int-uint)

**例**

**使用例**

```sql title=Query
SELECT randNegativeBinomial(100, .75) FROM numbers(5)
```

```response title=Response
┌─randNegativeBinomial(100, 0.75)─┐
│                              33 │
│                              32 │
│                              39 │
│                              40 │
│                              50 │
└─────────────────────────────────┘
```


## randNormal {#randNormal}

導入バージョン: v22.10

[正規分布](https://en.wikipedia.org/wiki/Normal_distribution)から抽出されたランダムなFloat64数値を返します。

**構文**

```sql
randNormal(mean, stddev[, x])
```

**引数**

- `mean` — 分布の平均値 [`Float64`](/sql-reference/data-types/float)
- `stddev` — 分布の標準偏差 [`Float64`](/sql-reference/data-types/float)
- `x` — オプション(無視されます)。この引数の唯一の目的は、同じ関数呼び出しがクエリ内で複数回使用される場合に[共通部分式の削除](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**返り値**

指定された正規分布から抽出されたランダムなFloat64数値を返します。[`Float64`](/sql-reference/data-types/float)

**例**

**使用例**

```sql title=Query
SELECT randNormal(10, 2) FROM numbers(5)
```

```response title=Response
┌──randNormal(10, 2)─┐
│ 13.389228911709653 │
│  8.622949707401295 │
│ 10.801887062682981 │
│ 4.5220192605895315 │
│ 10.901239123982567 │
└────────────────────┘
```


## randPoisson {#randPoisson}

導入バージョン: v22.10

[ポアソン分布](https://en.wikipedia.org/wiki/Poisson_distribution)から抽出されたランダムなFloat64型の数値を返します。

**構文**

```sql
randPoisson(n[, x])
```

**引数**

- `n` — 発生回数の平均値。[`UInt64`](/sql-reference/data-types/int-uint)
- `x` — 省略可能で無視されます。この引数の唯一の目的は、同じ関数呼び出しがクエリ内で複数回使用される際に[共通部分式の除去](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**返り値**

指定されたポアソン分布から抽出されたランダムなFloat64型の数値。[`UInt64`](/sql-reference/data-types/int-uint)

**例**

**使用例**

```sql title=Query
SELECT randPoisson(10) FROM numbers(5)
```

```response title=Response
┌─randPoisson(10)─┐
│               8 │
│               8 │
│               7 │
│              10 │
│               6 │
└─────────────────┘
```


## randStudentT {#randStudentT}

導入バージョン: v22.10

[スチューデントのt分布](https://en.wikipedia.org/wiki/Student%27s_t-distribution)から抽出されたランダムなFloat64数値を返します。

**構文**

```sql
randStudentT(degree_of_freedom[, x])
```

**引数**

- `degree_of_freedom` — 自由度。[`Float64`](/sql-reference/data-types/float)
- `x` — オプション引数で無視されます。この引数の唯一の目的は、同じ関数呼び出しがクエリ内で複数回使用される場合に[共通部分式の除去](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**返り値**

指定されたスチューデントのt分布から抽出されたランダムなFloat64数値を返します。[`Float64`](/sql-reference/data-types/float)

**例**

**使用例**

```sql title=クエリ
SELECT randStudentT(10) FROM numbers(5)
```

```response title=レスポンス
┌─────randStudentT(10)─┐
│   1.2217309938538725 │
│   1.7941971681200541 │
│ -0.28192176076784664 │
│   0.2508897721303792 │
│  -2.7858432909761186 │
└──────────────────────┘
```


## randUniform {#randUniform}

導入バージョン: v22.10

区間 $[\min, \max]$ から一様分布に従って抽出されたランダムなFloat64型の数値を返します。

**構文**

```sql
randUniform(min, max[, x])
```

**引数**

- `min` — 範囲の左境界(含む)。[`Float64`](/sql-reference/data-types/float)
- `max` — 範囲の右境界(含む)。[`Float64`](/sql-reference/data-types/float)
- `x` — 省略可能で無視されます。この引数の唯一の目的は、同じ関数呼び出しがクエリ内で複数回使用される際に[共通部分式の除去](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**返り値**

`min`と`max`によって形成される区間から一様分布に従って抽出されたランダムな数値を返します。[`Float64`](/sql-reference/data-types/float)

**例**

**使用例**

```sql title=Query
SELECT randUniform(5.5, 10) FROM numbers(5)
```

```response title=Response
┌─randUniform(5.5, 10)─┐
│    8.094978491443102 │
│   7.3181248914450885 │
│    7.177741903868262 │
│    6.483347380953762 │
│    6.122286382885112 │
└──────────────────────┘
```


## randomFixedString {#randomFixedString}

導入バージョン: v20.5

指定されたバイト数でランダムな固定長文字列を生成します。
返される文字は必ずしもASCII文字ではないため、印刷可能でない場合があります。

**構文**

```sql
randomFixedString(length)
```

**引数**

- `length` — 文字列の長さ(バイト単位)。[`UInt*`](/sql-reference/data-types/int-uint)

**戻り値**

ランダムなバイトで埋められた文字列を返します。[`FixedString`](/sql-reference/data-types/fixedstring)

**例**

**使用例**

```sql title=クエリ
SELECT randomFixedString(13) AS rnd, toTypeName(rnd)
```

```response title=レスポンス
┌─rnd──────┬─toTypeName(randomFixedString(13))─┐
│ j▒h㋖HɨZ'▒ │ FixedString(13)                 │
└──────────┴───────────────────────────────────┘
```


## randomPrintableASCII {#randomPrintableASCII}

導入バージョン: v20.1

指定された文字数のランダムな[ASCII](https://en.wikipedia.org/wiki/ASCII#Printable_characters)文字列を生成します。

`length < 0`を渡した場合、関数の動作は未定義です。

**構文**

```sql
randomPrintableASCII(length[, x])
```

**引数**

- `length` — 文字列の長さ(バイト単位)。[`(U)Int*`](/sql-reference/data-types/int-uint)
- `x` — 省略可能で無視されます。この引数の唯一の目的は、同じ関数呼び出しがクエリ内で複数回使用される場合に[共通部分式の削除](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**戻り値**

ASCII印字可能文字のランダムな集合からなる文字列を返します。[`String`](/sql-reference/data-types/string)

**例**

**使用例**

```sql title=Query
SELECT number, randomPrintableASCII(30) AS str, length(str) FROM system.numbers LIMIT 3
```

```response title=Response
┌─number─┬─str────────────────────────────┬─length(randomPrintableASCII(30))─┐
│      0 │ SuiCOSTvC0csfABSw=UcSzp2.`rv8x │                               30 │
│      1 │ 1Ag NlJ &RCN:*>HVPG;PE-nO"SUFD │                               30 │
│      2 │ /"+<"with:=LjJ Vm!c&hI*m#XTfzz │                               30 │
└────────┴────────────────────────────────┴──────────────────────────────────┘
```


## randomString {#randomString}

導入バージョン: v20.5

指定されたバイト数のランダムな文字列を生成します。
返される文字は必ずしもASCII文字ではないため、印刷可能でない場合があります。

**構文**

```sql
randomString(length[, x])
```

**引数**

- `length` — 文字列の長さ(バイト単位)。[`(U)Int*`](/sql-reference/data-types/int-uint)
- `x` — オプションで無視されます。この引数の唯一の目的は、同じ関数呼び出しがクエリ内で複数回使用される場合に[共通部分式の削除](/sql-reference/functions/overview#common-subexpression-elimination)を防ぐことです。[`Any`](/sql-reference/data-types)

**戻り値**

ランダムなバイトで埋められた文字列を返します。[`String`](/sql-reference/data-types/string)

**例**

**使用例**

```sql title=Query
SELECT randomString(5) AS str FROM numbers(2)
```

```response title=Response
���
�v6B�
```


## randomStringUTF8 {#randomStringUTF8}

導入バージョン: v20.5

指定されたコードポイント数のランダムな[UTF-8](https://en.wikipedia.org/wiki/UTF-8)文字列を生成します。
未割り当ての[プレーン](<https://en.wikipedia.org/wiki/Plane_(Unicode)>)(プレーン4から13)のコードポイントは返されません。
ClickHouseサーバーと通信するクライアントが、生成されたUTF-8文字列を正しく表示できない場合があります。

**構文**

```sql
randomStringUTF8(length)
```

**引数**

- `length` — コードポイント単位の文字列の長さ。[`(U)Int*`](/sql-reference/data-types/int-uint)

**戻り値**

ランダムなUTF-8コードポイントで構成された文字列を返します。[`String`](/sql-reference/data-types/string)

**例**

**使用例**

```sql title=クエリ
SELECT randomStringUTF8(13)
```

```response title=レスポンス
┌─randomStringUTF8(13)─┐
│ 𘤗𙉝д兠庇󡅴󱱎󦐪􂕌𔊹𓰛       │
└──────────────────────┘
```

<!--AUTOGENERATED_END-->
