---
date: 2024-04-22
title: Linux で LLVM と clang をビルドする方法
description: Linux で LLVM と clang をビルドするためのコマンド。
tags: ['コミュニティ', 'ツールとユーティリティ']
keywords: [貢献, llvm, clang]
---

{frontMatter.description}

{/* truncate */}

## Linux で LLVM と Clang をビルドする \{#build-llvm-and-clang-on-linux\}

ClickHouse をビルドして開発に貢献するには、[LLVM](https://llvm.org/) と [Clang](https://clang.llvm.org/) を使用する必要があります。

以下は、Linux で最新バージョンの LLVM と Clang をビルドするためのコマンドです。

```bash
git clone git@github.com:llvm/llvm-project.git
mkdir llvm-build
cd llvm-build
cmake -GNinja -DCMAKE_BUILD_TYPE:STRING=Release -DLLVM_ENABLE_PROJECTS=all -DLLVM_TARGETS_TO_BUILD=all ../llvm-project/llvm
time ninja
sudo ninja install
```
